# Changelog

- Extended the real PostgreSQL runtime slice through tenant-scoped rate-card
  publish, rating, invoice drafting, issued-invoice snapshots, tax-rate
  publish, tax assessments, collection cases and dunning events, payment
  intents, applied payment receipts, row-locked collection settlement, exact-
  zero write-offs and explicit collection settlements, cash journal proposals,
  tenant-scoped webhook subscriptions, delivery attempts, delivered outbox
  status, and atomic commercial outbox replay paths. The dedicated PostgreSQL
  suite now passes 685 tests at 100% statement/branch coverage (16,971
  statements and 5,870 branches). Webhook secrets remain process-local until
  a secure secret provider is added. The broader commercial persistence and GA
  recovery backlog remains open under issue #84.

## [Unreleased]

### Added

- Issue #87 gains its first immutable finance contracts: tenant billing periods
  advance only through authorized append-only lifecycle transitions;
  versioned exact FX rates produce frozen conversions at explicit zero-, two-,
  three-, or four-decimal target scales; and three-way reconciliation lines
  retain internal expectation, provider actual, cash actual, fees, withholding,
  and reserves separately while returning deterministic typed exceptions. The
  contracts and tests are side-effect-free foundations only; PostgreSQL
  persistence, maker-checker resolution, late adjustments, and FOCUS 1.4
  export remain open under #87 (ADR 0125).
- The #87 foundation now re-applies its domain invariants when validating raw
  contract dictionaries, preserves exact precision for large FX products and
  reconciliation deductions, enforces published decimal length limits, and
  requires source-currency evidence on reconciliation lines. Non-negative
  deductions reject signed zero, and reconciliation exception codes are
  derived from and checked against every stored comparison. The migration
  liveness query is also literalized so the protected Semgrep gate no longer
  treats the fixed internal table name as formatted SQL.
- The HTTP accept surface selects its ledger backend from the environment: `create_http_app(ledger=...)` now accepts either the deterministic `MemoryUsageLedger` reference adapter or the durable `PostgresUsageLedger` production system of record through the new `UsageLedger` union, and `metering_billing.http_app.create_default_ledger(environ=None)` builds the selected backend — `METERING_BILLING_LEDGER_BACKEND=postgres` constructs `PostgresUsageLedger` via the existing `PostgresUsageLedger.connect` convention from `METERING_BILLING_POSTGRES_DSN`, a missing or empty DSN raises a startup `ValueError` naming `METERING_BILLING_POSTGRES_DSN`, and every other value including unset keeps returning `MemoryUsageLedger()` so tests stay unchanged. Unauthenticated `GET /readyz` joins `GET /healthz` in the same dispatch style: healthy backends answer `200 {"status": "ready", "backend": "memory" | "postgres"}`, and a failing PostgreSQL probe answers `503 {"status": "not_ready", "backend": ..., "reason": "migration_history_unavailable"}` using one cheap migration-history row count (`public.metering_billing_schema_migration`) executed through the ledger's own connection and transaction conventions with no ad-hoc psycopg connections and no raw exception text. Memory stays the deterministic reference/test adapter; postgres becomes the selectable production system of record as partial progress on issue #84. Service constructor signatures, existing routes, exact-decimal money, journal boundaries, AIS pull behavior, and the #24 outbox stay unchanged. ADR 0123 documents the decision. There is no new third-party dependency, schema change, secret, or provider call on this path.
- ADR 0123 for environment-driven PostgreSQL ledger backend selection and the `/readyz` backend probe.
- Compose becomes the deployment surface: a root `Dockerfile` on `python:3.13-slim` installs only the hash-locked runtime dependency set with CI's exact pip flags (`--disable-pip-version-check --only-binary=:all: --require-hashes -r requirements-runtime.txt`), copies the package plus migration assets, runs as a non-root user, exposes port 8000, and starts `python -m metering_billing.http_app`; `compose/docker-compose.yml` fixes the project name to `metering-billing-platform` and boots `postgres_database` (PostgreSQL 18 with a `pg_isready` healthcheck and named data volume), a one-shot idempotent `schema_migration` service running the advisory-locked `scripts/migrate_postgres.py` before the API starts, and `billing_api` gated on both, serving the durable PostgreSQL system of record behind a stdlib `urllib` `/readyz` healthcheck so the image needs no curl; `compose/.env.example` documents every variable with dev-safe defaults and production guidance; `compose/k6/e2e_smoke.js` records an end-to-end baseline (ramp to 50 virtual users over 60 seconds, sustain 60 seconds, status-only checks, per-request-kind duration trends over `/healthz`, `/readyz`, and one authenticated tenant read seeded by idempotent `compose/k6/seed.py`) whose real measured numbers live in `docs/operations/load-test-baseline.md`; isolated test containers and volumes are removed after each run (ADR 0124). No new Python dependency, schema change, secret, or provider call is introduced.
- The standalone HTTP entrypoint now serves multithreaded: `main()` binds `0.0.0.0:$PORT` (default unchanged at 8000) through the new module-level `ThreadingWSGIServer` (`ThreadingMixIn` over stdlib `WSGIServer`) with daemon threads so concurrent tenant requests no longer serialize behind one request loop; behavior is otherwise byte-for-byte identical and the entrypoint test pins the threaded server class.
- ADR 0124 for the Compose deployment surface, threaded web tier, and the k6 load baseline method.

## [0.2.0] - 2026-08-25

### Added

- `operator_console` Storybook adds one same-tenant validated write-off journal fixture to the existing `JournalProposal` story using `AmountDue`, `StatusChip`, and the tenant pin. The fixture is one write-off journal (`proposal_status=validated`, exact write-off-expense debit `0.001`, AR credit `0.001`, action `wait`) whose `source_event_references` write-off matches the existing leftover remaining CollectionWriteOff fixture. Write-off remaining stays exact `0`. Leftover-apply remaining stays `19.999`. The fixture validates against the existing accounting-journal-proposal schema. Existing cash, invoice-draft, leftover, leftover-apply, and leftover-refund JournalProposal stay those presentments. Existing collection-write-off Storybook stays the write-off row presentment. `AmountDue` presents the exact write-off-expense debit string. `StatusChip` presents `validated`. Float money fails closed. `GET /v1/journal-proposals/{proposal_id}` stays the existing #14 read. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, leftover-apply journal Storybook, leftover-refund journal Storybook, leftover-refund row Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, leftover write-off settle Storybook, unused issued-invoice-void Storybook, cash journal Storybook, invoice-draft journal Storybook, leftover journal Storybook, leftover-apply journal Storybook, leftover-refund journal Storybook, and the #24 `journal_proposal.validated` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal compose, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. Leftover-apply remaining stays `19.999`. Collection-dispute remaining stays `0.003705`. Unused credit-note void stays `11.00`. Write-off remaining stays exact `0`. Settlement remaining stays exact `0`. Unused issued-invoice void inclusive `voided_amount` stays `110.00`. Cash journal debit stays `0.003705`. Invoice-draft journal morning debit stays `0.003705` / taxed inclusive `110.00`. Leftover journal debit `cash_receipt` stays `0.001` / credit `unapplied_cash` stays `0.001`. Leftover-apply journal debit `unapplied_cash` stays `0.001` / credit `accounts_receivable` stays `0.001`. Leftover-refund journal debit `unapplied_cash` stays `0.001` / credit `cash_receipt` stays `0.001`. The durable approaching fixture pin from #104–#132 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0122 for operator write-off journal Storybook presentment.
- `operator_console` Storybook adds one same-tenant validated leftover-refund journal fixture to the existing `JournalProposal` story using `AmountDue`, `StatusChip`, and the tenant pin. The fixture is one leftover-refund journal (`proposal_status=validated`, exact unapplied-cash debit `0.001`, cash-receipt credit `0.001`, action `wait`) whose `source_event_references` leftover-refund matches the existing leftover-refund UnappliedCash fixture. Leftover stays `parked`. Leftover-apply remaining stays `19.999`. The fixture validates against the existing accounting-journal-proposal schema. Existing cash, invoice-draft, leftover, and leftover-apply JournalProposal stay those presentments. Existing leftover-refund UnappliedCash Storybook stays the leftover-refund row presentment. `AmountDue` presents the exact leftover-refund credit string. `StatusChip` presents `validated`. Float money fails closed. `GET /v1/journal-proposals/{proposal_id}` stays the existing #14 read. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, leftover-apply journal Storybook, leftover-refund row Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, leftover write-off settle Storybook, unused issued-invoice-void Storybook, cash journal Storybook, invoice-draft journal Storybook, leftover journal Storybook, leftover-apply journal Storybook, and the #24 `journal_proposal.validated` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal compose, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. Leftover-apply remaining stays `19.999`. Collection-dispute remaining stays `0.003705`. Unused credit-note void stays `11.00`. Write-off remaining stays exact `0`. Settlement remaining stays exact `0`. Unused issued-invoice void inclusive `voided_amount` stays `110.00`. Cash journal debit stays `0.003705`. Invoice-draft journal morning debit stays `0.003705` / taxed inclusive `110.00`. Leftover journal debit `cash_receipt` stays `0.001` / credit `unapplied_cash` stays `0.001`. Leftover-apply journal debit `unapplied_cash` stays `0.001` / credit `accounts_receivable` stays `0.001`. The durable approaching fixture pin from #104–#131 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0121 for operator leftover-refund journal Storybook presentment.
- `operator_console` Storybook adds one same-tenant validated leftover-apply journal fixture to the existing `JournalProposal` story using `AmountDue`, `StatusChip`, and the tenant pin. The fixture is one leftover-apply journal (`proposal_status=validated`, exact unapplied-cash debit `0.001`, AR credit `0.001`, action `wait`) whose `source_event_references` leftover-apply matches the existing leftover-apply UnappliedCash fixture. Leftover-apply remaining stays `19.999`. The fixture validates against the existing accounting-journal-proposal schema. Existing cash, invoice-draft, and leftover JournalProposal stay those presentments. Existing leftover-apply UnappliedCash Storybook stays the leftover-apply row presentment. `AmountDue` presents the exact leftover-apply debit string. `StatusChip` presents `validated`. Float money fails closed. `GET /v1/journal-proposals/{proposal_id}` stays the existing #14 read. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, leftover-apply row Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, leftover write-off settle Storybook, unused issued-invoice-void Storybook, cash journal Storybook, invoice-draft journal Storybook, leftover journal Storybook, and the #24 `journal_proposal.validated` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal compose, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. Leftover-apply remaining stays `19.999`. Collection-dispute remaining stays `0.003705`. Unused credit-note void stays `11.00`. Write-off remaining stays exact `0`. Settlement remaining stays exact `0`. Unused issued-invoice void inclusive `voided_amount` stays `110.00`. Cash journal debit stays `0.003705`. Invoice-draft journal morning debit stays `0.003705` / taxed inclusive `110.00`. Leftover journal debit `cash_receipt` stays `0.001` / credit `unapplied_cash` stays `0.001`. The durable approaching fixture pin from #104–#130 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0120 for operator leftover-apply journal Storybook presentment.
- `operator_console` Storybook adds one same-tenant validated leftover journal fixture to the existing `JournalProposal` story using `AmountDue`, `StatusChip`, and the tenant pin. The fixture is one leftover journal (`proposal_status=validated`, exact cash debit `0.001`, unapplied-cash credit `0.001`, action `wait`) whose `source_event_references` leftover matches the existing parked leftover UnappliedCash fixture. Parked leftover stays `parked`. The fixture validates against the existing accounting-journal-proposal schema. Existing cash and invoice-draft JournalProposal stay those presentments. Existing leftover / unapplied-cash Storybook stays the leftover-row presentment. `AmountDue` presents the exact leftover debit string. `StatusChip` presents `validated`. Float money fails closed. `GET /v1/journal-proposals/{proposal_id}` stays the existing #14 read. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, leftover write-off settle Storybook, unused issued-invoice-void Storybook, cash journal Storybook, invoice-draft journal Storybook, and the #24 `journal_proposal.validated` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal compose, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. Leftover-apply remaining stays `19.999`. Collection-dispute remaining stays `0.003705`. Unused credit-note void stays `11.00`. Write-off remaining stays exact `0`. Settlement remaining stays exact `0`. Unused issued-invoice void inclusive `voided_amount` stays `110.00`. Cash journal debit stays `0.003705`. The durable approaching fixture pin from #104–#129 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0119 for operator leftover journal Storybook presentment.
- `operator_console` Storybook adds same-tenant validated invoice-draft journal fixtures to the existing `JournalProposal` story using `AmountDue`, `StatusChip`, and the tenant pin. The untaxed fixture is one morning invoice-draft journal (`proposal_status=validated`, exact AR debit `0.003705`, usage-revenue credit `0.003705`, action `wait`) whose `source_event_references` draft matches the existing untaxed InvoiceStatement fixture. The taxed fixture is one same-tenant invoice-draft journal (`proposal_status=validated`, exact AR debit `110.00`, usage-revenue credit `100.00`, tax-payable credit `10.00`, action `wait`) whose `source_event_references` draft matches the existing taxed IssuedInvoice and morning VAT TaxAssessment fixtures. Both fixtures validate against the existing accounting-journal-proposal schema. Existing cash JournalProposal stays the cash presentment. `AmountDue` presents the exact AR debit string. `StatusChip` presents `validated`. Float money fails closed. `GET /v1/journal-proposals/{proposal_id}` stays the existing #14 read. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, leftover write-off settle Storybook, unused issued-invoice-void Storybook, cash journal Storybook, and the #24 `journal_proposal.validated` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal compose, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. The durable approaching fixture pin from #104–#128 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0118 for operator invoice-draft journal Storybook presentment.
- `PostgresUsageLedger` persists one validated commercial invoice-draft `journal_proposal` per successful compose: `tenant_account_id`, `invoice_draft_id`, `journal_proposal_id`, exact semantic lines, `proposed_at`, `proposal_status`, source hash, and contract version. Untaxed lines debit `accounts_receivable` and credit `usage_revenue`. Taxed lines debit `accounts_receivable` inclusive, credit `usage_revenue` exclusive, and credit `tax_payable` tax. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant, draft, source hash, and contract version is `duplicate_replay` and does not insert a second row. A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay. A draft-only insert identity race classifies as the stored proposal. Rejected compose writes zero journal rows. Leftover-apply remaining stays `19.999`. Unused credit-note void stays `11.00`. Unused issued-invoice void inclusive `voided_amount` stays `110.00`. Cash journal debit stays `0.003705`. `GET /v1/journal-proposals/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Evaluation snapshots, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, collection-dispute persist, collection write-off persist, collection-case settlement persist, cash journal persist, credit journal persist, write-off journal persist, leftover journal persist, leftover-apply journal persist, leftover-refund journal persist, unused invoice-void journal persist, unused credit-note-void journal persist, cash journal Storybook, leftover / credit-note / write-off Storybooks, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0117 for durable PostgreSQL invoice-draft journal persistence.
- `PostgresUsageLedger` persists one validated commercial unused credit-note-void `journal_proposal` per successful compose: `tenant_account_id`, `issued_credit_note_void_id`, `invoice_draft_id`, `journal_proposal_id`, exact semantic lines, `proposed_at`, `proposal_status`, source hash, and contract version. Untaxed lines debit `accounts_receivable` and credit `usage_revenue`. Taxed unused notes also credit `tax_payable`. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and unused credit-note-void is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay. Rejected compose writes zero journal rows. Unused credit-note void stays `11.00`. Unused issued-invoice void inclusive `voided_amount` stays `110.00`. Leftover-apply remaining stays `19.999`. The void proposal binds the original credit journal by Billing `proposal_id` plus `credit_adjustment_id` / `issued_credit_note_id` only, fails closed if that original is missing, and never emits AIS `journal_entry_id`. `GET /v1/journal-proposals/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Invoice-draft journals, evaluation snapshots, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, collection-dispute persist, collection write-off persist, collection-case settlement persist, cash journal persist, credit journal persist, write-off journal persist, leftover journal persist, leftover-apply journal persist, leftover-refund journal persist, unused invoice-void journal persist, cash journal Storybook, leftover / credit-note / write-off Storybooks, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0116 for durable PostgreSQL unused credit-note-void journal persistence.
- `PostgresUsageLedger` persists one validated commercial unused invoice-void `journal_proposal` per successful compose: `tenant_account_id`, `issued_invoice_void_id`, `invoice_draft_id`, `journal_proposal_id`, exact semantic lines, `proposed_at`, `proposal_status`, source hash, and contract version. Untaxed lines debit `usage_revenue` and credit `accounts_receivable`. Taxed unused issues also debit `tax_payable`. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and unused invoice-void is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay. Rejected compose writes zero journal rows. Unused issued-invoice void inclusive `voided_amount` stays `110.00`. Leftover-apply remaining stays `19.999`. The void proposal binds the original invoice journal by Billing `proposal_id` only and never emits AIS `journal_entry_id`. `GET /v1/journal-proposals/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Unused credit-note-void and invoice-draft journals, evaluation snapshots, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, collection-dispute persist, collection write-off persist, collection-case settlement persist, cash journal persist, credit journal persist, write-off journal persist, leftover journal persist, leftover-apply journal persist, leftover-refund journal persist, cash journal Storybook, leftover / credit-note / write-off Storybooks, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0115 for durable PostgreSQL unused invoice-void journal persistence.
- `PostgresUsageLedger` persists one validated commercial leftover-refund `journal_proposal` per successful compose: `tenant_account_id`, `unapplied_cash_refund_id`, `invoice_draft_id`, `journal_proposal_id`, exact `unapplied_cash` debit and `cash_receipt` credit, `proposed_at`, `proposal_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and leftover refund is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay. Rejected compose writes zero journal rows. Leftover-apply remaining stays `19.999`. `GET /v1/journal-proposals/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Unused invoice-void, unused credit-note-void, and invoice-draft journals, evaluation snapshots, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, collection-dispute persist, collection write-off persist, collection-case settlement persist, cash journal persist, credit journal persist, write-off journal persist, leftover journal persist, leftover-apply journal persist, cash journal Storybook, leftover / credit-note / write-off Storybooks, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0114 for durable PostgreSQL leftover-refund journal persistence.
- `PostgresUsageLedger` persists one validated commercial leftover-apply `journal_proposal` per successful compose: `tenant_account_id`, `unapplied_cash_application_id`, `invoice_draft_id`, `journal_proposal_id`, exact `unapplied_cash` debit and `accounts_receivable` credit, `proposed_at`, `proposal_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and leftover apply is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay. Rejected compose writes zero journal rows. Leftover-apply remaining stays `19.999`. `GET /v1/journal-proposals/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Leftover-refund, unused invoice-void, unused credit-note-void, and invoice-draft journals, evaluation snapshots, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, collection-dispute persist, collection write-off persist, collection-case settlement persist, cash journal persist, credit journal persist, write-off journal persist, leftover journal persist, cash journal Storybook, leftover / credit-note / write-off Storybooks, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0113 for durable PostgreSQL leftover-apply journal persistence.
- `PostgresUsageLedger` persists one validated commercial leftover `journal_proposal` per successful compose: `tenant_account_id`, `unapplied_cash_id`, `invoice_draft_id`, `journal_proposal_id`, exact `cash_receipt` debit and `unapplied_cash` credit, `proposed_at`, `proposal_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and parked leftover is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay. Rejected compose writes zero journal rows. Parked leftover stays `parked`. `GET /v1/journal-proposals/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Leftover-apply, leftover-refund, unused invoice-void, unused credit-note-void, and invoice-draft journals, evaluation snapshots, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, collection-dispute persist, collection write-off persist, collection-case settlement persist, cash journal persist, credit journal persist, write-off journal persist, cash journal Storybook, leftover / credit-note / write-off Storybooks, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0112 for durable PostgreSQL leftover journal persistence.
- `PostgresUsageLedger` persists one validated commercial write-off `journal_proposal` per successful compose: `tenant_account_id`, `collection_write_off_id`, `invoice_draft_id`, `journal_proposal_id`, exact `write_off_expense` debit and `accounts_receivable` credit, `proposed_at`, `proposal_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and collection write-off is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay. Rejected compose writes zero journal rows. Remaining stays exact zero. `GET /v1/journal-proposals/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Leftover, refund, leftover-apply, invoice-void, and credit-note-void journals, evaluation snapshots, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, collection-dispute persist, collection write-off persist, collection-case settlement persist, cash journal Storybook, leftover / credit-note / write-off Storybooks, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0111 for durable PostgreSQL write-off journal persistence.
- `operator_console` Storybook adds a `JournalProposal` story using `AmountDue`, `StatusChip`, and the tenant pin. The fixture is one same-tenant validated morning cash journal (`proposal_status=validated`, exact cash debit `0.003705`, action `wait`) whose `source_event_references` receipt matches the existing full PaymentReceipt fixture and whose `proposal_id` matches the existing pending `journal_proposal.validated` outbox `source_id`. The fixture validates against the existing accounting-journal-proposal schema. Existing PaymentReceipt and WebhookOutboxEvent stories stay the receipt and outbox presentment. `AmountDue` presents the exact cash debit string. `StatusChip` presents `validated`. Float money fails closed. `GET /v1/journal-proposals/{proposal_id}` stays the existing #14 read. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, leftover write-off settle Storybook, unused issued-invoice-void Storybook, and the #24 `journal_proposal.validated` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal compose, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. The durable approaching fixture pin from #104–#120 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0110 for operator journal-proposal Storybook presentment.
- `operator_console` Storybook adds an `IssuedInvoiceVoid` story using `AmountDue`, `StatusChip`, and the tenant pin. The unused-void fixture is one same-tenant unused taxed issued invoice (`issued_invoice_void_status=recorded`, exact inclusive `voided_amount` `110.00`, action `wait`) whose `issued_invoice_id` matches the existing taxed IssuedInvoice fixture. The unused-void fixture validates against the existing issued-invoice-void presentment schema. Existing IssuedInvoice stories stay the issued presentment. `AmountDue` presents the exact inclusive voided string. `StatusChip` presents `recorded`. Float money fails closed. `POST /v1/issued-invoices/{id}/voids` stays the existing #63 write. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, leftover write-off settle Storybook, and the #24 `invoice.voided` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. The durable approaching fixture pin from #104–#119 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0109 for operator unused issued-invoice-void Storybook presentment.
- `operator_console` Storybook adds a leftover write-off settle-when-zero `CollectionCaseSettlement` story using `AmountDue`, `StatusChip`, and the tenant pin. The fixture is one same-tenant explicit settle of the leftover remaining write-off case (`collection_case_settlement_status=settled`, case `settled`, remaining `0`, action `wait`) whose `collection_case_id` matches the existing leftover write-off fixture. The fixture validates against the existing collection-case-settlement presentment schema. Existing morning-zero CollectionCaseSettlement stays the generic settle presentment. `AmountDue` presents the exact-zero remaining string. `StatusChip` presents `settled`. Float money fails closed. `POST /v1/collection-cases/{id}/settlements` stays the existing #46 write. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, collection-write-off Storybook, and the #24 `collection.settled` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. The durable approaching fixture pin from #104–#118 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0108 for operator leftover write-off collection-case-settlement Storybook presentment.
- `operator_console` Storybook adds a `CollectionWriteOff` story using `AmountDue`, `StatusChip`, and the tenant pin. The leftover-remaining fixture is one same-tenant recorded write-off (`collection_write_off_status=recorded`, exact leftover `write_off_amount` `0.001`, remaining `0`, case stays `open`, action `settle`). The fixture validates against the existing collection-write-off presentment schema. `AmountDue` presents the exact leftover written off and the exact-zero remaining. `StatusChip` presents `recorded`. Float money fails closed. `POST /v1/collection-cases/{id}/write-offs` stays the existing #49 write. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, collection-dispute Storybook, unused issued-credit-note-void Storybook, and the #24 `write_off.recorded` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. The durable approaching fixture pin from #104–#117 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0107 for operator collection-write-off Storybook presentment.
- `operator_console` Storybook adds an `IssuedCreditNoteVoid` story using `AmountDue`, `StatusChip`, and the tenant pin. The unused-void fixture is one same-tenant unused taxed issued credit note (`issued_credit_note_void_status=recorded`, exact `voided_amount` `11.00`, action `wait`) whose `issued_credit_note_id` matches the existing unused taxed IssuedCreditNote fixture. The unused-void fixture validates against the existing issued-credit-note-void presentment schema. Existing IssuedCreditNote and CreditNoteApplication stories stay the issued and applied presentment. `AmountDue` presents the exact voided string. `StatusChip` presents `recorded`. Float money fails closed. `POST /v1/issued-credit-notes/{id}/voids` stays the existing #72 write. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, leftover Storybook, collection-dispute Storybook, and the #24 `credit_note.voided` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. The durable approaching fixture pin from #104–#116 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0106 for operator unused issued-credit-note-void Storybook presentment.
- `operator_console` Storybook adds an `UnappliedCash` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are one same-tenant parked morning leftover (`unapplied_cash_status=parked`, exact leftover `0.001`, received/applied receipt snapshots `0.003705`), one leftover-apply to another open case (`unapplied_cash_application_status=applied`, applied `0.001`, remaining `19.999`, action `collect`), and one leftover-refund of the parked leftover (`unapplied_cash_refund_status=recorded`, leftover stays `parked`, refund `0.001`, action `wait`). The parked fixture validates against the existing unapplied-cash presentment schema. The apply fixture validates against the existing leftover-apply presentment schema. The refund fixture validates against the existing leftover-refund presentment schema. `AmountDue` presents the exact leftover, applied, or refund string. `StatusChip` presents parked versus applied versus recorded. Float money fails closed. `POST /v1/payment-receipts/{id}/unapplied-cash`, `POST /v1/collection-cases/{id}/unapplied-cash-applications`, and `POST /v1/unapplied-cash/{id}/refunds` stay the existing #54/#55/#57 writes. Persist, GET, leftover/credit-note/spend-budget/collection-dispute persist, and the #24 `unapplied_cash.applied` / `refund.recorded` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. The durable approaching fixture pin from #104–#115 is unchanged: PostgreSQL morning `rated_amount` is not memory-ledger `KNOWN_MORNING_TOTAL`. First-at stays exact equality to rated spend. #85 atomic authorization remains later.
- ADR 0105 for operator leftover / unapplied-cash Storybook presentment.
- `operator_console` Storybook adds a `CollectionDispute` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are one same-tenant held morning dispute (`collection_dispute_status=held`, case `disputed`, exact remaining `0.003705`) and one released/fail-close of that same identity (`collection_dispute_status=released`, case `open`, remaining unchanged). The held fixture validates against the existing collection-dispute presentment schema. The released fixture validates against the existing collection-dispute release presentment schema. `AmountDue` presents the exact remaining string. `StatusChip` presents held versus released. Next operator action is `wait`. A later hold of the released case still fail-closes as `collection_dispute_released`. `POST /v1/collection-cases/{id}/disputes` and `POST /v1/collection-disputes/{id}/releases` stay the existing #66/#67 writes. Persist, GET, leftover/credit-note/spend-budget persist, and the #24 `dispute.held` / `dispute.released` outbox stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, dimension-scoped budget, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Tenant API credentials stay memory-only. #85 atomic authorization remains later.
- ADR 0104 for operator collection-dispute Storybook presentment.
- `PostgresUsageLedger` persists one commercial `collection_dispute` hold per successful hold: `tenant_account_id`, `collection_case_id`, `invoice_draft_id`, optional `issued_invoice_id`, `collection_dispute_id`, exact `remaining_outstanding_amount` snapshot, `held_at`, `collection_dispute_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and collection case is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `dispute.held` outbox enqueue is healed by the next replay. Rejected hold writes zero rows. On hold `duplicate_replay`, a case still `open` or `dunning` flips to `disputed` once; already-disputed cases stay as-is. Remaining stays unchanged. Release flips the same row to `released` and stores `released_at`. A crash after the row flip and before restoring the case is healed by the next replay when the case is still `disputed`. Case status returns to `open`, or to `dunning` when stored notices already exist. `GET /v1/collection-disputes/{id}`, `GET /v1/collection-dispute-releases/{id}`, and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Write-off, leftover-apply, settle-when-zero, and void fail-close on a real disputed case after restart. Evaluation snapshots, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, leftover-refund persist, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0103 for durable PostgreSQL collection-dispute persistence.
- `PostgresUsageLedger` persists one recorded commercial `unapplied_cash_refund` leftover refund per successful refund: `tenant_account_id`, `unapplied_cash_id`, `payment_receipt_id`, `payment_intent_id`, `collection_case_id`, `unapplied_cash_refund_id`, exact `refund_amount`, exact parked `unapplied_amount` snapshot, `refunded_at`, `unapplied_cash_refund_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and parked leftover is `duplicate_replay` and does not insert a second row or mutate remaining. A crash after insert and before the existing `refund.recorded` outbox enqueue is healed by the next replay. Rejected refund writes zero rows. Refund the full parked amount. The parked leftover row stays `parked`. A leftover-refund row fail-closes leftover-apply after restart. `GET /v1/unapplied-cash-refunds/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. On leftover-apply `duplicate_replay`, remaining that still equals unused opened outstanding is reduced once; already-reduced remaining stays as-is and remaining zero does not settle. Evaluation snapshots, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, leftover-apply persist, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0102 for durable PostgreSQL leftover-refund persistence.
- `PostgresUsageLedger` persists one applied commercial `unapplied_cash_application` leftover-apply per successful apply: `tenant_account_id`, `unapplied_cash_id`, `collection_case_id`, `payment_receipt_id`, `invoice_draft_id`, `unapplied_cash_application_id`, exact `applied_amount`, `applied_at`, `unapplied_cash_application_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and parked leftover is `duplicate_replay` and does not insert a second row or reduce remaining again. A crash after insert and before the existing `unapplied_cash.applied` outbox enqueue is healed by the next replay. Rejected apply writes zero rows. Apply the full parked amount. Remaining zero does not settle; #46 remains the explicit settle-when-zero command. The parked leftover row stays `parked`. A leftover-apply row fail-closes unused invoice void after restart. Remaining outstanding is not stored on the application row. `GET /v1/unapplied-cash-applications/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Leftover refund, evaluation snapshots, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, parked leftover persist, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0101 for durable PostgreSQL leftover-apply persistence.
- `PostgresUsageLedger` persists one parked commercial `unapplied_cash` leftover per successful park: `tenant_account_id`, `payment_receipt_id`, `payment_intent_id`, `collection_case_id`, `unapplied_cash_id`, exact `unapplied_amount`, exact receipt `received_amount` / `applied_amount` snapshots, `parked_at`, `unapplied_cash_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and payment receipt is `duplicate_replay` and does not insert a second row or mutate remaining. Park leftover writes no outbox. Rejected park writes zero rows, including omitted leftover (`payment_receipt_already_consumed`). Receipt amount and case remaining stay unchanged. `GET /v1/unapplied-cash/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Leftover-apply persist, leftover refund, evaluation snapshots, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, unused-invoice-void persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0100 for durable PostgreSQL parked leftover persistence.
- `PostgresUsageLedger` persists one unused commercial `issued_invoice_void` per successful void: `tenant_account_id`, `issued_invoice_id`, `invoice_draft_id`, optional `collection_case_id`, `issued_invoice_void_id`, exact `voided_amount`, exact-zero `remaining_outstanding_amount`, `voided_at`, `issued_invoice_void_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. First accept closes an unused open or dunning case as `voided` at exact-zero remaining. Replay of the same tenant and issued invoice is `duplicate_replay` and does not insert a second row. Already-`voided` cases stay as-is. Do not reuse `settled`. A crash after insert and before the existing `invoice.voided` outbox enqueue is healed by the next replay. A crash after insert and before `mark_collection_case_voided` is healed by the next replay when the stored void's case is still `open` or `dunning` and remaining still equals the issued amount. Rejected void writes zero rows, including used cases. The issued snapshot stays `issued`. `GET /v1/issued-invoice-voids/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Evaluation snapshots, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-invoice persist, issued-credit-note persist, unused-credit-note-void persist, credit-note-application persist, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0099 for durable PostgreSQL unused issued-invoice-void persistence.
- `PostgresUsageLedger` persists one applied commercial `credit_note_application` per successful apply: `tenant_account_id`, `issued_credit_note_id`, `collection_case_id`, `invoice_draft_id`, optional `issued_invoice_id`, `credit_note_application_id`, exact `applied_amount`, `applied_at`, `credit_note_application_status`, source hashes, and contract versions. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and issued credit note is `duplicate_replay` and does not insert a second row or reduce outstanding again. A crash after insert and before the existing `credit_note.applied` outbox enqueue is healed by the next replay. Rejected apply writes zero rows, including voided notes. Remaining outstanding is not stored on the application row. `GET /v1/credit-note-applications/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Evaluation snapshots, credit-note lines, statutory numbers, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-credit-note persist, unused-void persist, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0098 for durable PostgreSQL credit-note-application persistence.
- `PostgresUsageLedger` persists one unused commercial `issued_credit_note_void` per successful void: `tenant_account_id`, `issued_credit_note_id`, `credit_adjustment_id`, `invoice_draft_id`, optional `issued_invoice_id`, `issued_credit_note_void_id`, exact `voided_amount`, `voided_at`, `issued_credit_note_void_status`, source hash, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and issued credit note is `duplicate_replay` and does not insert a second row. A crash after insert and before the existing `credit_note.voided` outbox enqueue is healed by the next replay. Rejected void writes zero rows, including already-applied notes. The issued snapshot stays `issued`. Collection remaining is unchanged because the note cannot have been applied. `GET /v1/issued-credit-note-voids/{id}` and list presentment stay the existing contracts when the row is loaded from PostgreSQL. Evaluation snapshots, credit-note lines, statutory numbers, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Issued-credit-note persist, spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization stay unchanged.
- ADR 0097 for durable PostgreSQL unused issued-credit-note-void persistence.
- `PostgresUsageLedger` persists one issued commercial `issued_credit_note` per successful issue: `tenant_account_id`, `credit_adjustment_id`, `invoice_draft_id`, optional `issued_invoice_id`, `issued_credit_note_id`, closed `credit_reason_code`, exact exclusive/tax/inclusive amounts, `issued_at`, `issued_credit_note_status`, source hashes, and contract versions. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same tenant and credit is `duplicate_replay` and does not insert a second row. A crash after insert and before the existing `credit_note.issued` outbox enqueue is healed by the next replay. Rejected issue writes zero rows. `GET /v1/issued-credit-notes/{id}`, list presentment, Storybook, and the #24 outbox stay the existing contracts when the row is loaded from PostgreSQL. Evaluation snapshots, credit-note lines, statutory numbers, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. Spend-budget publish, evaluation, over/approaching signals, and #85 atomic authorization remain later.
- ADR 0096 for durable PostgreSQL issued-credit-note persistence.
- `SpendBudgetApproachingSignalPresentmentService.present_spend_budget_approaching_signal` presents one published commercial `spend_budget` using the same `SpendBudgetEvaluationPresentmentService` remaining/over math as `GET /v1/spend-budgets/{id}/evaluation`. `GET /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the HTTP read. The body reuses the existing approaching-signal envelope plus `{webhook_outbox_events}` with zero or one existing #24 `spend_budget.approaching` webhook-outbox presentment. Same-tenant published budgets are HTTP 200. Unknown or cross-tenant is HTTP 404 with no leak. Missing tenant pin is HTTP 422. A budget whose billing account belongs to another commercial `tenant_account` is HTTP 403. Live under and over observations stay `accepted` with exact complementary remaining and zero approaching-signal rows. Live at without a prior write stays `accepted` and does not enqueue. After first-at enqueue, GET presents the current evaluation plus the stored first-at outbox row. Replay is a safe GET and writes no money fact. Float money fails closed. Notify threshold stays ADR 0082 `utilization_status=at`. `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` stays the existing #104 write. Over write/GET, GET evaluation, GET budget-status, persist, SpendBudget/RatedSpend/BudgetStatus/SpendBudgetOver/SpendBudgetApproaching Storybooks, `spend_budget.published`, and `spend_budget.over` stay unchanged. The read does not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, or add a VAT/NTS adapter. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0095 for spend-budget approaching-signal HTTP presentment.
- `operator_console` Storybook adds a `SpendBudgetApproaching` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are one same-tenant first-at accepted observation (`utilization_status=at`, exact `remaining_amount` `0`), one same-tenant under observation (accepted, zero approaching-signal rows), and one `duplicate_replay` of the same source. Observation fixtures validate against the existing approaching-signal schema. The first-at outbox fixture validates against the existing webhook-outbox presentment schema (`event_type_code` `spend_budget.approaching`). `AmountDue` presents exact `remaining_amount` and `budget_amount` strings. `StatusChip` presents `utilization_status` at versus under/over. Next operator action is `wait`. Replay HTTP body may show the current evaluation; the outbox row stays first-at-wins. `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` stays the existing #104 write. Over write/GET, GET evaluation, GET budget-status, persist, SpendBudget/RatedSpend/BudgetStatus/SpendBudgetOver Storybooks, `spend_budget.published`, and `spend_budget.over` stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, notify percentage, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0094 for operator spend-budget-approaching Storybook presentment.
- `SpendBudgetApproachingSignalService.observe_spend_budget_approaching` observes one published commercial `spend_budget` using the same `SpendBudgetEvaluationPresentmentService` remaining/over math as `GET /v1/spend-budgets/{id}/evaluation`. The documented notify threshold is exact `budget_amount` via `utilization_status=at` (ADR 0082). No stored notify percentage is added to the #82 publish contract. `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the write. Same-tenant published budgets are HTTP 200. Unknown or cross-tenant is HTTP 404 with no leak. Missing tenant pin is HTTP 422. A budget whose billing account belongs to another commercial `tenant_account` is HTTP 403. First observation with `utilization_status=at` enqueues one existing #24 `webhook_outbox_event` with canonical type `spend_budget.approaching`. `source_id` is `spend_budget_id`. The envelope `data` is a thin reference plus hash (ids, contract version, `source_payload_hash`, currency, exact `budget_amount`, exact `remaining_amount`, window instants, `spend_budget_status`, `utilization_status=at`). Over, rated lines, PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers are omitted so currencies stay unmixed. under and over write zero approaching-signal outbox rows. Replay of the same at observation is `duplicate_replay` and does not enqueue a second row. A crash after compute and before enqueue is healed by the next observe. Rejected observe writes zero rows. Existing subscriptions opt in by including `spend_budget.approaching`. HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts. GET evaluation, GET budget-status, GET over-signal, persist, Storybook, `spend_budget.published`, and `spend_budget.over` stay unchanged. The write does not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, or add a VAT/NTS adapter. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0093 for `spend_budget.approaching` on the existing commercial webhook outbox.
- `SpendBudgetOverSignalPresentmentService.present_spend_budget_over_signal` presents one published commercial `spend_budget` using the same `SpendBudgetEvaluationPresentmentService` remaining/over math as `GET /v1/spend-budgets/{id}/evaluation`. `GET /v1/spend-budgets/{spend_budget_id}/over-signal` is the HTTP read. The body reuses the existing over-signal envelope plus `{webhook_outbox_events}` with zero or one existing #24 `spend_budget.over` webhook-outbox presentment. Same-tenant published budgets are HTTP 200. Unknown or cross-tenant is HTTP 404 with no leak. Missing tenant pin is HTTP 422. A budget whose billing account belongs to another commercial `tenant_account` is HTTP 403. Live under and at observations stay `accepted` with exact `over_amount` `0` and zero over-signal rows. Live over without a prior write stays `accepted` and does not enqueue. After first-over enqueue, GET presents the current evaluation plus the stored first-over outbox row. Replay is a safe GET and writes no money fact. Float money fails closed. `POST /v1/spend-budgets/{spend_budget_id}/over-signal` stays the existing #101 write. GET evaluation, GET budget-status, persist, Storybook, and `spend_budget.published` stay unchanged. The read does not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, or add a VAT/NTS adapter. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0092 for spend-budget over-signal HTTP presentment.
- `operator_console` Storybook adds a `SpendBudgetOver` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are one same-tenant first-over accepted observation (`utilization_status=over`, exact `over_amount`), one same-tenant under observation (accepted, zero over-signal rows), and one `duplicate_replay` of the same source. Observation fixtures validate against the existing over-signal schema. The first-over outbox fixture validates against the existing webhook-outbox presentment schema (`event_type_code` `spend_budget.over`). `AmountDue` presents exact `over_amount` and `budget_amount` strings. `StatusChip` presents `utilization_status` over versus under/at. Next operator action is `wait`. Replay HTTP body may show the current evaluation; the outbox row stays first-over-wins. `POST /v1/spend-budgets/{spend_budget_id}/over-signal` stays the existing #101 write. GET evaluation, GET budget-status, persist, SpendBudget/RatedSpend/BudgetStatus Storybooks, and `spend_budget.published` stay unchanged. There is no login wall, Figma-only work, production SPA, new HTTP write, GET side-effect, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, evaluation snapshot persist, or BudgetGauge. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0091 for operator spend-budget-over Storybook presentment.
- `SpendBudgetOverSignalService.observe_spend_budget_over` observes one published commercial `spend_budget` using the same `SpendBudgetEvaluationPresentmentService` remaining/over math as `GET /v1/spend-budgets/{id}/evaluation`. `POST /v1/spend-budgets/{spend_budget_id}/over-signal` is the write. Same-tenant published budgets are HTTP 200. Unknown or cross-tenant is HTTP 404 with no leak. Missing tenant pin is HTTP 422. A budget whose billing account belongs to another commercial `tenant_account` is HTTP 403. First observation with `utilization_status=over` enqueues one existing #24 `webhook_outbox_event` with canonical type `spend_budget.over`. `source_id` is `spend_budget_id`. The envelope `data` is a thin reference plus hash (ids, contract version, `source_payload_hash`, currency, exact `budget_amount`, exact `over_amount`, window instants, `spend_budget_status`, `utilization_status=over`). Remaining, rated lines, PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers are omitted so currencies stay unmixed. under and at write zero over-signal outbox rows. Replay of the same over observation is `duplicate_replay` and does not enqueue a second row. A crash after compute and before enqueue is healed by the next observe. Rejected observe writes zero rows. Existing subscriptions opt in by including `spend_budget.over`. HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts. GET evaluation, GET budget-status, persist, Storybook, and `spend_budget.published` stay unchanged. The write does not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, or add a VAT/NTS adapter. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0090 for `spend_budget.over` on the existing commercial webhook outbox.
- `PostgresUsageLedger` persists one published commercial `spend_budget` per successful publish: `tenant_account_id`, `billing_account_id`, `spend_budget_id`, `currency_code`, exact `budget_amount`, half-open window instants, `published_at`, `spend_budget_status`, `source_payload_hash`, and contract version. Tenant-scoped composite foreign keys, PostgreSQL 18 `uuidv7()`, and exact `numeric` amounts stay in force. Replay of the same publish is `duplicate_replay` and does not insert a second row. A crash after insert and before the existing `spend_budget.published` outbox enqueue is healed by the next replay. Rejected publish writes zero rows. `GET /v1/spend-budgets/{id}`, evaluation, budget-status, Storybook, and the #24 outbox stay the existing contracts when the row is loaded from PostgreSQL. Evaluation snapshots, utilization, rated remaining/over, journals, AIS calls, VAT/NTS, `retained_earnings` / 310100, tenant auto-create, and dimension-scoped budgets are not persisted. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`; missing or mismatched pins fail closed. `MemoryUsageLedger` remains the deterministic reference adapter. #85 atomic authorization remains later.
- ADR 0089 for durable PostgreSQL published spend-budget persistence.
- Migration `0039` stores `spend_budget_status` as `published` only on the existing tenant-scoped `spend_budget` row.
- `operator_console` Storybook adds a `BudgetStatus` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are one same-tenant account with published under and over utilization (and at, because the third row stays cheap) plus a keyset `next_cursor` page. `budget_amount`, `rated_amount`, `remaining_amount`, and `over_amount` stay exact-decimal strings. Currencies stay unmixed. Next operator action is `wait`. `next_cursor` is the existing `{published_at}|{spend_budget_id}` token, not a money widget. `GET /v1/billing-accounts/{billing_account_id}/budget-status` stays the existing #97 read. Spend-budget write/evaluation, the RatedSpend story, and the #24 outbox stay unchanged. There is no login wall, Figma-only work, production SPA, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, or new HTTP write. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0088 for operator budget-status Storybook presentment.
- `operator_console` Storybook adds a `RatedSpend` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are one product-grouped morning window (`group_by` omitted or `product`) and one project-grouped window. `rated_amount` stays an exact-decimal string. Currencies stay unmixed. The existing presentment does not publish `next_operator_action`; operator copy stays “Inspect rated product, project, credential, principal, or cost-center spend, then draft an invoice.” `GET /v1/billing-accounts/{billing_account_id}/rated-spend` stays the existing #77–#81 read. Spend-budget write, evaluation, budget-status, the SpendBudget story, and the #24 outbox stay unchanged. There is no login wall, Figma-only work, production SPA, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, tenant auto-create, or new HTTP write. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0087 for operator rated-spend Storybook presentment.
- `SpendBudgetEvaluationPresentmentService.list_billing_account_budget_statuses` evaluates every published commercial `spend_budget` on one same-tenant billing account using the same rated-spend plus exact remaining/over math as one-budget evaluation. `GET /v1/billing-accounts/{billing_account_id}/budget-status` lists `{budget_statuses, next_cursor}` ordered by `published_at` then `spend_budget_id`. `page_limit` defaults to 50 and maxes at 100. Each row carries `spend_budget_id`, `currency_code`, exact `budget_amount`, `rated_amount`, `remaining_amount`, `over_amount`, `utilization_status` `under`/`at`/`over`, window instants, `spend_budget_status`, and `next_operator_action` `wait`. Currencies are never mixed in one monetary total. Same-tenant is HTTP 200. Unknown billing account is HTTP 404. Cross-tenant account is HTTP 403. Missing tenant pin is HTTP 422. Unknown or cross-tenant budgets are omitted with no leak. Replay is a safe GET and writes no money fact. Float money fails closed. The read does not persist, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a webhook or journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory identifier, or add a VAT/NTS adapter. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. POST spend-budgets, one-budget evaluation GET, #24 outbox, and Storybook stay unchanged. #85 atomic authorization remains later.
- ADR 0086 for billing-account budget-status presentment.
- `operator_console` Storybook adds a `SpendBudget` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are one published commercial budget under utilization, one at, and one over. `budget_amount`, `rated_amount`, `remaining_amount`, and `over_amount` stay exact-decimal strings. `utilization_status` is `under`, `at`, or `over`. Next operator action is `wait`. `GET /v1/spend-budgets/{spend_budget_id}` and `GET /v1/spend-budgets/{spend_budget_id}/evaluation` stay the existing #82/#93 reads. `POST` spend-budgets and the #24 outbox stay unchanged. There is no login wall, Figma-only work, production SPA, journal, AIS call, VAT/NTS adapter, `retained_earnings` or 310100, dimension-scoped budget, tenant auto-create, or new HTTP write. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0085 for operator spend-budget Storybook presentment.
- First successful `SpendBudgetService.publish_spend_budget` enqueues one existing #24 `webhook_outbox_event` with canonical type `spend_budget.published`. `source_id` is `spend_budget_id`. The envelope `data` is a thin reference plus hash (ids, contract version, `source_payload_hash`, currency, exact `budget_amount`, window instants, `published_at`, `spend_budget_status`). Rated spend, remaining/over, utilization, lines, PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers are omitted. Replay of the same publish does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay. Existing subscriptions opt in by including `spend_budget.published`. HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts. Rejected publish writes zero outbox rows. The write does not persist evaluation, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, or add a VAT/NTS adapter. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`. #85 atomic authorization remains later.
- ADR 0084 for `spend_budget.published` on the existing commercial webhook outbox.
- Journal compose fail-closes when any `accounting_journal_proposal` line `debit_amount` or `credit_amount` has more than six significant fractional digits. The stored Exact Decimal is not rounded, truncated, or coerced into `numeric(38, 6)`. Integers, six-place amounts, and trailing zeros that do not change the value (`0.0037050`, `1.0000000`) still compose. A non-zero seventh place, including `0.0000001` and `0.0037051`, is `journal_line_amount_invalid` and writes zero proposals. Credit accept of an unpostable amount is `credit_amount_invalid` and writes zero credits and journals. Ledger insert raises after debit-XOR-credit so a bypass cannot persist an unpostable line. `GET /v1/journal-proposals` stays the AIS pull. `proposal_status` stays `validated`. Semantic roles only. No `retained_earnings`, 310100, VAT register, NTS adapter, or AIS posting. Float money stays Exact Decimal fail-closed. `X-CWL-Tenant-Reference` stays pinned to the commercial `tenant_account`.
- ADR 0083 for journal compose fail-close at six fractional digits so AIS Draft #2 never receives an unpostable proposal.
- `SpendBudgetEvaluationPresentmentService.present_spend_budget_evaluation` compares one published commercial `spend_budget` to already-rated spend for the same tenant, billing account, half-open window, and currency. The read reuses `RatedSpendPresentmentService` with `group_by=product` and sums only same-currency product rows. `budget_amount`, `rated_amount`, `remaining_amount`, and `over_amount` are exact Decimal strings. `remaining_amount` and `over_amount` are complementary non-negative amounts. `utilization_status` is `under`, `at`, or `over`. Next operator action is `wait`. `GET /v1/spend-budgets/{spend_budget_id}/evaluation` is HTTP 200 for the same tenant and HTTP 404 across tenants or unknown identifiers, with no leak. Missing tenant is HTTP 422. A stored budget whose billing account belongs to another commercial `tenant_account` is HTTP 403. Replay is a safe GET and writes no money fact. Float money fails closed. The read does not persist, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a webhook or journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory identifier, or add a VAT/NTS adapter. #85 atomic authorization remains later. #7 / #8 / #62 / #75 / #77–#81 / #82 stay unchanged.
- ADR 0082 for commercial spend-budget evaluation against already-rated spend.
- `SpendBudgetService.publish_spend_budget` persists one append-only commercial `spend_budget` for one same-tenant billing account, half-open window, and currency. Identity is `(tenant_account_id, billing_account_id, window_started_at, window_ended_at, currency_code, source_payload_hash, spend_budget_contract_version)`. Internal primary key is opaque generated `spend_budget_id`. `budget_amount` is an exact Decimal greater than zero. The service computes `source_payload_hash` from the canonical payload; a caller-supplied hash must match. Replay of the same identity returns the stored `spend_budget_id` as `duplicate_replay` and does not grow the store. A later distinct amount or hash appends a new row. Status is `published` only. `POST /v1/billing-accounts/{billing_account_id}/spend-budgets` is the write. Missing tenant is HTTP 422. Unknown billing account is HTTP 404. Cross-tenant account is HTTP 403. Float money, zero/negative amount, illegal window, and illegal currency fail closed. PAN, CVC, and provider secrets are refused. `SpendBudgetPresentmentService` projects the stored row. `GET /v1/spend-budgets/{spend_budget_id}` is HTTP 200 for the same tenant and HTTP 404 across tenants. `GET /v1/spend-budgets` lists `{spend_budgets, next_cursor}` ordered by `published_at` then `spend_budget_id`. `page_limit` defaults to 50 and maxes at 100. Next operator action is `wait`. The write does not compare rated spend, stop rating, ingest, or invoice draft, emit a webhook or journal, call AIS, invent a dimension-scoped budget, emit `retained_earnings` or 310100, or invent a statutory identifier. #7 / #8 / #62 / #75 / #77–#81 stay a read.
- ADR 0079 for commercial spend-budget publish on one billing account.
- Existing `RatedSpendPresentmentService.present_rated_spend` accepts optional `group_by=product|project|credential|principal|cost_center` on the same GET. Omitting `group_by` or sending `group_by=product` keeps the #77 rows: one per `(currency_code, product_code)` with no `project_reference`, `credential_reference`, `billing_principal_reference`, or `cost_center_reference`. `group_by=project` stays the #78 rows. `group_by=credential` stays the #79 rows. `group_by=principal` stays the #80 rows. `group_by=cost_center` publishes one row per `(currency_code, product_code, cost_center_reference)` whose `cost_center_reference` is the stored exclusive-account usage-event cost-center URN in that window. Usage without `cost_center_reference` is omitted from the cost-center grouping. Mixed cost centers omit the run so the read cannot invent a split. Unknown `group_by` is HTTP 422 `request_invalid`. Money still comes only from stored rating-run and exclusive invoice-draft lines. The read does not re-rate, invent a unit price, include unrated usage, or write a money fact. No spend-budget write, journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning is added.
- ADR 0078 for optional `group_by=cost_center` on the existing rated-spend GET.
- Existing `RatedSpendPresentmentService.present_rated_spend` accepts optional `group_by=product|project|credential|principal` on the same GET. Omitting `group_by` or sending `group_by=product` keeps the #77 rows: one per `(currency_code, product_code)` with no `project_reference`, `credential_reference`, or `billing_principal_reference`. `group_by=project` stays the #78 rows. `group_by=credential` stays the #79 rows. `group_by=principal` publishes one row per `(currency_code, product_code, billing_principal_reference)` whose `billing_principal_reference` is the stored exclusive-account usage-event principal URN in that window, resolved from stored `billing_principal_id`. Mixed principals omit the run so the read cannot invent a split. An unresolved `billing_principal_id` omits the run. Unknown `group_by` is HTTP 422 `request_invalid`. Money still comes only from stored rating-run and exclusive invoice-draft lines. The read does not re-rate, invent a unit price, include unrated usage, or write a money fact. No `group_by=cost_center`, spend-budget write, journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning is added.
- ADR 0077 for optional `group_by=principal` on the existing rated-spend GET.
- Existing `RatedSpendPresentmentService.present_rated_spend` accepts optional `group_by=product|project|credential` on the same GET. Omitting `group_by` or sending `group_by=product` keeps the #77 rows: one per `(currency_code, product_code)` with no `project_reference` or `credential_reference`. `group_by=project` stays the #78 rows: one per `(currency_code, product_code, project_reference)`. `group_by=credential` publishes one row per `(currency_code, product_code, credential_reference)` whose `credential_reference` is the stored exclusive-account usage-event credential URN in that window. Usage without `credential_reference` is omitted from the credential grouping. Mixed credentials omit the run so the read cannot invent a split. Unknown `group_by` is HTTP 422 `request_invalid`. Money still comes only from stored rating-run and exclusive invoice-draft lines. The read does not re-rate, invent a unit price, include unrated usage, or write a money fact. No `group_by=principal`, `group_by=cost_center`, spend-budget write, journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning is added.
- ADR 0076 for optional `group_by=credential` on the existing rated-spend GET.
- Existing `RatedSpendPresentmentService.present_rated_spend` accepts optional `group_by=product|project` on the same GET. Omitting `group_by` or sending `group_by=product` keeps the #77 rows: one per `(currency_code, product_code)` with no `project_reference`. `group_by=project` publishes one row per `(currency_code, product_code, project_reference)` whose `project_reference` is the stored exclusive-account usage URN in that window. Usage without `project_reference` is omitted from the project grouping. Mixed projects omit the run so the read cannot invent a split. Unknown `group_by` is HTTP 422 `request_invalid`. Money still comes only from stored rating-run and exclusive invoice-draft lines. The read does not re-rate, invent a unit price, include unrated usage, or write a money fact. No `group_by=credential`, spend-budget write, journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning is added.
- ADR 0075 for optional `group_by=project` on the existing rated-spend GET.
- `RatedSpendPresentmentService.present_rated_spend` projects already-rated spend for one billing account and half-open window. Identity is the tenant pin plus `billing_account_id` plus the query window. Money comes only from stored `rating_run` rows whose window equals the query and from exclusive `invoice_draft` lines attributed only to that account. Mixed-account and lineless drafts are omitted. Unrated usage is omitted. Rows group by `(currency_code, product_code)` and never mix currencies. `rated_amount` is the stored rated or exclusive draft line amount as an exact Decimal string. The read does not re-rate or invent a unit price. `GET /v1/billing-accounts/{billing_account_id}/rated-spend?window_started_at=&window_ended_at=` is the HTTP read. Missing tenant is HTTP 422. Missing account is HTTP 404. Cross-tenant account is HTTP 403. An illegal window is HTTP 422. Replay is a safe GET and writes no money fact. No `group_by`, spend-budget write, journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning is added.
- ADR 0074 for billing-account rated-spend presentment grouped by product.
- `operator_console` Storybook adds an `AccountStatement` story using `AmountDue`, `StatusChip`, and the tenant pin. Fixtures are a settled account statement and a same-currency statement with exact inclusive non-zero `voided_invoice_total` and `voided_credit_total`. Amounts stay exact-decimal strings. `GET /v1/billing-accounts/{billing_account_id}/statement` stays the existing #62/#75 read. There is no login wall, Figma-only work, production SPA, Stripe, AIS call, or new HTTP write.
- ADR 0073 for operator account-statement Storybook presentment.
- Existing `AccountStatementPresentmentService.present_account_statement` currency rows now include exact inclusive `voided_invoice_total` and `voided_credit_total`. `issued_invoice_total` stays the issued snapshot total and is not reduced by unused issued-invoice voids. `applied_credit_total` stays applied credits only; unused issued-credit-note voids are not counted as applied. Attribution still uses invoice-draft lines exclusive to the requested billing account. Mixed-account and lineless drafts stay omitted. `GET /v1/billing-accounts/{billing_account_id}/statement` stays a safe read and writes no money fact. Missing tenant is HTTP 422. Missing account is HTTP 404. Cross-tenant account is HTTP 403. No journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, or collection-status flip is added.
- ADR 0072 for unused issued-invoice and issued-credit-note void totals on the existing account statement.
- `AccountingExportService.propose_credit_note_void_journal` composes one validated reverse of the #17/#52 credit journal from a stored unused `issued_credit_note_void`. Identity is `(tenant_account_id, issued_credit_note_void_id)`. Replay returns the same `proposal_id` as `duplicate_replay` and does not grow the store. Idempotency key is `{tenant_reference}:issued_credit_note_void:{issued_credit_note_void_id}:{void.source_payload_hash}:v{issued_credit_note_void_contract_version}`. Untaxed lines debit semantic `accounts_receivable` and credit semantic `usage_revenue` for the exact tax-inclusive voided amount. Taxed unused notes also credit `tax_payable` on the same journal for the issued exclusive/tax split. The original credit journal is bound by Billing `proposal_id` plus `credit_adjustment_id` / `issued_credit_note_id` only (`reversed_journal_proposal_id` on the Python result). Missing original Billing proposal fails closed. `proposal_status` stays `validated` and is never `posted`. `POST /v1/issued-credit-note-voids/{issued_credit_note_void_id}/journal-proposals` is the explicit compose. AIS pull stays existing GET journal-proposal routes. First compose enqueues one existing `journal_proposal.validated` outbox event. Collection, issued-credit-note, and void status are unchanged. Fail closed on missing/cross-tenant void, missing tenant, already-applied note, currency mismatch, and zero, negative, or IEEE amounts. No AIS call, statutory account ID, `journal_entry_id`, new webhook type, PSP, VAT register, NTS filing, 연말정산, or posted flip is added.
- ADR 0071 for credit-note void journal proposal from a stored unused issued-credit-note void.
- First successful `IssuedCreditNoteVoidService.void_issued_credit_note` enqueues one existing #24 `webhook_outbox_event` with canonical type `credit_note.voided`.  `source_id` is `issued_credit_note_void_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, hash, currency, exact tax-inclusive voided amount, `voided_at`, `issued_credit_note_void_status`, optional `issued_invoice_id`).  Remaining outstanding, collection identity, lines, and AIS ids are omitted.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same void does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `credit_note.voided`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected void writes zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, refund, write-off, or settlement rewrite is added.
- ADR 0070 for `credit_note.voided` on the existing commercial webhook outbox.
- `IssuedCreditNoteVoidService.void_issued_credit_note` records one commercial void of an unused same-tenant `issued_credit_note`. Identity is `(tenant_account_id, issued_credit_note_id)`. Replay returns the stored `issued_credit_note_void_id` as `duplicate_replay`. `voided_amount` is the issued tax-inclusive credit in the same currency. Status is `recorded`. The issued snapshot stays `issued`. Fail closed when the note is missing or cross-tenant, already applied, already voided (replay only), currency mismatches, or the tenant is missing. Collection remaining is unchanged because the note cannot have been applied. After a void exists, apply fail-closes as `issued_credit_note_voided`. `POST /v1/issued-credit-notes/{issued_credit_note_id}/voids` is the nested void command and refuses PAN and provider secrets. `IssuedCreditNoteVoidPresentmentService.present_issued_credit_note_void` projects the stored row. `GET /v1/issued-credit-note-voids/{issued_credit_note_void_id}` is HTTP 200 for the same tenant and 404 across tenants. `GET /v1/issued-credit-note-voids` lists `{issued_credit_note_voids, next_cursor}` ordered by `voided_at` then `issued_credit_note_void_id`. Missing tenant is HTTP 422. No journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory account, negative invoice, or AIS call is added.
- ADR 0069 for commercial issued-credit-note void of an unused note.
- Existing `IssuedCreditNotePresentmentService.present_issued_credit_note` item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment still reproduces the frozen credit exclusive/tax split.  The field is omitted when no assessment exists, the assessment cannot be split, or a later assessment no longer matches.  List summaries stay `{issued_credit_note_id, credit_adjustment_id, currency_code, tax_inclusive_amount, issued_at, next_operator_action}`.  `GET /v1/issued-credit-notes/{issued_credit_note_id}` and `GET /v1/issued-credit-notes` stay the existing #43 reads.  Missing tenant is HTTP 422.  Missing or cross-tenant note is HTTP 404.  Credit-note amounts stay the stored snapshot; the read does not persist `tax_assessment_id` on `issued_credit_note`, invent amounts, a VAT register, 세금계산서, NTS/hometax field, statutory tax invoice number, journal, webhook, PSP, or AIS call.
- ADR 0068 for optional stored tax-assessment identity on existing issued-credit-note presentment.
- Existing `IssuedInvoicePresentmentService.present_issued_invoice` item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment exclusive/tax/inclusive amounts still match the frozen issued snapshot.  The field is omitted when no assessment exists or a later assessment no longer matches.  List summaries stay `{issued_invoice_id, invoice_draft_id, currency_code, tax_inclusive_amount, issued_at, next_operator_action}`.  `GET /v1/issued-invoices/{issued_invoice_id}` and `GET /v1/issued-invoices` stay the existing #41 reads.  Missing tenant is HTTP 422.  Missing or cross-tenant invoice is HTTP 404.  Issued amounts stay the stored snapshot; the read does not persist `tax_assessment_id` on `issued_invoice`, invent amounts, a VAT register, 세금계산서, NTS/hometax field, statutory tax invoice number, journal, webhook, PSP, void rewrite, statement rewrite, or AIS call.
- ADR 0067 for optional stored tax-assessment identity on existing issued-invoice presentment.
- First successful `CollectionDisputeReleaseService.release_collection_dispute` enqueues one existing #24 `webhook_outbox_event` with canonical type `dispute.released`.  `source_id` is `collection_dispute_id` on the same hold row.  The envelope `data` is a thin reference plus hash (ids, contract version, hash, currency, exact remaining outstanding at release, `released_at`, `collection_dispute_status`, optional `issued_invoice_id`).  Collection-case status, operator action, outcome codes, `held_at`, and dispute-reason blobs are omitted.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same release does not enqueue a second row; a crash after flip and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `dispute.released`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected or not-released rows write zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, refund, write-off, or settlement rewrite is added.
- ADR 0066 for `dispute.released` on the existing commercial webhook outbox.
- First successful `CollectionDisputeService.hold_collection_case` enqueues one existing #24 `webhook_outbox_event` with canonical type `dispute.held`.  `source_id` is `collection_dispute_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, hash, currency, exact remaining outstanding at hold, `held_at`, `collection_dispute_status`, optional `issued_invoice_id`).  Collection-case status, operator action, outcome codes, and dispute-reason blobs are omitted.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same hold does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `dispute.held`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected hold writes zero outbox rows.  `dispute.released` is not added.  No AIS call, journal/`proposal_status` flip, statutory numbering, refund, write-off, or settlement rewrite is added.
- ADR 0065 for `dispute.held` on the existing commercial webhook outbox.
- `CollectionDisputeReleaseService.release_collection_dispute` releases one held same-tenant `collection_dispute` in place. Identity is `(tenant_account_id, collection_dispute_id)`. Replay returns the stored `collection_dispute_id` as `duplicate_replay` and never changes remaining outstanding. Status on the hold becomes `released`. Case status returns to `open`, or to `dunning` when stored notices already exist; `settled` and `voided` are not reused. Remaining outstanding stays the current exact-decimal snapshot in the same currency. After release, dunning, payment receipt, credit apply, leftover apply, write-off, settle-when-zero, and void follow the existing open-case rules. A later hold of the same case fail-closes as `collection_dispute_released`. `POST /v1/collection-disputes/{collection_dispute_id}/releases` is the nested release command and refuses PAN and provider secrets. `CollectionDisputeReleasePresentmentService.present_collection_dispute_release` projects the stored release and current remaining. `GET /v1/collection-dispute-releases/{collection_dispute_id}` is HTTP 200 for the same tenant and 404 across tenants or still-held rows. `GET /v1/collection-dispute-releases` lists `{collection_dispute_releases, next_cursor}` ordered by `released_at` then `collection_dispute_id`. Missing tenant is HTTP 422. No journal, webhook, PSP, refund, write-off rewrite, void rewrite, statement rewrite, AIS call, or statutory numbering is added.
- ADR 0064 for commercial collection dispute release of one held dispute.
- `CollectionDisputeService.hold_collection_case` records one commercial dispute hold of an open or dunning same-tenant `collection_case`. Identity is `(tenant_account_id, collection_case_id)`. Replay returns the stored `collection_dispute_id` as `duplicate_replay` and never changes remaining outstanding. Status on the hold is `held`. Case status becomes `disputed`; `settled` and `voided` are not reused. Remaining outstanding stays the current exact-decimal snapshot in the same currency. New dunning fails closed as `collection_case_disputed`; replay of a notice that already existed before the hold stays `#10` `duplicate_replay`. Payment receipt, credit apply, leftover apply, write-off, settle-when-zero, and void fail closed while held. `POST /v1/collection-cases/{collection_case_id}/disputes` is the nested hold command and refuses PAN and provider secrets. `CollectionDisputePresentmentService.present_collection_dispute` projects the stored hold and current remaining. `GET /v1/collection-disputes/{collection_dispute_id}` is HTTP 200 for the same tenant and 404 across tenants. `GET /v1/collection-disputes` lists `{collection_disputes, next_cursor}` ordered by `held_at` then `collection_dispute_id`. Missing tenant is HTTP 422. No journal, webhook, PSP, refund, write-off rewrite, void rewrite, statement rewrite, AIS call, or statutory numbering is added. Release is a later slice.
- ADR 0063 for commercial collection dispute hold of one open case.
- `AccountingExportService.propose_void_journal` emits one balanced revenue/AR reverse `accounting_journal_proposal` from a stored `issued_invoice_void`.  Identity is `(tenant_account_id, issued_invoice_void_id)`.  Replay returns the stored `proposal_id` as `duplicate_replay` and never grows the store.  Untaxed lines debit semantic `usage_revenue` and credit semantic `accounts_receivable` for the exact inclusive voided amount.  Taxed unused issues also debit `tax_payable` on the same journal.  The original invoice journal is bound by Billing `proposal_id` / stored invoice-draft journal identity only.  `proposal_status` stays `validated` and is never `posted`.  The AIS-compatible idempotency key is `{tenant}:issued_invoice_void:{issued_invoice_void_id}:{void.source_payload_hash}:v{version}`.  `POST /v1/issued-invoice-voids/{issued_invoice_void_id}/journal-proposals` is the explicit compose command because #63 shipped without compose.  AIS pull stays existing `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Fail closed on missing/cross-tenant void, currency mismatch, and zero/negative/IEEE amounts.  Collection and issued-invoice status stay unchanged.  No AIS call, `posted` status, statutory account ID, `journal_entry_id`, new webhook type, PSP, write-off, settlement rewrite, or negative invoice is added.
- ADR 0062 for void journal proposal from a stored issued-invoice void.
- First successful `IssuedInvoiceVoidService.void_issued_invoice` enqueues one existing #24 `webhook_outbox_event` with canonical type `invoice.voided`.  `source_id` is `issued_invoice_void_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, hash, currency, exact voided amount, `voided_at`, `issued_invoice_void_status`, optional `collection_case_id`).  Remaining outstanding and collection-case status are omitted.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same void does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `invoice.voided`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected void writes zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, refund, write-off, or settlement rewrite is added.
- ADR 0061 for `invoice.voided` on the existing commercial webhook outbox.
- `IssuedInvoiceVoidService.void_issued_invoice` records one commercial void of an unused same-tenant `issued_invoice`. Identity is `(tenant_account_id, issued_invoice_id)`. Replay returns the stored `issued_invoice_void_id` as `duplicate_replay` and never re-closes a collection case. `voided_amount` is the issued tax-inclusive amount in the same currency. Status is `recorded`. The issued snapshot stays `issued`. Fail closed unless the related `collection_case` has had no payment receipt, credit-note apply, unapplied-cash apply, or write-off, and remaining still equals the issued amount. An unused open or dunning case closes as `voided` at exact-zero remaining; `settled` is not reused. Settle-when-zero then fail-closes as `collection_case_voided`. `POST /v1/issued-invoices/{issued_invoice_id}/voids` is the nested void command and refuses PAN and provider secrets. `IssuedInvoiceVoidPresentmentService.present_issued_invoice_void` projects the stored row and current remaining. `GET /v1/issued-invoice-voids/{issued_invoice_void_id}` is HTTP 200 for the same tenant and 404 across tenants. `GET /v1/issued-invoice-voids` lists `{issued_invoice_voids, next_cursor}` ordered by `voided_at` then `issued_invoice_void_id`. Missing tenant is HTTP 422. No journal, webhook, PSP, refund, write-off rewrite, statement rewrite, AIS call, or statutory numbering is added.
- ADR 0060 for commercial issued-invoice void of an unused issue.
- `AccountStatementPresentmentService.present_account_statement` projects one billing-account commercial statement from stored facts. Identity is the tenant pin plus `billing_account_id`. Totals group by `currency_code` and never mix currencies. Each currency row carries exact inclusive `issued_invoice_total`, `open_collection_remaining`, `applied_credit_total`, `write_off_total`, `parked_unapplied_cash`, and `refunded_unapplied_cash`. Money is attributed only through invoice-draft lines that belong exclusively to that account. `GET /v1/billing-accounts/{billing_account_id}/statement` is the HTTP read. Missing tenant is HTTP 422. Missing account is HTTP 404. Cross-tenant account is HTTP 403. Replay is a safe GET and writes no money fact. No journal, webhook, PSP, void, dispute, settlement rewrite, AIS call, statutory numbering, or new money fact is added.
- ADR 0059 for billing-account statement presentment from stored commercial facts.
- `AccountingExportService.propose_unapplied_cash_application_journal` emits one balanced unapplied-cash/AR `accounting_journal_proposal` from a stored `unapplied_cash_application`.  Identity is `(tenant_account_id, unapplied_cash_application_id)`.  Replay returns the stored `proposal_id` as `duplicate_replay` and never grows the store.  Lines debit semantic `unapplied_cash` and credit semantic `accounts_receivable` for the exact applied inclusive amount.  `proposal_status` stays `validated` and is never `posted`.  The AIS-compatible idempotency key is `{tenant}:unapplied_cash_application:{unapplied_cash_application_id}:{source_payload_hash}:v{version}`.  `POST /v1/unapplied-cash-applications/{unapplied_cash_application_id}/journal-proposals` is the explicit compose command because #55 shipped without compose.  AIS pull stays existing `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Fail closed on missing/cross-tenant application, currency mismatch, and zero/negative/IEEE amounts.  Leftover, apply, refund, payment receipts, write-offs, and settlements stay unchanged.  No AIS call, `posted` status, statutory account ID, new webhook type, PSP, write-off, settlement command, payment-receipt rewrite, park rewrite, or refund rewrite is added.
- ADR 0058 for leftover-apply journal proposal from a stored unapplied-cash application.
- `AccountingExportService.propose_unapplied_cash_journal` emits one balanced cash/unapplied-cash `accounting_journal_proposal` from a stored parked `unapplied_cash`.  Identity is `(tenant_account_id, unapplied_cash_id)`.  Replay returns the stored `proposal_id` as `duplicate_replay` and never grows the store.  Lines debit semantic `cash_receipt` and credit semantic `unapplied_cash` for the exact parked leftover inclusive amount.  `proposal_status` stays `validated` and is never `posted`.  The AIS-compatible idempotency key is `{tenant}:unapplied_cash:{unapplied_cash_id}:{source_payload_hash}:v{version}`.  `POST /v1/unapplied-cash/{unapplied_cash_id}/journal-proposals` is the explicit compose command because #54 shipped without compose.  AIS pull stays existing `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Fail closed on missing/cross-tenant leftover, leftover not parked, currency mismatch, and zero/negative/IEEE amounts.  Leftover, refund, payment receipts, write-offs, and settlements stay unchanged.  No AIS call, `posted` status, statutory account ID, new webhook type, PSP, write-off, settlement command, payment-receipt rewrite, or refund rewrite is added.
- ADR 0057 for leftover journal proposal from parked unapplied cash.
- `AccountingExportService.propose_refund_journal` emits one balanced unapplied-cash/cash `accounting_journal_proposal` from a stored `unapplied_cash_refund`.  Identity is `(tenant_account_id, unapplied_cash_refund_id)`.  Replay returns the stored `proposal_id` as `duplicate_replay` and never grows the store.  Lines debit semantic `unapplied_cash` and credit semantic `cash_receipt` for the exact refund inclusive amount.  `proposal_status` stays `validated` and is never `posted`.  The AIS-compatible idempotency key is `{tenant}:unapplied_cash_refund:{unapplied_cash_refund_id}:{source_payload_hash}:v{version}`.  `POST /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}/journal-proposals` is the explicit compose command because #57 shipped without compose.  AIS pull stays existing `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Fail closed on missing/cross-tenant refund, currency mismatch, and zero/negative/IEEE amounts.  Leftover, refund, payment receipts, write-offs, and settlements stay unchanged.  No AIS call, `posted` status, statutory account ID, new webhook type, PSP, write-off, settlement command, or payment-receipt rewrite is added.
- ADR 0056 for refund journal proposal from a stored leftover refund.
- First successful `UnappliedCashRefundService.refund_unapplied_cash` enqueues one existing #24 `webhook_outbox_event` with canonical type `refund.recorded`.  `source_id` is `unapplied_cash_refund_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, hash, currency, exact refund amount, `refunded_at`, `unapplied_cash_refund_status`).  Payment-intent id, collection-case id, parked leftover snapshot, and leftover status are omitted.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same refund does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `refund.recorded`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected refund writes zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, write-off, settlement command, PSP capture, or payment-receipt rewrite is added.
- ADR 0055 for `refund.recorded` on the existing commercial webhook outbox.
- `UnappliedCashRefundService.refund_unapplied_cash` records one commercial refund of parked leftover. Identity is `(tenant_account_id, unapplied_cash_id)`. Replay returns the stored `unapplied_cash_refund_id` as `duplicate_replay` and never writes a second row. The refund uses the full parked amount. Omitting `refund_amount` uses the parked leftover. A supplied amount must equal the parked leftover. The parked leftover row stays `parked`; refund uniqueness consumes it. `POST /v1/unapplied-cash/{unapplied_cash_id}/refunds` is the nested refund command and refuses PAN and provider secrets. `UnappliedCashRefundPresentmentService.present_unapplied_cash_refund` projects the stored row and current leftover status. `GET /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}` is HTTP 200 for the same tenant and 404 across tenants. `GET /v1/unapplied-cash-refunds` lists `{unapplied_cash_refunds, next_cursor}` ordered by `refunded_at` then `unapplied_cash_refund_id`. Fail closed on missing/cross-tenant leftover, leftover already applied, leftover not parked, currency mismatch, zero/negative leftover, mismatched amount, and IEEE leftover. Apply fail-closes when a refund already exists. Journals, outbox, payment receipts, write-offs, settlements, applications, and parked leftover rows stay unchanged. No journal, webhook, write-off, settlement, credit note, dunning engine, PSP capture, AIS call, or statutory numbering is added. `refund.recorded` is a later slice.
- ADR 0054 for unapplied-cash refund of parked leftover.
- First successful `UnappliedCashApplicationService.apply_unapplied_cash` enqueues one existing #24 `webhook_outbox_event` with canonical type `unapplied_cash.applied`.  `source_id` is `unapplied_cash_application_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, hash, currency, exact applied amount, `applied_at`, `unapplied_cash_application_status`, optional `issued_invoice_id`).  Remaining outstanding is omitted because it is not stored on the application row.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, statutory identifiers, and current case status.  Replay of the same application does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `unapplied_cash.applied`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected apply writes zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, write-off, settlement command, or payment-receipt rewrite is added.
- ADR 0053 for `unapplied_cash.applied` on the existing commercial webhook outbox.
- `UnappliedCashApplicationService.apply_unapplied_cash` applies one parked `unapplied_cash` leftover onto one same-tenant open `collection_case`. Identity is `(tenant_account_id, unapplied_cash_id)`. Replay returns the stored `unapplied_cash_application_id` as `duplicate_replay` and never double-reduces outstanding. The apply uses the full parked amount. Omitting `applied_amount` uses the parked leftover. A supplied amount must equal the parked leftover. Remaining outstanding is reduced by the exact applied inclusive amount. Case status stays `open` or `dunning`; #46 remains the explicit settle-when-zero command. The parked leftover row stays `parked`; the application uniqueness consumes it. `POST /v1/collection-cases/{collection_case_id}/unapplied-cash-applications` is the nested apply command and refuses PAN and provider secrets. `UnappliedCashApplicationPresentmentService.present_unapplied_cash_application` projects the stored row and current remaining outstanding. `GET /v1/unapplied-cash-applications/{unapplied_cash_application_id}` is HTTP 200 for the same tenant and 404 across tenants. `GET /v1/unapplied-cash-applications` lists `{unapplied_cash_applications, next_cursor}` ordered by `applied_at` then `unapplied_cash_application_id`. Fail closed on missing/cross-tenant leftover or case, leftover already applied, already settled case, currency mismatch, apply greater than parked or remaining, zero/negative leftover, negative remaining, and IEEE leftover. Journals, outbox, payment receipts, write-offs, settlements, and parked leftover rows stay unchanged. No journal, webhook, write-off, credit note, dunning engine, PSP, AIS call, statutory numbering, or settlement command is added. Apply parked leftover, then collect the residual or settle at exact zero.
- ADR 0052 for unapplied-cash application to a collection case.
- `UnappliedCashService.park_unapplied_cash` parks leftover remittance against one same-tenant stored `payment_receipt`. Identity is `(tenant_account_id, payment_receipt_id)`. Replay returns the stored `unapplied_cash_id` as `duplicate_replay` and never writes a second row. #12 still rejects overpay; implied leftover on a stored receipt is exact zero because the full received amount was applied. Omitting `unapplied_amount` fail-closes as `payment_receipt_already_consumed`. A supplied leftover must be a positive exact decimal that does not exceed the stored receipt. Currency, when supplied, must match the receipt. Status is `parked`. Next operator action is `wait`. `POST /v1/payment-receipts/{payment_receipt_id}/unapplied-cash` is the nested park command and refuses PAN and provider secrets. `UnappliedCashPresentmentService.present_unapplied_cash` projects the stored row. `GET /v1/unapplied-cash/{unapplied_cash_id}` is HTTP 200 for the same tenant and 404 across tenants. `GET /v1/unapplied-cash` lists `{unapplied_cash, next_cursor}` ordered by `parked_at` then `unapplied_cash_id`. Fail closed on missing/cross-tenant receipt, leftover already parked, leftover zero/negative, leftover greater than the receipt, currency mismatch, IEEE leftover, and omitted leftover. Receipt amount, case remaining, payment-receipt count, journals, and outbox stay unchanged. Do not auto-apply leftover to another case. No journal, webhook, write-off, settlement, credit note, dunning engine, PSP, AIS call, or statutory numbering is added.
- ADR 0051 for unapplied cash from a stored payment receipt.
- `CollectionAgingPresentmentService.present_collection_aging` projects stored open or dunning `collection_case` remaining into current / 1-30 / 31-60 / 61-90 / 90+ buckets. Identity is the tenant pin. Due date is issued-invoice `due_at` when stored, otherwise `collection_case.opened_at`. Totals group by `currency_code` and never mix currencies. Each bucket carries case count and exact inclusive outstanding. Settled cases and exact-zero remaining are omitted so write-offs do not inflate aged dollars. `GET /v1/collection-aging` is the HTTP read. Missing tenant is HTTP 422. Replay is a safe GET and writes no money fact. No journal, webhook, write-off, settlement, payment, dunning engine, AIS call, statutory numbering, or new money fact is added.
- ADR 0050 for collection aging presentment from stored remaining.
- `AccountingExportService.propose_credit_journal` exposes the existing credit `accounting_journal_proposal` already composed from a stored `credit_adjustment`.  Identity is `(tenant_account_id, credit_adjustment_id)`.  Replay returns the stored `proposal_id` as `duplicate_replay` and never grows the store.  Untaxed lines debit semantic `usage_revenue` and credit semantic `accounts_receivable` for the exact credit inclusive amount.  Taxed credits reuse the existing #20 `tax_payable` unwind on the same journal.  `proposal_status` stays `validated` and is never `posted`.  The AIS-compatible idempotency key stays `{tenant}:credit_adjustment:{credit_adjustment_id}:{credit.source_payload_hash}:v{version}`.  `POST /v1/credit-adjustments/{credit_adjustment_id}/journal-proposals` is the explicit compose/replay because #17 already composes on accept.  AIS pull stays existing `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Fail closed on missing/cross-tenant credit, currency mismatch, and zero/negative/IEEE amounts.  Collection outstanding, payment receipts, issued credit notes, write-offs, settlements, and `credit_adjustment.recorded` stay unchanged.  No AIS call, `posted` status, statutory account ID, new webhook type, second journal store, or second tax unwind is added.
- ADR 0049 for credit journal proposal from a stored credit adjustment.
- `AccountingExportService.propose_write_off_journal` emits one balanced write-off/AR `accounting_journal_proposal` from a stored `collection_write_off`.  Identity is `(tenant_account_id, collection_write_off_id)`.  Replay returns the stored `proposal_id` as `duplicate_replay` and never grows the store.  Lines debit semantic `write_off_expense` and credit semantic `accounts_receivable` for the exact write-off inclusive amount.  `proposal_status` stays `validated` and is never `posted`.  The AIS-compatible idempotency key is `{tenant}:collection_write_off:{collection_write_off_id}:{source_payload_hash}:v{version}`.  `POST /v1/collection-write-offs/{collection_write_off_id}/journal-proposals` is the explicit compose command because #49 shipped without compose.  AIS pull stays existing `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Fail closed on missing/cross-tenant write-off, currency mismatch, and zero/negative/IEEE amounts.  Collection outstanding, payment receipts, credit notes, settlements, and `write_off.recorded` stay unchanged.  No AIS call, `posted` status, statutory account ID, new webhook type, tax unwind, dunning engine, PSP, or settlement command is added.
- ADR 0048 for write-off journal proposal from a stored collection write-off.
- First successful `CollectionWriteOffService.write_off_collection_case` enqueues one existing #24 `webhook_outbox_event` with canonical type `write_off.recorded`.  `source_id` is `collection_write_off_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, hash, currency, exact write-off amount, stored exact-zero remaining, `written_off_at`, `collection_write_off_status`, optional `issued_invoice_id`).  Remaining outstanding is the stored write-off fact, not later-mutated case remaining.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, statutory identifiers, and current case status.  Replay of the same write-off does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `write_off.recorded`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected write-off writes zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, payment receipt, credit note, or settlement command is added.
- ADR 0047 for `write_off.recorded` on the existing commercial webhook outbox.
- `CollectionWriteOffService.write_off_collection_case` writes off leftover remaining on one same-tenant open `collection_case`.  Identity is `(tenant_account_id, collection_case_id)`.  Replay returns the stored `collection_write_off_id` as `duplicate_replay` and never re-zeros outstanding.  The stored write-off amount is the current remaining inclusive amount.  Remaining outstanding becomes exact zero.  Case status stays `open` or `dunning`; #46 remains the explicit settle-when-zero command.  Body may omit amount, or require amount to equal current remaining.  Fail closed on amount mismatch, currency mismatch, missing case, already settled, remaining already zero, or negative remaining.  `collection_write_off_id` is an opaque generated identifier.  `POST /v1/collection-cases/{collection_case_id}/write-offs` is the nested write-off command and refuses PAN and provider secrets.  `CollectionWriteOffPresentmentService.present_collection_write_off` projects the stored row and current remaining outstanding.  `GET /v1/collection-write-offs/{collection_write_off_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/collection-write-offs` lists `{collection_write_offs, next_cursor}` ordered by `written_off_at` then `collection_write_off_id`.  Payment receipts, issued credit notes, applications, settlements, and `proposal_status` stay unchanged.  No journal, tax unwind, dunning engine, PSP, AIS call, statutory numbering, or webhook event is added.  Write off leftover remaining, then settle.
- ADR 0046 for commercial collection write-off of leftover remaining.
- First successful `CreditNoteApplicationService.apply_credit_note` enqueues one existing #24 `webhook_outbox_event` with canonical type `credit_note.applied`.  `source_id` is `credit_note_application_id`.  The envelope `data` is a thin reference plus hash (ids, contract versions, hashes, currency, exact applied amount, `applied_at`, optional `issued_invoice_id`).  Remaining outstanding is omitted because it is not stored on the application row.  The payload omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same application does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `credit_note.applied`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected apply writes zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, write-off, settlement, or payment receipt is added.
- ADR 0045 for `credit_note.applied` on the existing commercial webhook outbox.
- First successful `CollectionCaseSettlementService.settle_collection_case` enqueues one existing #24 `webhook_outbox_event` with canonical type `collection.settled`.  `source_id` is `collection_case_settlement_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, currency, exact-zero remaining, `settled_at`, `collection_case_status`, optional `issued_invoice_id`).  It omits PII, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, statutory identifiers, and write-off amounts.  Replay of the same settlement does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `collection.settled`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected settle writes zero outbox rows.  Cases already settled by #12/#45 without a settlement row are not backfilled.  No AIS call, journal/`proposal_status` flip, statutory numbering, write-off, or payment receipt is added.
- ADR 0044 for `collection.settled` on the existing commercial webhook outbox.
- `CollectionCaseSettlementService.settle_collection_case` settles one same-tenant open `collection_case` whose remaining outstanding is exact zero.  Identity is `(tenant_account_id, collection_case_id)`.  Replay returns the stored `collection_case_settlement_id` as `duplicate_replay` and never double-settles.  Fail closed when outstanding is not zero, the case is already `settled`, or the tenant does not match.  Implicit #12/#17/#45 settle-when-zero on positive apply paths stays; those settled cases write zero settlement rows.  `collection_case_settlement_id` is an opaque generated identifier.  `POST /v1/collection-cases/{collection_case_id}/settlements` is the nested settle command and refuses PAN and provider secrets.  `CollectionCaseSettlementPresentmentService.present_collection_case_settlement` projects the stored row and current remaining outstanding.  `GET /v1/collection-case-settlements/{collection_case_settlement_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/collection-case-settlements` lists `{collection_case_settlements, next_cursor}` ordered by `settled_at` then `collection_case_settlement_id`.  Payment receipts, credit-note applications, and `proposal_status` stay unchanged.  No journal, tax unwind, dunning engine, PSP, AIS call, statutory numbering, write-off, or new webhook event type is added.  Settle the zero-outstanding case, then wait.
- `operator_console` Storybook adds a `CollectionCaseSettlement` story using `AmountDue`, `StatusChip`, and the tenant pin.  The fixture is a settled morning zero-outstanding case.  There is no login wall, Figma-only work, or production SPA.
- ADR 0043 for settle-when-zero of an existing collection case.
- `CreditNoteApplicationService.apply_credit_note` applies one existing `issued_credit_note` onto one open same-tenant `collection_case`.  Identity is `(tenant_account_id, issued_credit_note_id)`.  Replay returns the stored `credit_note_application_id` as `duplicate_replay` and never double-reduces.  Outstanding is reduced by the exact issued tax-inclusive credit.  Fail closed on currency mismatch, settled case, remaining that would go negative, already-applied credit, or invoice mismatch.  `credit_note_application_id` is an opaque generated identifier.  `POST /v1/collection-cases/{collection_case_id}/credit-note-applications` is the nested apply command and refuses PAN and provider secrets.  `CreditNoteApplicationPresentmentService.present_credit_note_application` projects the stored row and current remaining outstanding.  `GET /v1/credit-note-applications/{credit_note_application_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/credit-note-applications` lists `{credit_note_applications, next_cursor}` ordered by `applied_at` then `credit_note_application_id`.  Issued credit note, credit adjustment, payment receipt, and `proposal_status` stay unchanged.  No journal, tax unwind, dunning engine, PSP, AIS call, statutory numbering, or new webhook event type is added.  Apply the issued credit note, then collect the residual.
- `operator_console` Storybook adds a `CreditNoteApplication` story using `AmountDue`, `StatusChip`, and the tenant pin.  The fixture is an applied morning credit note.  There is no login wall, Figma-only work, or production SPA.
- ADR 0042 for credit-note application to a collection case.
- First successful `IssuedCreditNoteService.issue_credit_note` enqueues one existing #24 `webhook_outbox_event` with canonical type `credit_note.issued`.  `source_id` is `issued_credit_note_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, currency, exact totals, `issued_at`, closed `credit_reason_code`, optional `issued_invoice_id`).  It omits credit-note lines, PII, reason prose, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same issued credit note does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `credit_note.issued`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  Rejected issue writes zero outbox rows.  No AIS call, journal/`proposal_status` flip, statutory numbering, or payment capture is added.
- ADR 0041 for `credit_note.issued` on the existing commercial webhook outbox.
- `IssuedCreditNoteService.issue_credit_note` writes one append-only commercial `issued_credit_note` from a stored `credit_adjustment`.  Identity is `(tenant_account_id, credit_adjustment_id)`.  Replay of the same tenant and credit returns the stored `issued_credit_note_id` as `duplicate_replay`.  Totals freeze exact currency and tax-exclusive/tax/inclusive credit amounts from the adjustment.  `issued_credit_note_id` is an opaque generated identifier; the path does not invent statutory numbering.  `issued_invoice_id` is stored only when an issued invoice already exists for the same draft.  The adjustment has no lines, so the snapshot invents none.  `POST /v1/credit-adjustments/{credit_adjustment_id}/issued-credit-notes` is the nested issue command and refuses PAN and provider secrets.  `IssuedCreditNotePresentmentService.present_issued_credit_note` projects the stored snapshot.  `GET /v1/issued-credit-notes/{issued_credit_note_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/issued-credit-notes` lists `{issued_credit_notes, next_cursor}` ordered by `issued_at` then `issued_credit_note_id`.  Credit-adjustment status stays `recorded`.  The validated journal remains available for AIS.  First successful issue enqueues one #24 `credit_note.issued` outbox event.  No payment capture or AIS call is added.  Issue the credit note; the validated journal remains available for AIS.
- `operator_console` Storybook adds an `IssuedCreditNote` story using `AmountDue`, `StatusChip`, and the tenant pin.  Fixtures are issued-morning and issued-taxed credit notes.  There is no login wall, Figma-only work, or production SPA.
- ADR 0040 for issued-credit-note from credit adjustment.
- First successful `IssuedInvoiceService.issue_invoice` enqueues one existing #24 `webhook_outbox_event` with canonical type `invoice.issued`.  `source_id` is `issued_invoice_id`.  The envelope `data` is a thin reference plus hash (ids, contract version, currency, exact totals, `issued_at`, optional `due_at`, rating-run and usage-snapshot hashes).  It omits invoice lines, billing-account references, meter codes, PAN, tenant secrets, API keys, webhook secrets/hashes, raw documents, and statutory identifiers.  Replay of the same issued invoice does not enqueue a second row; a crash after insert and before enqueue is healed by the next replay.  Existing subscriptions opt in by including `invoice.issued`.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, SSRF, delivery attempts, and outbox presentment stay the #24 contracts.  No AIS call, journal/`proposal_status` flip, statutory numbering, or payment capture is added.
- ADR 0039 for `invoice.issued` on the existing commercial webhook outbox.
- `IssuedInvoiceService.issue_invoice` writes one append-only commercial `issued_invoice` from a stored `invoice_draft`.  Identity is `(tenant_account_id, invoice_draft_id)`.  Replay of the same tenant and draft returns the stored `issued_invoice_id` as `duplicate_replay`.  Totals freeze exact currency, lines, and tax-exclusive/tax/inclusive amounts from the draft or its tax assessment.  `issued_invoice_id` is an opaque generated identifier; the path does not invent statutory numbering.  Optional `due_at` is stored only when the caller supplies a valid timezone-aware instant.  `POST /v1/invoice-drafts/{invoice_draft_id}/issued-invoices` is the nested issue command and refuses PAN and provider secrets.  `IssuedInvoicePresentmentService.present_issued_invoice` projects the stored snapshot.  `GET /v1/issued-invoices/{issued_invoice_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/issued-invoices` lists `{issued_invoices, next_cursor}` ordered by `issued_at` then `issued_invoice_id`.  Invoice-draft status stays `draft`.  First successful issue enqueues one #24 `invoice.issued` outbox event.  No payment capture or AIS call is added.  Issue invoice, then collect or credit.
- `operator_console` Storybook adds an `IssuedInvoice` story using `AmountDue`, `LineTable`, `StatusChip`, and the tenant pin.  Fixtures are issued-untaxed-morning and issued-taxed-hundred.  There is no login wall, Figma-only work, or production SPA.
- ADR 0038 for issued-invoice from invoice draft.
- `WebhookOutboxEventPresentmentService.present_webhook_outbox_event` projects one tenant-scoped commercial webhook-outbox metadata statement from stored `webhook_outbox_event` rows.  Next operator action is `run_deliveries` while pending, otherwise `wait`.  Commercial-fact enqueue and `POST /v1/webhook-deliveries` remain the #24 write path and refuse PAN and provider secrets.  `GET /v1/webhook-outbox-events/{outbox_event_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/webhook-outbox-events` lists `{webhook_outbox_events, next_cursor}` ordered by `enqueued_at` then `outbox_event_id`.  GET never publishes, sends, retries, or marks delivered, and never returns `payload_json`, the webhook secret, hash, prefix, or signature.  This is the Billing commercial webhook outbox, not the AIS posting-receipt outbox.  Register an https callback, then run deliveries; AIS may keep polling.
- `operator_console` Storybook adds a `WebhookOutboxEvent` story using `StatusChip`.  Fixtures are pending-journal-validated and delivered-receipt-applied.  There is no login wall, Figma-only work, or production SPA.
- ADR 0037 for webhook-outbox-event HTTP presentment.
- `DunningEventPresentmentService.present_dunning_event` projects one tenant-scoped reminder statement from stored `collection_dunning_event` rows.  Next operator action is `wait` when the parent case is settled, otherwise `collect`.  `POST /v1/collection-cases/{collection_case_id}/dunning-events` remains the #10 record and refuses PAN and provider secrets.  `GET /v1/dunning-events/{dunning_event_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/dunning-events` lists `{dunning_events, next_cursor}` ordered by `occurred_at` then `collection_dunning_event_id`.  GET never invents a send channel, recipient, or notice body.  Record the commercial reminder, then collect or credit.
- `operator_console` Storybook adds a `DunningNotice` story using `StatusChip`.  Fixtures are first-notice morning and overdue-notice evening.  There is no login wall, Figma-only work, or production SPA.
- ADR 0036 for dunning-event HTTP presentment.
- `WebhookSubscriptionPresentmentService.present_webhook_subscription` projects one tenant-scoped subscription metadata statement from stored `webhook_subscription` rows.  Next operator action is `run_deliveries` while active, otherwise `register`.  `POST /v1/webhook-subscriptions` remains the #24 register and refuses PAN and provider secrets.  `POST /v1/webhook-subscriptions/{webhook_subscription_id}/revoke` remains the #24 revoke and refuses PAN and provider secrets.  `GET /v1/webhook-subscriptions/{webhook_subscription_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/webhook-subscriptions` lists `{webhook_subscriptions, next_cursor}` ordered by `issued_at` then `webhook_subscription_id`.  GET never returns the secret, hash, prefix, or signed body.  The #24 callback URL SSRF policy, HMAC `X-CWL-Webhook-Signature: sha256=<hex>`, secret one-time-return, and known event types are unchanged.  Register an https callback, then run deliveries; AIS may keep polling.
- `operator_console` Storybook adds a `WebhookSubscription` story using `StatusChip`.  Fixtures are active-https and revoked-https `example.test` callbacks.  There is no login wall, Figma-only work, or production SPA.
- ADR 0035 for webhook-subscription HTTP presentment.
- `TenantApiCredentialPresentmentService.present_tenant_api_credential` projects one tenant-scoped credential metadata statement from stored `tenant_api_credential` rows.  Next operator action is `wait` while active, otherwise `issue`.  `POST /v1/tenant-api-credentials` remains the #22 issue and refuses PAN and provider secrets.  `POST /v1/tenant-api-credentials/{tenant_api_credential_id}/revoke` remains the #22 revoke and refuses PAN and provider secrets.  `GET /v1/tenant-api-credentials/{tenant_api_credential_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/tenant-api-credentials` lists `{tenant_api_credentials, next_cursor}` ordered by `issued_at` then `tenant_api_credential_id`.  GET never returns the secret, hash, or verifier.  The #22 bootstrap window is unchanged.  Issue a key, then send it on every `/v1` call; revoke when leaked.
- `operator_console` Storybook adds a `TenantApiCredential` story using `StatusChip`.  Fixtures are active-operator and revoked-leaked fake prefixes.  There is no login wall, Figma-only work, or production SPA.
- ADR 0034 for tenant API credential HTTP presentment.
- `WebhookDeliveryPresentmentService.present_webhook_delivery` projects one tenant-scoped delivery-attempt statement from stored `webhook_delivery_attempt` rows.  Identity stays `delivery_attempt_id`.  Next operator action is `wait` after a stored success, otherwise `run_deliveries`.  `POST /v1/webhook-deliveries` remains the #24 deliver run and refuses PAN and provider secrets.  `GET /v1/webhook-deliveries/{delivery_attempt_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/webhook-deliveries` lists `{webhook_deliveries, next_cursor}` ordered by `attempted_at` then `delivery_attempt_id`.  GET never resends and never returns the webhook secret, hash, or signed body.  HMAC `X-CWL-Webhook-Signature: sha256=<hex>` is unchanged.  Register an https callback, then run deliveries; AIS may keep polling.
- `operator_console` Storybook adds a `WebhookDelivery` story using `StatusChip`.  Fixtures are delivered-morning and failed-callback.  There is no login wall, Figma-only work, or production SPA.
- ADR 0033 for webhook-delivery HTTP presentment.
- `PostingReceiptObservationPresentmentService.present_posting_receipt_observation` projects one tenant-scoped observation statement from stored `posting_receipt_observation` rows.  `posting_status_code` stays AIS-owned.  Next operator action is `wait`.  `POST /v1/posting-receipt-observations` remains the #16 pull and refuses PAN and provider secrets.  `GET /v1/posting-receipt-observations/{idempotency_key}` stays the existing #16 item read: HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/posting-receipt-observations` lists `{posting_receipt_observations, next_cursor}` ordered by `observed_at` then `posting_receipt_observation_id`.  Drain AIS outbox, then store the receipt observation.  `proposal_status` stays `validated`.
- `operator_console` Storybook adds a `PostingReceiptObservation` story using `StatusChip`.  Fixtures are observed-posted and observed-held.  There is no login wall, Figma-only work, or production SPA.
- ADR 0032 for posting-receipt observation HTTP presentment.
- `TaxAssessmentPresentmentService.present_tax_assessment` projects one tenant-scoped tax statement from stored `tax_assessment` rows.  Amounts stay exact-decimal strings.  Next operator action is `propose_journal`.  `POST /v1/tax-assessments` remains the #19 assess command and refuses PAN and provider secrets.  `GET /v1/tax-assessments/{tax_assessment_id}` stays the existing #19 item read: HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/tax-assessments` lists `{tax_assessments, next_cursor}` ordered by `assessed_at` then `tax_assessment_id`.  Publish a tax rate, assess the draft, then propose the journal and let AIS pull.
- `operator_console` Storybook adds a `TaxAssessment` story using `AmountDue` and `StatusChip`.  Fixtures are assessed-morning and assessed-partial VAT.  There is no login wall, Figma-only work, or production SPA.
- ADR 0031 for tax-assessment HTTP presentment.
- `RatingRunPresentmentService.present_rating_run` projects one tenant-scoped rating statement from stored `rating_run` rows.  Totals stay exact-decimal strings.  Next operator action is `draft_invoice`.  `POST /v1/rating-runs` remains the #7 rate-a-window command and refuses PAN and provider secrets.  `GET /v1/rating-runs/{rating_run_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/rating-runs` lists `{rating_runs, next_cursor}` ordered by `recorded_at` then `rating_run_id`.  Rate a window, then draft an invoice.
- `operator_console` Storybook adds a `RatingRun` story using `AmountDue` and `StatusChip`.  Fixtures are rated-morning and rated-partial.  There is no login wall, Figma-only work, or production SPA.
- ADR 0030 for rating-run HTTP presentment.
- `UsageEventPresentmentService.present_usage_event` projects one tenant-scoped usage statement from stored `usage_event` rows.  Quantities stay exact-decimal strings.  Next operator action is `rate_window`.  `POST /v1/usage-events` remains the #5 ingest and refuses PAN and provider secrets.  `GET /v1/usage-events/{usage_event_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/usage-events` lists `{usage_events, next_cursor}` ordered by `recorded_at` then `usage_event_id`.  Ingest usage, then rate a window against a published card.
- `operator_console` Storybook adds a `UsageEvent` story using `AmountDue` and `StatusChip`.  Fixtures are stored-morning and stored-partial-token.  There is no login wall, Figma-only work, or production SPA.
- ADR 0029 for usage-event HTTP presentment.
- `RateCardPresentmentService.present_rate_card` projects one tenant-scoped catalog statement from stored `rate_card` and latest `rate_card_version` rows.  Unit amounts stay exact-decimal strings.  Next operator action is `rate_window`.  `POST /v1/rate-cards` remains the #18 write and refuses PAN and provider secrets.  `GET /v1/rate-cards/{rate_card_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/rate-cards` lists `{rate_cards, next_cursor}` ordered by `created_at` then `rate_card_id`.  Version GET routes stay the #18 catalog reads.  Publish a rate card, then rate a window against that version.
- `operator_console` Storybook adds a `RateCard` story using `AmountDue` and `StatusChip`.  Fixtures are published-standard and published-premium.  There is no login wall, Figma-only work, or production SPA.
- ADR 0028 for rate-card HTTP presentment.
- `CreditAdjustmentPresentmentService.present_credit_adjustment` projects one tenant-scoped credit statement from stored `credit_adjustment` rows.  Amounts stay exact-decimal strings using stored `credit_amount`, `tax_exclusive_amount`, and `tax_amount`.  Status stays `recorded`.  Next operator action is `wait`.  `POST /v1/credit-adjustments` remains the #17 write from `invoice_draft_id` and refuses PAN and provider secrets.  `GET /v1/credit-adjustments/{credit_adjustment_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/credit-adjustments` lists `{credit_adjustments, next_cursor}`.  Record the credit; AIS pulls the validated journal.
- `operator_console` Storybook adds a `CreditAdjustment` story using `AmountDue` and `StatusChip`.  Fixtures are recorded-morning and recorded-taxed.  There is no login wall, Figma-only work, or production SPA.
- ADR 0027 for credit-adjustment HTTP presentment.
- `PaymentSettlementService.record_payment_receipt` composes the existing #13 cash journal on accept and on duplicate replay.  Lines stay debit `cash_receipt` / credit `accounts_receivable`.  `proposal_status` stays `validated`.  The idempotency key stays `{tenant}:cash_receipt:{payment_receipt_id}:{source_payload_hash}:v{version}`.  Replay writes no second proposal.  `POST /v1/cash-journal-proposals` remains a manual replay.  Record the receipt; the cash journal is already validated for AIS to pull.
- ADR 0026 for cash-journal compose on receipt accept.
- `PaymentReceiptPresentmentService.present_payment_receipt` projects one tenant-scoped payment-receipt statement from stored `payment_receipt` rows and the current collection case.  `received_amount` and `remaining_outstanding_amount` are exact-decimal strings.  Receipt status stays `applied`.  Next operator action is `record_receipt` or `drain_or_wait`.  `POST /v1/payment-receipts` remains the #12 write from `payment_intent_id` and refuses PAN and provider secrets.  `GET /v1/payment-receipts/{payment_receipt_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/payment-receipts` lists `{payment_receipts, next_cursor}`.  Record the receipt; the cash journal is already validated for AIS to pull.
- `operator_console` Storybook adds a `PaymentReceipt` story using `AmountDue` and `StatusChip`.  Fixtures are applied-full and applied-partial.  There is no login wall, Figma-only work, or production SPA.
- ADR 0025 for payment-receipt HTTP presentment.
- `PaymentIntentPresentmentService.present_payment_intent` projects one tenant-scoped payment-intent statement from stored `payment_intent` rows.  `payment_amount` is an exact-decimal string.  Status stays `projected`, `cancelled`, or `rejected`.  Next operator action is `record_receipt` or `wait`.  `POST /v1/payment-intents` remains the #11 write from `collection_case_id` and refuses PAN and provider secrets.  `GET /v1/payment-intents/{payment_intent_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/payment-intents` lists `{payment_intents, next_cursor}`.  Create a projected payment intent, then record the receipt.
- `operator_console` Storybook adds a `PaymentIntent` story using `AmountDue` and `StatusChip`.  Fixtures are projected and cancelled.  There is no login wall, Figma-only work, or production SPA.
- ADR 0024 for payment-intent HTTP presentment.
- `CollectionCasePresentmentService.present_collection_case` projects one tenant-scoped collection statement from stored `collection_case` and dunning rows.  `collection_outstanding` is an exact-decimal string.  Status stays `open`, `dunning`, or `settled`.  Next operator action is `collect`, `credit`, or `wait`.  `GET /v1/collection-cases/{collection_case_id}` is HTTP 200 for the same tenant and 404 across tenants.  `GET /v1/collection-cases` lists `{collection_cases, next_cursor}`.  Open the collection case, then collect or credit.
- `operator_console` Storybook adds a `CollectionCase` story using `AmountDue` and `StatusChip`.  Fixtures are open, dunning, and settled.  There is no login wall, Figma-only work, or production SPA.
- ADR 0023 for collection-case presentment.
- `AisOutboxDrainService.drain_ais_outbox` GETs AIS `GET /outbox-events?event_type_code=posting_receipt`, matches `payload_reference` / `aggregate_reference` by equality to URNs constructed from our stored `proposal_id`, then GETs `/posting-receipts?idempotency_key=` with the stored Billing key.  Empty unpublished pages skip receipt GETs.  After a stored observation, POST `/outbox-events/{outbox_event_id}/publish`.  `proposal_status` stays `validated`.  Do not drain `journal_reversal` or `period_close`.  Do not parse the payload URN.
- `POST /v1/ais-outbox-drains` on the existing WSGI app uses the tenant pin plus the #22 key rule.  `AIS_BASE_URL` is operator-configured https; http is allowed only for localhost tests.  There is no scheduler.
- ADR 0022 for the AIS outbox drain.  Customer copy: drain AIS outbox, then store the receipt observation; AIS may keep being polled only when the outbox is non-empty.
- `WebhookSubscriptionService.register_subscription` writes one tenant-scoped `webhook_subscription` (`active`/`revoked`).  `callback_url` must be https; http is allowed only for localhost tests.  Replay of the same tenant, URL, event set, and contract version returns the same `webhook_subscription_id` as `duplicate_replay`.  The secret is returned once; the ledger stores only a keyed HMAC and prefix.
- Accepted journal proposals, payment receipts, credits, issued invoices, and issued credit notes append one `webhook_outbox_event` (`journal_proposal.validated`, `payment_receipt.applied`, `credit_adjustment.recorded`, `invoice.issued`, `credit_note.issued`) with a thin envelope around the published contract or issued-document reference.  Replay of the commercial fact does not enqueue a second row.  API secrets and PANs are refused.
- `WebhookDeliveryService.deliver_due_events` POSTs JSON to active same-tenant subscriptions and signs the raw body with HMAC-SHA256 (`X-CWL-Webhook-Signature: sha256=<hex>`).  Attempts are append-only `webhook_delivery_attempt` rows.  There is no scheduler beyond the explicit deliver call and `POST /v1/webhook-deliveries`.
- `POST /v1/webhook-subscriptions`, `GET /v1/webhook-subscriptions`, `POST /v1/webhook-subscriptions/{id}/revoke`, and `POST /v1/webhook-deliveries` on the existing WSGI app use the tenant pin plus the #22 key rule.  GET list never includes the secret or hash.  AIS pull stays bootstrap.
- ADR 0021 for the webhook outbox.  Customer copy: register an https callback, then run deliveries; AIS may keep polling.
- `operator_console` Storybook presents the #21 invoice-draft statement with tokenized `AmountDue`, `LineTable`, `StatusChip`, and tenant-pin modules.  Fixtures are taxed plus partial credit, untaxed morning usage, and settled.  Amounts stay exact-decimal strings.  Customer copy is amount due and the next operator action: collect or credit.  Storybook is the UI surface; there is no production SPA, login wall, Stripe, or AIS call.
- ADR 0020 and `docs/STORYBOOK.md` for the operator presentment console.
- `TenantApiCredentialService.issue_credential` mints one append-only `tenant_api_credential`, returns prefix plus secret once, and persists only a keyed HMAC.  A second issue of the same tenant, optional `credential_label`, and contract version always mints a new secret.  `revoke_credential` is idempotent.  After one or more active keys exist, every `/v1` write and GET except credential issue requires `Authorization: Bearer <secret>` or `X-CWL-Api-Key: <secret>` whose tenant equals the pin.  Zero active keys keep the tenant pin (bootstrap window) so AIS `X-CWL-Tenant-Reference` pull stays up until a key is issued.  `GET /healthz` stays unauthenticated.  `POST /v1/tenant-api-credentials` issues a key; `GET /v1/tenant-api-credentials` lists metadata only; `POST /v1/tenant-api-credentials/{id}/revoke` revokes.  Issue a key, then send it on every `/v1` call; revoke when leaked.
- ADR 0019 and `docs/SECURITY.md` for tenant API credentials without changing journal, tax, credit, or presentment shapes.
- `InvoicePresentmentService.present_invoice_draft` projects one tenant-scoped statement from stored draft, tax, credit, rating, and collection rows.  `amount_due` is tax-inclusive consideration minus accepted credits and never below zero.  Tax fields are zero when no assessment exists.  `GET /v1/invoice-drafts/{invoice_draft_id}` returns the statement; `GET /v1/invoice-drafts` lists summaries as `{invoice_drafts, next_cursor}`.  Cross-tenant or unknown is 404.  Open the draft statement, then collect or credit.
- ADR 0018 for invoice-draft presentment without PDF, email, or a web UI.
- Taxed `credit_adjustment` rows store `tax_exclusive_amount` and `tax_amount` using the proportional split `round_half_even(credit_amount * tax_amount / tax_inclusive_amount)`.  A full inclusive credit reconstructs the original exclusive and tax.  The paired journal debits `usage_revenue` and `tax_payable` and credits `accounts_receivable` when tax is positive.  Untaxed credits stay two-line.  The credit idempotency key is unchanged; the hash includes the tax split when the draft is taxed.  Record the credit; AIS pulls the validated three-line unwind.
- ADR 0017 for tax-payable unwind without AIS journal-reversals or operator UI.
- `TaxRateService.publish_tax_rate` writes one tenant-scoped `tax_rate_schedule` and one immutable `tax_rate_version`.  Closed codes are `vat`, `gst`, and `sales_tax`.  `tax_rate` is an exact `Decimal` in `[0, 1]`.  The same tenant, code, rate, and contract version replay the stored version.
- `TaxAssessmentService.assess_tax` applies a persisted version to a stored invoice draft.  `tax_exclusive_amount` is the drafted subtotal.  `tax_amount` is half-even rounded to the documented ISO 4217 minor units (`0` for JPY/KRW, `2` for listed two-decimal currencies).  Unknown currencies and assess-after-collection fail closed.
- `propose_journal` on a taxed draft emits a balanced three-line proposal: debit `accounts_receivable` inclusive, credit `usage_revenue` exclusive, credit `tax_payable` tax.  A half-even product that rounds to zero omits the `tax_payable` line so every persisted line stays debit XOR credit; the hash still includes the tax facts.  Untaxed drafts keep the two-line AR/revenue proposal.  The invoice-draft idempotency key is unchanged; the hash includes tax lines when assessed.  AIS must map `tax_payable`.
- `open_collection_case` uses `tax_inclusive_amount` when an assessment exists.  Credit `remaining_adjustable` is inclusive when assessed.
- `POST /v1/tax-rates`, `GET /v1/tax-rates`, `GET /v1/tax-rate-versions/{tax_rate_version}`, `POST /v1/tax-assessments`, and `GET /v1/tax-assessments/{tax_assessment_id}` on the existing WSGI app.
- ADR 0016 for commercial tax assessment without an OSS engine, exemptions, or UI.
- `RateCardService.publish_rate_card` writes one tenant-scoped `rate_card` and one immutable `rate_card_version` with exact-decimal `rate_card_line` rows.  The same tenant, card name, canonical line hash, and contract version replay the stored version.  A later distinct line set increments the version.  A published version is never edited.
- `UsageRatingService.rate_usage_window` now resolves a persisted same-tenant `rate_card_version`.  Unknown or cross-tenant versions reject.  Rating uses the stored `unit_amount` for the matching `metric_code`.  A missing metric fails closed and does not invent a price.
- `POST /v1/rate-cards`, `GET /v1/rate-cards`, `GET /v1/rate-cards/{rate_card_id}`, `GET /v1/rate-cards/{rate_card_id}/versions`, and `GET /v1/rate-card-versions/{rate_card_version}` on the existing WSGI app.  Tenant pin matches credit and journal-proposal query.  Cross-tenant reads are 404.  `POST /v1/rating-runs` still takes `rate_card_version`, and that integer must now exist for the tenant.
- ADR 0015 for a versioned rate-card catalog without tax, discounts, or tiered prices.
- `CreditAdjustmentService.record_credit_adjustment` records one commercial `credit_adjustment` against a stored invoice draft.  `credit_amount` is an exact `Decimal` greater than zero and cannot exceed remaining adjustable consideration.  Closed reasons are `rating_correction`, `goodwill`, and `billing_error`.  If a collection case exists, outstanding is reduced by the same amount and remaining zero marks the case `settled`.  The path emits one balanced validated journal proposal that debits `usage_revenue` and credits `accounts_receivable`.
- Idempotent credit identity on `(tenant, invoice_draft_id, source_payload_hash, credit_adjustment_contract_version)` so a replay returns the same `credit_adjustment_id` and `proposal_id`.
- AIS-compatible credit idempotency key `{tenant}:credit_adjustment:{credit_adjustment_id}:{source_payload_hash}:v{contract_version}`.
- `POST /v1/credit-adjustments` and tenant-scoped `GET /v1/credit-adjustments/{credit_adjustment_id}` on the existing WSGI app.  Credit proposals appear on existing `GET /v1/journal-proposals`.  This slice does not call AIS.
- ADR 0014 for commercial credit without posting, tax, refund-to-card, or chargeback.
- `PostingReceiptPullService.pull_posting_receipt` GETs an AIS posting receipt and stores one append-only `posting_receipt_observation`.  The AIS-owned contract is copied under `schemas/consumed/`.  `posting_status_code` is not mapped onto Billing `proposal_status`.  AIS 403 is cross-tenant and writes zero rows; AIS 404 is `not_yet_accepted` and writes zero rows.  `POST /v1/posting-receipt-observations` triggers the pull; `GET /v1/posting-receipt-observations/{idempotency_key}` reads a stored observation without calling AIS.
- ADR 0013 for observation-only posting-receipt pull.
- `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}` let AIS pull persisted proposals without mutating `proposal_status`.  Cash and AR proposals share the same list.  Tenant is required via optional `X-CWL-Tenant-Reference` or `tenant_reference`.  If both are present they must match.  HTTP 200 is a successful read; HTTP 422 is a missing tenant, tenant mismatch, or illegal filter; HTTP 404 is an unknown or cross-tenant proposal.  Responses keep semantic account roles and do not emit statutory account IDs.
- ADR 0012 for query-without-mutation and AIS pull.
- Stdlib HTTP accept surface (`python -m metering_billing.http_app` or `create_http_app(ledger=...)`) exposes the existing commercial services as JSON.  Money stays exact-decimal strings.  Tenant is required on every write.  HTTP 200 means `accepted` or `duplicate_replay`; HTTP 422 means `rejected`; HTTP 404 is only an unknown route.
- ADR 0011 for HTTP as a thin adapter that does not post journals or add a named payment provider.
- `AccountingExportService.propose_cash_journal` emits one balanced cash/AR `accounting_journal_proposal` from a stored payment receipt.
- Idempotent cash-proposal identity on `(tenant, payment_receipt_id, source_payload_hash, proposal_contract_version)` so a replay returns the same `proposal_id`.
- AIS-compatible cash idempotency key `{tenant}:cash_receipt:{payment_receipt_id}:{source_payload_hash}:v{version}`.
- ADR 0010 for cash journal export without posting, fiscal-period control, or statutory account IDs.
- Importable `metering_billing` payment-settlement service that applies an exact commercial receipt against a projected payment intent and reduces collection outstanding.
- Idempotent payment-receipt identity on `(tenant, payment_intent_id, source_payload_hash, settlement_contract_version)` so a replay returns the same `payment_receipt_id`.
- Append-only `payment_receipt` persistence.  Status stays `applied`.  A full receipt settles the collection case; a partial receipt leaves residual outstanding.
- `cancel_payment_intent` flips a projected intent to `cancelled` without writing a receipt or changing outstanding.
- ADR 0009 for commercial settlement without provider capture, PAN storage, cash journal export, or AIS posting.
- Importable `metering_billing` payment-intent service that projects a provider-neutral initiation record from a stored collection case.
- Idempotent payment-intent identity on `(tenant, collection_case_id, source_payload_hash, payment_intent_contract_version)` so a replay returns the same `payment_intent_id`.
- Append-only `payment_intent` persistence.  Status stays `projected`, `cancelled`, or `rejected`.
- ADR 0008 for projected payment initiation without capture, settlement, PAN storage, or journal posting.
- Importable `metering_billing` collection-case service that opens a tenant-scoped commercial case from a stored invoice draft and appends dunning reminders.
- Idempotent collection-case identity on `(tenant, invoice_draft_id)` so a replay returns the same `collection_case_id` and exact outstanding.
- Append-only `collection_case` and `collection_dunning_event` persistence.  Status stays `open` or `dunning`.
- ADR 0007 for commercial collection without payment capture or journal posting.
- Importable `metering_billing` accounting-export service that copies a stored invoice draft into one balanced, exact-decimal `accounting_journal_proposal`.
- Idempotent journal-proposal identity on `(tenant, invoice_draft_id, source_payload_hash, proposal_contract_version)` so a replay returns the same `proposal_id`.
- Append-only `journal_proposal` and `journal_proposal_line` persistence that keeps debit totals equal to credit totals.
- ADR 0006 for proposal-only journal export; AIS remains the posting consumer.
- Importable `metering_billing` invoice-draft service that copies a stored rating run into an exact, tenant-scoped, draft-only invoice-intent document.
- Idempotent invoice-draft identity on `(tenant, rating_run_id)` so a replay returns the same `invoice_draft_id` and totals.
- Append-only `invoice_draft` and `invoice_draft_line` persistence and a closed invoice-draft contract.
- ADR 0005 for invoice drafts as commercial documents, not statutory invoices.
- Importable `metering_billing` rating service that turns a tenant plus half-open ISO 8601 window into exact invoice-intent line totals from already-stored usage.
- Idempotent rating-run identity on `(tenant, window, rate_card_version, usage snapshot)` so a replay returns the same `rating_run_id` and totals.
- Append-only `rating_run` and `rating_line` persistence, versioned `rate_card` prices, and a closed rating-run contract.
- Billable-only rating against `meter_quality_rule`; analytics-only and manual-review measurements stay out of invoice-intent money.
- ADR 0004 for deterministic time-windowed rating.
- Importable `metering_billing` usage-ingestion service with tenant-scoped attribution, exact-decimal measurements, and half-open ISO 8601 query windows.
- Idempotent deduplication on `(tenant, source_event_key)` and `(tenant, source_payload_hash, event_contract_version)`.
- Internal `usage_event_id` generation so a reused producer `event_id` cannot overwrite stored usage.
- Append-only `usage_ingestion_receipt` rows for every accept, replay, and reject.
- Usage-ingestion receipt contract and PostgreSQL migration for contract version, payload-hash uniqueness, and ingestion receipts.
- Receipt contract invariants: accepted and `duplicate_replay` events require `usage_event_id`, `event_contract_version`, and `source_payload_hash`; rejected events require `rejection_reason_code`; batch counts must match `event_receipts` outcomes.
- Repository SQL diagnostics now name the migration path, scan every migration for provider identifiers, and reject single-word `ALTER TABLE ... ADD COLUMN` names.
- ADR 0003 for immutable usage ingestion.
- Initial product, architecture, data-model, and accounting-boundary baseline.
- Canonical usage-event, provider-capability, and accounting-journal-proposal schemas.
- Normalized PostgreSQL billing-core migration with tenant-scoped composite references.
- Semantic journal-proposal validation for exact decimals, unique line numbers, and balanced totals.
- Meter-specific quality-rule enforcement for usage measurements.
- Offline repository-contract validation and exact-head CI.

### Security

- Tenant HTTP API credentials persist only a keyed HMAC.  The plaintext secret is returned once on issue and is never logged, listed, or placed on AIS contracts.  Unknown, revoked, and cross-tenant keys fail closed.
- Excluded prompt, response, credential plaintext, provider secrets, and card data from initial contracts.
- Required commit-pinned GitHub Actions.
