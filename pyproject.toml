[project]
name = "metering-billing-platform"
version = "0.1.0"
description = "Provider-neutral commercial metering and billing control plane"
requires-python = ">=3.13"
dependencies = [
    "psycopg[binary]>=3.2,<4",
]

[dependency-groups]
dev = [
    "coverage==7.15.4",
]

[tool.uv]
package = false
