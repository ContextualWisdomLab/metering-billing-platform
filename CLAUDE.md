# Metering Billing Platform Development Context

This repository is the CWL commercial usage and billing authority. It is provider-neutral and accounting-aware, but it is not the statutory accounting authority.

Before changing behavior:

1. Identify the authoritative fact being changed.
2. Add a failing test for monetary and idempotency behavior.
3. Preserve immutable history through correction or reversal records.
4. Keep provider integration behind capability-specific ports.
5. Export accounting proposals without claiming legal posting.
6. Ingest usage through `metering_billing.UsageIngestionService` so retries are idempotent and tenants cannot attribute usage to each other.
7. Rate stored usage through `metering_billing.UsageRatingService` so a tenant window produces exact invoice-intent totals from billable quality only.
8. Draft invoice intent through `metering_billing.InvoiceDraftService` from a stored rating run.  Do not issue, collect, or post from that path.
9. Export a journal proposal through `metering_billing.AccountingExportService` from a stored invoice draft, a cash journal from a stored payment receipt, a credit journal from a stored credit adjustment, a write-off journal from a stored collection write-off, a refund journal from a stored leftover refund, a leftover journal from parked unapplied cash, an apply journal from a stored leftover apply, a void journal from a stored issued-invoice void, or a credit-note void journal from a stored issued-credit-note void.  Fail closed when any journal-line debit or credit has more than six significant fractional digits; do not round into `numeric(38, 6)`.  Hand the proposal to AIS.  Do not post journals, open fiscal periods, or resolve statutory account IDs.
10. Open a collection case through `metering_billing.CollectionCaseService` from a stored invoice draft.  Send a dunning notice next.  Do not capture payment or post journals from that path.
11. Project a payment intent through `metering_billing.PaymentIntentService` from a stored collection case.  Bind a provider projection later or cancel the intent.  Do not capture, settle, or post from that path.
12. Record a payment receipt through `metering_billing.PaymentSettlementService` from a stored projected payment intent.  Apply the exact received amount, reduce collection outstanding, settle the case when remaining is zero, and idempotently propose the existing cash journal.  Do not capture via a provider or post journals.
13. Propose a cash journal through `metering_billing.AccountingExportService.propose_cash_journal` from a stored payment receipt.  Receipt accept already composes this write; the HTTP path remains a manual replay.  Hand the persisted cash proposal to AIS.  Do not change collection outstanding, capture via a provider, or post journals.
14. Accept the same writes over HTTP through `metering_billing.http_app.create_http_app` or `python -m metering_billing.http_app`.  Require a tenant on every write.  Return each service `as_contract_dict` result as JSON.  Do not post journals, store a card PAN, or add a named payment provider.
15. Let AIS pull persisted journal proposals through `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Require a tenant on every read.  Accept optional `X-CWL-Tenant-Reference`; body or query `tenant_reference` still works when the header is absent; a mismatch is 422.  Do not mark exported, posted, or consumed.  Do not emit statutory account IDs.  AIS pulls validated proposals and owns `posting_receipt`.
16. Pull an AIS posting receipt through `metering_billing.PostingReceiptPullService.pull_posting_receipt` after AIS accept.  Store it as a commercial observation.  If AIS returns 404, accept the proposal on AIS and retry.  Do not flip Billing `proposal_status`.  Do not start credit or adjustment from that path.
17. Record a commercial credit through `metering_billing.CreditAdjustmentService.record_credit_adjustment` from a stored invoice draft.  Apply the exact inclusive credit amount, reduce collection outstanding when a case exists, and emit one validated journal proposal.  When the draft is taxed, split the credit proportionally and debit `usage_revenue` plus `tax_payable` against an `accounts_receivable` credit.  Untaxed credits stay two-line revenue/AR.  Fail closed before insert when the credit or its exclusive/tax split has more than six significant fractional digits.  Record the credit; AIS pulls the validated three-line unwind.  Do not post, call AIS, or open a fiscal period from that path.
18. Publish a tenant-scoped rate card through `metering_billing.RateCardService.publish_rate_card`.  Persist one `rate_card` and one immutable `rate_card_version` with `rate_card_line` rows.  Rate stored usage only against that persisted version.  A missing or cross-tenant version, or a missing metric on the card, fails closed.  Do not invent a hidden default price or edit a published version from that path.  Publish a rate card, then rate a window against that version.
19. Publish a tenant-scoped tax rate through `metering_billing.TaxRateService.publish_tax_rate` and assess a stored draft through `metering_billing.TaxAssessmentService.assess_tax`.  Persist one `tax_rate_schedule`, one immutable `tax_rate_version`, and one `tax_assessment`.  Round tax half-even to the documented ISO 4217 minor units.  Reject assess after collection is open.  Propose a three-line journal when assessed (`accounts_receivable`, `usage_revenue`, `tax_payable`).  AIS must map `tax_payable`.  Do not call an OSS tax engine, store exemptions, or start operator UI from that path.  Publish a tax rate, assess the draft, then propose the journal and let AIS pull.
20. Present a stored draft through `metering_billing.InvoicePresentmentService.present_invoice_draft`.  `GET /v1/invoice-drafts/{invoice_draft_id}` returns the tenant-scoped statement.  Cross-tenant or unknown is 404.  Open the draft statement, then collect or credit.  Do not post, call AIS, or start a web UI from that path.
21. Issue a tenant API credential through `metering_billing.TenantApiCredentialService.issue_credential`.  Return prefix plus secret once and persist only a keyed HMAC.  After one active key exists, every `/v1` call except credential issue requires that key.  Zero active keys keep the tenant pin so AIS can keep pulling with `X-CWL-Tenant-Reference`.  `GET /healthz` stays unauthenticated.  Issue a key, then send it on every `/v1` call; revoke when leaked.  Do not log the secret, put it on AIS contracts, or start a web UI from that path.
22. Render a stored presentment statement in `operator_console` Storybook.  Use design tokens and the InvoiceStatement, AmountDue, LineTable, and StatusChip stories.  Show amount due and the next operator action: collect or credit.  Do not replace the Python package, add a production SPA, call AIS, or add Stripe.
23. Register an https webhook callback through `metering_billing.WebhookSubscriptionService.register_subscription`, then run `WebhookDeliveryService.deliver_due_events` or `POST /v1/webhook-deliveries`.  Accepted journal proposals, payment receipts, credits, issued invoices, issued-invoice voids, issued credit notes, issued-credit-note voids, credit-note applications, collection-case settlements, and collection write-offs append `webhook_outbox_event` rows.  Sign POSTs with `X-CWL-Webhook-Signature: sha256=<hex>`.  AIS may keep polling.  Do not flip `proposal_status`, call AIS posting-receipt, or emit statutory IDs.
24. Drain AIS `posting_receipt` outbox events through `metering_billing.AisOutboxDrainService.drain_ais_outbox` or `POST /v1/ais-outbox-drains`.  Match constructed `urn:cwl:accounting:posting_receipt:{proposal_id}` and `urn:cwl:accounting:general_journal:{proposal_id}` by equality.  Do not parse the payload URN.  GET `/posting-receipts` with the stored Billing `idempotency_key` only.  Empty unpublished pages skip receipt GETs.  After a stored observation, POST publish.  Do not drain `journal_reversal` or `period_close`.  Do not flip `proposal_status`.
25. Present a stored collection case through `metering_billing.CollectionCasePresentmentService.present_collection_case`.  `GET /v1/collection-cases/{collection_case_id}` returns the tenant-scoped statement.  `GET /v1/collection-cases` lists `{collection_cases, next_cursor}`.  Cross-tenant or unknown is 404.  Open the collection case, then collect or credit.  Do not capture, call AIS, flip `proposal_status`, or start a production SPA.
26. Project and present a stored payment intent through `metering_billing.PaymentIntentService.project_payment_intent` and `PaymentIntentPresentmentService.present_payment_intent`.  `POST /v1/payment-intents` keys on `collection_case_id`.  `GET /v1/payment-intents/{payment_intent_id}` returns the tenant-scoped statement.  `GET /v1/payment-intents` lists `{payment_intents, next_cursor}`.  Cross-tenant or unknown is 404.  Create a projected payment intent, then record the receipt.  Do not capture, call AIS, flip `proposal_status`, or start a production SPA.
27. Apply and present a stored payment receipt through `metering_billing.PaymentSettlementService.record_payment_receipt` and `PaymentReceiptPresentmentService.present_payment_receipt`.  `POST /v1/payment-receipts` keys on `payment_intent_id` and composes the #13 cash journal.  `GET /v1/payment-receipts/{payment_receipt_id}` returns the tenant-scoped statement.  `GET /v1/payment-receipts` lists `{payment_receipts, next_cursor}`.  Cross-tenant or unknown is 404.  Record the receipt; the cash journal is already validated for AIS to pull.  Do not capture, call AIS, flip `proposal_status`, or start a production SPA.
28. Present a stored credit adjustment through `metering_billing.CreditAdjustmentPresentmentService.present_credit_adjustment`.  `POST /v1/credit-adjustments` stays the #17 write keyed on `invoice_draft_id`.  `GET /v1/credit-adjustments/{credit_adjustment_id}` returns the tenant-scoped statement.  `GET /v1/credit-adjustments` lists `{credit_adjustments, next_cursor}`.  Cross-tenant or unknown is 404.  Record the credit; AIS pulls the validated journal.  Do not invent a journal, call AIS, flip `proposal_status`, or start a production SPA.
29. Present a stored rate card through `metering_billing.RateCardPresentmentService.present_rate_card`.  `POST /v1/rate-cards` stays the #18 write.  `GET /v1/rate-cards/{rate_card_id}` returns the tenant-scoped statement.  `GET /v1/rate-cards` lists `{rate_cards, next_cursor}`.  Cross-tenant or unknown is 404.  Publish a rate card, then rate a window against that version.  Do not invent a catalog, call AIS, or start a production SPA.
30. Present a stored usage event through `metering_billing.UsageEventPresentmentService.present_usage_event`.  `POST /v1/usage-events` stays the #5 ingest.  `GET /v1/usage-events/{usage_event_id}` returns the tenant-scoped statement.  `GET /v1/usage-events` lists `{usage_events, next_cursor}`.  Cross-tenant or unknown is 404.  Ingest usage, then rate a window against a published card.  Do not invent an ingest shape, call AIS, or start a production SPA.
31. Present a stored rating run through `metering_billing.RatingRunPresentmentService.present_rating_run`.  `POST /v1/rating-runs` stays the #7 rate-a-window command.  `GET /v1/rating-runs/{rating_run_id}` returns the tenant-scoped statement.  `GET /v1/rating-runs` lists `{rating_runs, next_cursor}`.  Cross-tenant or unknown is 404.  Rate a window, then draft an invoice.  Do not invent a rating shape, call AIS, or start a production SPA.
32. Present a stored tax assessment through `metering_billing.TaxAssessmentPresentmentService.present_tax_assessment`.  `POST /v1/tax-assessments` stays the #19 assess command.  `GET /v1/tax-assessments/{tax_assessment_id}` stays the existing #19 item read.  `GET /v1/tax-assessments` lists `{tax_assessments, next_cursor}`.  Cross-tenant or unknown is 404.  Publish a tax rate, assess the draft, then propose the journal and let AIS pull.  Do not invent a tax engine, journal, call AIS, or start a production SPA.
33. Present a stored posting-receipt observation through `metering_billing.PostingReceiptObservationPresentmentService.present_posting_receipt_observation`.  `POST /v1/posting-receipt-observations` stays the #16 pull.  `GET /v1/posting-receipt-observations/{idempotency_key}` stays the existing #16 item read.  `GET /v1/posting-receipt-observations` lists `{posting_receipt_observations, next_cursor}`.  Cross-tenant or unknown is 404.  Drain AIS outbox, then store the receipt observation.  Do not flip `proposal_status`, invent a receipt shape, call AIS, or start a production SPA.
34. Present a stored webhook delivery attempt through `metering_billing.WebhookDeliveryPresentmentService.present_webhook_delivery`.  `POST /v1/webhook-deliveries` stays the #24 deliver run.  `GET /v1/webhook-deliveries/{delivery_attempt_id}` returns the tenant-scoped statement.  `GET /v1/webhook-deliveries` lists `{webhook_deliveries, next_cursor}`.  Cross-tenant or unknown is 404.  Register an https callback, then run deliveries; AIS may keep polling.  Do not resend on GET, invent `delivery_status`, leak webhook secrets or signed bodies, change HMAC `X-CWL-Webhook-Signature`, call AIS, or start a production SPA.
35. Present a stored tenant API credential through `metering_billing.TenantApiCredentialPresentmentService.present_tenant_api_credential`.  `POST /v1/tenant-api-credentials` stays the #22 issue.  `POST /v1/tenant-api-credentials/{tenant_api_credential_id}/revoke` stays the #22 revoke.  `GET /v1/tenant-api-credentials/{tenant_api_credential_id}` returns the tenant-scoped metadata statement.  `GET /v1/tenant-api-credentials` lists `{tenant_api_credentials, next_cursor}`.  Cross-tenant or unknown is 404.  Issue a key, then send it on every `/v1` call; revoke when leaked.  Do not reconstruct a secret, invent rotation, change the bootstrap window, call AIS, or start a production SPA.
36. Present a stored webhook subscription through `metering_billing.WebhookSubscriptionPresentmentService.present_webhook_subscription`.  `POST /v1/webhook-subscriptions` stays the #24 register.  `POST /v1/webhook-subscriptions/{webhook_subscription_id}/revoke` stays the #24 revoke.  `GET /v1/webhook-subscriptions/{webhook_subscription_id}` returns the tenant-scoped metadata statement.  `GET /v1/webhook-subscriptions` lists `{webhook_subscriptions, next_cursor}`.  Cross-tenant or unknown is 404.  Register an https callback, then run deliveries; AIS may keep polling.  Do not reconstruct a secret, invent rotation or retry, change SSRF or HMAC contracts, call AIS, or start a production SPA.
37. Present a stored collection dunning event through `metering_billing.DunningEventPresentmentService.present_dunning_event`.  `POST /v1/collection-cases/{collection_case_id}/dunning-events` stays the #10 record.  `GET /v1/dunning-events/{dunning_event_id}` returns the tenant-scoped metadata statement.  `GET /v1/dunning-events` lists `{dunning_events, next_cursor}`.  Cross-tenant or unknown is 404.  Record the commercial reminder, then collect or credit.  Do not invent a send, email, SMS, or PSP engine, call AIS, or start a production SPA.
38. Present a stored commercial webhook outbox event through `metering_billing.WebhookOutboxEventPresentmentService.present_webhook_outbox_event`.  Commercial-fact enqueue and `POST /v1/webhook-deliveries` stay the #24 write path.  `GET /v1/webhook-outbox-events/{outbox_event_id}` returns the tenant-scoped metadata statement.  `GET /v1/webhook-outbox-events` lists `{webhook_outbox_events, next_cursor}`.  Cross-tenant or unknown is 404.  Register an https callback, then run deliveries; AIS may keep polling.  Do not publish, send, retry, or mark delivered on GET, leak `payload_json` or webhook secrets, change HMAC or SSRF contracts, drain AIS outbox, or start a production SPA.
39. Issue a commercial invoice snapshot through `metering_billing.IssuedInvoiceService.issue_invoice` from a stored invoice draft.  Replay of the same tenant and `invoice_draft_id` returns the stored `issued_invoice_id`.  `POST /v1/invoice-drafts/{invoice_draft_id}/issued-invoices` is the nested issue command.  Present the snapshot through `IssuedInvoicePresentmentService.present_issued_invoice`.  `GET /v1/issued-invoices/{issued_invoice_id}` returns the tenant-scoped statement.  `GET /v1/issued-invoices` lists `{issued_invoices, next_cursor}`.  Cross-tenant or unknown is 404.  Issue invoice, then collect or credit.  Do not invent statutory numbering, change draft/tax/journal/collection/payment/AIS contracts, capture payment, call AIS, or start a production SPA.
40. On first successful issue, enqueue one existing #24 `webhook_outbox_event` with type `invoice.issued` and `source_id` `issued_invoice_id`.  Existing subscriptions opt in by including `invoice.issued`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not invoice lines, PAN, secrets, or statutory identifiers.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, or capture payment.
41. Issue a commercial credit-note snapshot through `metering_billing.IssuedCreditNoteService.issue_credit_note` from a stored credit adjustment.  Replay of the same tenant and `credit_adjustment_id` returns the stored `issued_credit_note_id`.  `PostgresUsageLedger` persists that issued row; GET presentment survives process restart.  `POST /v1/credit-adjustments/{credit_adjustment_id}/issued-credit-notes` is the nested issue command.  Present the snapshot through `IssuedCreditNotePresentmentService.present_issued_credit_note`.  `GET /v1/issued-credit-notes/{issued_credit_note_id}` returns the tenant-scoped statement.  `GET /v1/issued-credit-notes` lists `{issued_credit_notes, next_cursor}`.  Cross-tenant or unknown is 404.  Issue the credit note; the validated journal remains available for AIS.  Do not invent statutory numbering, credit-note lines, change credit/tax-unwind/journal/invoice/collection/payment/AIS contracts, capture payment, call AIS, or start a production SPA.
42. On first successful credit-note issue, enqueue one existing #24 `webhook_outbox_event` with type `credit_note.issued` and `source_id` `issued_credit_note_id`.  Existing subscriptions opt in by including `credit_note.issued`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not credit-note lines, PAN, secrets, or statutory identifiers.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, or capture payment.
43. Apply one issued credit note onto one open same-tenant collection case through `metering_billing.CreditNoteApplicationService.apply_credit_note`.  Replay of the same tenant and `issued_credit_note_id` returns the stored `credit_note_application_id`.  `POST /v1/collection-cases/{collection_case_id}/credit-note-applications` is the nested apply command.  Present the stored application through `CreditNoteApplicationPresentmentService.present_credit_note_application`.  `GET /v1/credit-note-applications/{credit_note_application_id}` returns the tenant-scoped statement.  `GET /v1/credit-note-applications` lists `{credit_note_applications, next_cursor}`.  Cross-tenant or unknown is 404.  Reduce outstanding by the exact issued inclusive amount.  First successful apply enqueues one #24 `credit_note.applied` outbox event.  Apply the issued credit note, then collect the residual.  Do not invent a journal, tax unwind, dunning engine, PSP, AIS call, statutory numbering, write-off, settlement, or payment receipt.
44. Settle one same-tenant open collection case at exact-zero outstanding through `metering_billing.CollectionCaseSettlementService.settle_collection_case`.  Replay of the same tenant and `collection_case_id` returns the stored `collection_case_settlement_id`.  `POST /v1/collection-cases/{collection_case_id}/settlements` is the nested settle command.  Present the stored settlement through `CollectionCaseSettlementPresentmentService.present_collection_case_settlement`.  `GET /v1/collection-case-settlements/{collection_case_settlement_id}` returns the tenant-scoped statement.  `GET /v1/collection-case-settlements` lists `{collection_case_settlements, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed when outstanding is not zero or the case is already settled.  First successful settle enqueues one #24 `collection.settled` outbox event.  Settle the zero-outstanding case, then wait.  Do not invent a journal, tax unwind, dunning engine, PSP, AIS call, statutory numbering, write-off, or payment receipt.
45. On first successful collection-case settle, enqueue one existing #24 `webhook_outbox_event` with type `collection.settled` and `source_id` `collection_case_settlement_id`.  Existing subscriptions opt in by including `collection.settled`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not PII, PAN, secrets, statutory identifiers, or a write-off.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a write-off, or a payment receipt.  Do not backfill cases already settled by #12/#45 without a settlement row.
46. On first successful credit-note apply, enqueue one existing #24 `webhook_outbox_event` with type `credit_note.applied` and `source_id` `credit_note_application_id`.  Existing subscriptions opt in by including `credit_note.applied`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not PII, PAN, secrets, statutory identifiers, remaining outstanding, a write-off, or a settlement.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a write-off, a settlement, or a payment receipt.
47. Write off leftover remaining on one same-tenant open collection case through `metering_billing.CollectionWriteOffService.write_off_collection_case`.  Replay of the same tenant and `collection_case_id` returns the stored `collection_write_off_id`.  `POST /v1/collection-cases/{collection_case_id}/write-offs` is the nested write-off command.  Present the stored write-off through `CollectionWriteOffPresentmentService.present_collection_write_off`.  `GET /v1/collection-write-offs/{collection_write_off_id}` returns the tenant-scoped statement.  `GET /v1/collection-write-offs` lists `{collection_write_offs, next_cursor}`.  Cross-tenant or unknown is 404.  Remaining becomes exact zero without flipping case status.  Fail closed when the case is missing or settled, remaining is already zero or negative, currency mismatches, or a supplied amount does not equal remaining.  First successful write-off enqueues one #24 `write_off.recorded` outbox event.  Write off leftover remaining, then settle.  Do not invent a journal, tax unwind, dunning engine, PSP, AIS call, statutory numbering, payment receipt, credit note, or settlement command.
48. On first successful collection write-off, enqueue one existing #24 `webhook_outbox_event` with type `write_off.recorded` and `source_id` `collection_write_off_id`.  Existing subscriptions opt in by including `write_off.recorded`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not PII, PAN, secrets, statutory identifiers, later-mutated case remaining, a payment receipt, or a settlement.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a payment receipt, a credit note, or a settlement command.
49. Propose a write-off journal through `metering_billing.AccountingExportService.propose_write_off_journal` from a stored collection write-off.  Replay of the same tenant and `collection_write_off_id` returns the stored `proposal_id`.  `POST /v1/collection-write-offs/{collection_write_off_id}/journal-proposals` is the explicit compose command.  Debit semantic `write_off_expense` and credit semantic `accounts_receivable` for the exact write-off inclusive amount.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant write-off, currency mismatch, and zero or negative amounts.  Write off leftover remaining, compose the journal, then settle.  Do not call AIS, flip `proposal_status` to `posted`, invent a statutory account ID, a new webhook type, tax unwind, dunning engine, PSP, payment receipt, credit note, or settlement command.
50. Propose a credit journal through `metering_billing.AccountingExportService.propose_credit_journal` from a stored credit adjustment.  Credit accept already composes this write.  Replay of the same tenant and `credit_adjustment_id` returns the stored `proposal_id`.  `POST /v1/credit-adjustments/{credit_adjustment_id}/journal-proposals` is the explicit compose/replay.  Debit semantic `usage_revenue` and credit semantic `accounts_receivable` for the exact credit inclusive amount.  Taxed credits reuse the existing #20 `tax_payable` unwind on the same journal.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant credit, currency mismatch, and zero or negative amounts.  Record the credit, then POST compose if the journal is missing.  Do not call AIS, flip `proposal_status` to `posted`, invent a statutory account ID, a new webhook type, second journal store, write-off, settlement, or payment.
51. Present collection aging through `metering_billing.CollectionAgingPresentmentService.present_collection_aging` so a tenant returns current / 1-30 / 31-60 / 61-90 / 90+ outstanding grouped by `currency_code`.  `GET /v1/collection-aging` is the HTTP read.  Missing tenant is 422.  Age only `open` or `dunning` cases whose remaining is a positive exact decimal.  Due date is issued-invoice `due_at` when stored, otherwise `collection_case.opened_at`.  Settled cases and exact-zero remaining are omitted.  Do not invent a journal, webhook, write-off, settlement, payment, dunning engine, AIS call, statutory numbering, or new money fact.
52. Park leftover remittance against one same-tenant stored payment receipt through `metering_billing.UnappliedCashService.park_unapplied_cash`.  Replay of the same tenant and `payment_receipt_id` returns the stored `unapplied_cash_id`.  `POST /v1/payment-receipts/{payment_receipt_id}/unapplied-cash` is the nested park command.  Present the stored leftover through `UnappliedCashPresentmentService.present_unapplied_cash`.  `GET /v1/unapplied-cash/{unapplied_cash_id}` returns the tenant-scoped statement.  `GET /v1/unapplied-cash` lists `{unapplied_cash, next_cursor}`.  Cross-tenant or unknown is 404.  #12 still rejects overpay.  Omitting leftover fail-closes as already consumed.  Fail closed on zero, negative, oversized leftover, currency mismatch, IEEE leftover, and missing or cross-tenant receipt.  Do not auto-apply leftover to another case.  Do not invent a journal, webhook, write-off, settlement, credit note, dunning engine, PSP, AIS call, or statutory numbering.
53. Apply one parked leftover onto one same-tenant open collection case through `metering_billing.UnappliedCashApplicationService.apply_unapplied_cash`.  Replay of the same tenant and `unapplied_cash_id` returns the stored `unapplied_cash_application_id`.  `POST /v1/collection-cases/{collection_case_id}/unapplied-cash-applications` is the nested apply command.  Present the stored application through `UnappliedCashApplicationPresentmentService.present_unapplied_cash_application`.  `GET /v1/unapplied-cash-applications/{unapplied_cash_application_id}` returns the tenant-scoped statement.  `GET /v1/unapplied-cash-applications` lists `{unapplied_cash_applications, next_cursor}`.  Cross-tenant or unknown is 404.  Apply the full parked amount.  Remaining zero does not settle; #46 remains the explicit settle-when-zero command.  Fail closed on missing/cross-tenant leftover or case, leftover already applied, already settled case, currency mismatch, apply greater than parked or remaining, zero/negative leftover, negative remaining, and IEEE leftover.  First successful apply enqueues one #24 `unapplied_cash.applied` outbox event.  Apply parked leftover, then collect the residual or settle at exact zero.  Do not invent a journal, write-off, credit note, dunning engine, PSP, AIS call, statutory numbering, or settlement command.
54. On first successful leftover apply, enqueue one existing #24 `webhook_outbox_event` with type `unapplied_cash.applied` and `source_id` `unapplied_cash_application_id`.  Existing subscriptions opt in by including `unapplied_cash.applied`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not PII, PAN, secrets, statutory identifiers, remaining outstanding, a write-off, or a settlement.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a write-off, a settlement command, or a payment-receipt rewrite.
55. Refund one parked leftover through `metering_billing.UnappliedCashRefundService.refund_unapplied_cash`.  Replay of the same tenant and `unapplied_cash_id` returns the stored `unapplied_cash_refund_id`.  `POST /v1/unapplied-cash/{unapplied_cash_id}/refunds` is the nested refund command.  Present the stored refund through `UnappliedCashRefundPresentmentService.present_unapplied_cash_refund`.  `GET /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}` returns the tenant-scoped statement.  `GET /v1/unapplied-cash-refunds` lists `{unapplied_cash_refunds, next_cursor}`.  Cross-tenant or unknown is 404.  Refund the full parked amount.  The parked leftover row stays `parked`.  Fail closed on missing/cross-tenant leftover, leftover already applied, leftover not parked, currency mismatch, zero/negative leftover, mismatched amount, and IEEE leftover.  Apply fail-closes when a refund already exists.  First successful refund enqueues one #24 `refund.recorded` outbox event.  Do not invent a journal, write-off, settlement, credit note, dunning engine, PSP capture, AIS call, or statutory numbering.
56. On first successful leftover refund, enqueue one existing #24 `webhook_outbox_event` with type `refund.recorded` and `source_id` `unapplied_cash_refund_id`.  Existing subscriptions opt in by including `refund.recorded`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not PII, PAN, secrets, statutory identifiers, a write-off, a settlement, or a PSP capture.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a write-off, a settlement command, or a payment-receipt rewrite.
57. Propose a refund journal through `metering_billing.AccountingExportService.propose_refund_journal` from a stored leftover refund.  Replay of the same tenant and `unapplied_cash_refund_id` returns the stored `proposal_id`.  `POST /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}/journal-proposals` is the explicit compose command.  Debit semantic `unapplied_cash` and credit semantic `cash_receipt` for the exact refund inclusive amount.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant refund, currency mismatch, and zero or negative amounts.  Refund unused leftover, compose the journal, then let AIS pull.  Do not call AIS, flip `proposal_status` to `posted`, invent a statutory account ID, a new webhook type, PSP, write-off, settlement, or payment-receipt rewrite.
58. Propose a leftover journal through `metering_billing.AccountingExportService.propose_unapplied_cash_journal` from a stored parked leftover.  Replay of the same tenant and `unapplied_cash_id` returns the stored `proposal_id`.  `POST /v1/unapplied-cash/{unapplied_cash_id}/journal-proposals` is the explicit compose command.  Debit semantic `cash_receipt` and credit semantic `unapplied_cash` for the exact parked leftover inclusive amount.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant leftover, leftover not parked, currency mismatch, and zero or negative amounts.  Park leftover remittance, compose the journal, then let AIS pull.  Do not call AIS, flip `proposal_status` to `posted`, invent a statutory account ID, a new leftover role, a new webhook type, PSP, write-off, settlement, payment-receipt rewrite, or refund rewrite.
59. Propose an apply journal through `metering_billing.AccountingExportService.propose_unapplied_cash_application_journal` from a stored leftover apply.  Replay of the same tenant and `unapplied_cash_application_id` returns the stored `proposal_id`.  `POST /v1/unapplied-cash-applications/{unapplied_cash_application_id}/journal-proposals` is the explicit compose command.  Debit semantic `unapplied_cash` and credit semantic `accounts_receivable` for the exact applied inclusive amount.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant application, currency mismatch, and zero or negative amounts.  Apply parked leftover, compose the journal, then let AIS pull.  Do not call AIS, flip `proposal_status` to `posted`, invent a statutory account ID, a new leftover role, a new webhook type, PSP, write-off, settlement, payment-receipt rewrite, park rewrite, or refund rewrite.
60. Present a billing-account statement through `metering_billing.AccountStatementPresentmentService.present_account_statement` so a tenant and `billing_account_id` return issued-invoice totals, unused issued-invoice voids, open collection remaining, applied credits, unused issued-credit-note voids, write-offs, parked leftover, and refunded leftover grouped by `currency_code`.  `GET /v1/billing-accounts/{billing_account_id}/statement` is the HTTP read.  Missing tenant is 422.  Missing account is 404.  Cross-tenant account is 403.  Attribute money only through invoice-draft lines exclusive to that account.  `issued_invoice_total` stays the issued snapshot.  `applied_credit_total` stays applied credits only.  Do not invent a journal, webhook, PSP, dispute, settlement rewrite, AIS call, statutory numbering, or new money fact.
61. Void one unused same-tenant issued invoice through `metering_billing.IssuedInvoiceVoidService.void_issued_invoice`.  Replay of the same tenant and `issued_invoice_id` returns the stored `issued_invoice_void_id`.  `POST /v1/issued-invoices/{issued_invoice_id}/voids` is the nested void command.  Present the stored void through `IssuedInvoiceVoidPresentmentService.present_issued_invoice_void`.  `GET /v1/issued-invoice-voids/{issued_invoice_void_id}` returns the tenant-scoped statement.  `GET /v1/issued-invoice-voids` lists `{issued_invoice_voids, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed when the invoice is missing or cross-tenant, the case already has a payment receipt, credit-note apply, unapplied-cash apply, or write-off, remaining no longer equals the issued amount, the case is already settled, currency mismatches, or the tenant is missing.  An unused open or dunning case closes as `voided` at exact-zero remaining.  Do not reuse `settled`.  The issued snapshot stays `issued`.  Do not invent a journal, webhook, PSP, refund, write-off rewrite, statement rewrite, AIS call, or statutory numbering.
62. On first successful issued-invoice void, enqueue one existing #24 `webhook_outbox_event` with type `invoice.voided` and `source_id` `issued_invoice_void_id`.  Existing subscriptions opt in by including `invoice.voided`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not remaining outstanding, collection status, PII, PAN, secrets, or statutory identifiers.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a journal, a refund, a write-off, or a settlement rewrite.
73. Extend the existing #62 billing-account statement with exact inclusive `voided_invoice_total` and `voided_credit_total` on each currency row.  `voided_invoice_total` sums stored unused issued-invoice voids whose invoice-draft lines belong exclusively to that billing account.  `voided_credit_total` sums stored unused issued-credit-note voids whose credit/draft lines belong exclusively to that account.  Do not rewrite `issued_invoice_total` or count a voided unused note as applied credit.  `GET /v1/billing-accounts/{billing_account_id}/statement` stays the safe read.  Missing tenant is 422.  Missing account is 404.  Cross-tenant account is 403.  Do not invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, or collection-status flip.
74. Render a stored billing-account statement in `operator_console` Storybook.  Use design tokens and the AccountStatement, AmountDue, StatusChip, and tenant-pin modules.  Fixtures are a settled account statement and a same-currency statement with exact inclusive non-zero `voided_invoice_total` and `voided_credit_total`.  Show remaining and the next operator action: collect, credit, park, apply, or refund.  `GET /v1/billing-accounts/{billing_account_id}/statement` stays the existing #62/#75 read.  Do not replace the Python package, add a production SPA, change statement totals, invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, or add Stripe.
75. Present already-rated spend through `metering_billing.RatedSpendPresentmentService.present_rated_spend` so a tenant, `billing_account_id`, and half-open `TimeWindow` return stored rating-run and exclusive invoice-draft line amounts grouped by `product_code`.  `GET /v1/billing-accounts/{billing_account_id}/rated-spend?window_started_at=&window_ended_at=` is the HTTP read.  Missing tenant is 422.  Missing account is 404.  Cross-tenant account is 403.  An illegal window is 422.  Take money only from already-stored rating or exclusive-draft lines whose usage or draft attribution belongs only to that billing account.  Do not re-rate, invent a unit price, include unrated usage, or mix currencies.  One row per `(currency_code, product_code)` carries exact `rated_amount` as a Decimal string.  Mixed-account and lineless drafts are omitted.  Identity is the tenant pin plus `billing_account_id` plus the window.  Replay is a safe GET and writes no money fact.  Do not add `group_by=project` or `group_by=credential`, open a spend-budget write, invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning.
76. Extend the existing #77 rated-spend GET with optional `group_by=product|project`.  Omitting `group_by` or sending `group_by=product` keeps one row per `(currency_code, product_code)` with no `project_reference`.  `group_by=project` publishes one row per `(currency_code, product_code, project_reference)` whose `project_reference` is the stored exclusive-account usage URN.  Usage without `project_reference` is omitted from the project grouping.  Mixed projects omit the run.  Unknown `group_by` is 422.  Do not invent a second route, re-rate, invent a unit price, include unrated usage, invent a project or sentinel, add `group_by=credential`, open a spend-budget write, invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning.
77. Extend the existing #77/#78 rated-spend GET with optional `group_by=product|project|credential`.  Omitting `group_by` or sending `group_by=product` keeps one row per `(currency_code, product_code)` with no `project_reference` or `credential_reference`.  `group_by=project` stays one row per `(currency_code, product_code, project_reference)`.  `group_by=credential` publishes one row per `(currency_code, product_code, credential_reference)` whose `credential_reference` is the stored exclusive-account usage-event credential URN.  Usage without `credential_reference` is omitted from the credential grouping.  Mixed credentials omit the run.  Unknown `group_by` is 422.  Do not invent a second route, re-rate, invent a unit price, include unrated usage, invent a credential or sentinel, add `group_by=principal` or `group_by=cost_center`, open a spend-budget write, invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning.
78. Extend the existing #77/#78/#79 rated-spend GET with optional `group_by=product|project|credential|principal`.  Omitting `group_by` or sending `group_by=product` keeps one row per `(currency_code, product_code)` with no `project_reference`, `credential_reference`, or `billing_principal_reference`.  `group_by=project` stays one row per `(currency_code, product_code, project_reference)`.  `group_by=credential` stays one row per `(currency_code, product_code, credential_reference)`.  `group_by=principal` publishes one row per `(currency_code, product_code, billing_principal_reference)` whose `billing_principal_reference` is the stored exclusive-account usage-event principal URN.  Mixed or unresolved principals omit the run.  Unknown `group_by` is 422.  Do not invent a second route, re-rate, invent a unit price, include unrated usage, invent a principal or sentinel, add `group_by=cost_center`, open a spend-budget write, invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning.
79. Extend the existing #77/#78/#79/#80 rated-spend GET with optional `group_by=product|project|credential|principal|cost_center`.  Omitting `group_by` or sending `group_by=product` keeps one row per `(currency_code, product_code)` with no `project_reference`, `credential_reference`, `billing_principal_reference`, or `cost_center_reference`.  `group_by=project` stays one row per `(currency_code, product_code, project_reference)`.  `group_by=credential` stays one row per `(currency_code, product_code, credential_reference)`.  `group_by=principal` stays one row per `(currency_code, product_code, billing_principal_reference)`.  `group_by=cost_center` publishes one row per `(currency_code, product_code, cost_center_reference)` whose `cost_center_reference` is the stored exclusive-account usage-event cost-center URN.  Usage without `cost_center_reference` is omitted from the cost-center grouping.  Mixed cost centers omit the run.  Unknown `group_by` is 422.  Do not invent a second route, re-rate, invent a unit price, include unrated usage, invent a cost center or sentinel, open a spend-budget write, invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, collection-status flip, or tenant provisioning.
80. Publish one commercial spend budget through `metering_billing.SpendBudgetService.publish_spend_budget` so a tenant, `billing_account_id`, ISO 4217 currency, exact `budget_amount` greater than zero, and half-open `TimeWindow` persist one append-only `spend_budget`.  Identity is `(tenant_account_id, billing_account_id, window_started_at, window_ended_at, currency_code, source_payload_hash, spend_budget_contract_version)`.  Internal primary key is opaque generated `spend_budget_id`.  Compute the hash from the canonical payload; a caller-supplied hash must match.  Replay of the same identity returns the stored `spend_budget_id` as `duplicate_replay`.  A later distinct amount or hash appends a new row.  Status is `published` only.  `POST /v1/billing-accounts/{billing_account_id}/spend-budgets` is the write.  Present the stored row through `SpendBudgetPresentmentService.present_spend_budget`.  `GET /v1/spend-budgets/{spend_budget_id}` returns the tenant-scoped statement.  `GET /v1/spend-budgets` lists `{spend_budgets, next_cursor}`.  Cross-tenant or unknown is 404.  Missing tenant is 422.  Unknown billing account is 404.  Cross-tenant account is 403.  Next operator action is `wait`.  Do not compare the budget to rated spend, hard-stop rating, ingest, or invoice draft, emit a webhook or journal, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory ID, call AIS, flip collection status, or provision a tenant.
81. Present a stored spend-budget evaluation through `metering_billing.SpendBudgetEvaluationPresentmentService.present_spend_budget_evaluation` so a tenant and `spend_budget_id` compare one published commercial `spend_budget` to already-rated spend for the same billing account, half-open window, and currency.  Reuse `RatedSpendPresentmentService` with `group_by=product`.  `remaining_amount` and `over_amount` are complementary non-negative exact Decimals.  `utilization_status` is `under`, `at`, or `over`.  `GET /v1/spend-budgets/{spend_budget_id}/evaluation` is the HTTP read.  Same-tenant is 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  A budget whose billing account belongs to another commercial `tenant_account` is 403.  Next operator action is `wait`.  Replay is a safe GET and writes no money fact.  Do not persist, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a webhook or journal, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory ID, call AIS, flip collection status, or provision a tenant.
82. Present every published commercial spend budget on one same-tenant billing account through `metering_billing.SpendBudgetEvaluationPresentmentService.list_billing_account_budget_statuses` so a tenant and `billing_account_id` return `{budget_statuses, next_cursor}` using the same rated-spend plus exact remaining/over math as #81.  `GET /v1/billing-accounts/{billing_account_id}/budget-status` is the HTTP read.  Order is `published_at` then `spend_budget_id`.  `page_limit` defaults to 50 and maxes at 100.  Same-tenant is 200.  Unknown billing account is 404.  Cross-tenant account is 403.  Missing tenant is 422.  Unknown or cross-tenant budgets are omitted with no leak.  Next operator action is `wait`.  Replay is a safe GET and writes no money fact.  Do not persist, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a webhook or journal, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory ID, call AIS, flip collection status, or provision a tenant. POST spend-budgets, one-budget evaluation GET, and #24 outbox stay unchanged.
83. Render published account-level spend-budget evaluations in `operator_console` Storybook.  Use design tokens and the BudgetStatus, AmountDue, StatusChip, and tenant-pin modules.  Fixtures are one same-tenant account covering under and over (and at) plus a keyset `next_cursor` page.  Show remaining, over, utilization, and the next operator action: wait.  `GET /v1/billing-accounts/{billing_account_id}/budget-status` stays the existing #97 read.  Do not replace the Python package, add a production SPA, invent BudgetGauge, change spend-budget write or evaluation, invent a journal, webhook, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, or add Stripe.
84. Observe first-over utilization through `metering_billing.SpendBudgetOverSignalService.observe_spend_budget_over` so a tenant and `spend_budget_id` reuse `SpendBudgetEvaluationPresentmentService` remaining/over math and enqueue one existing #24 `webhook_outbox_event` with type `spend_budget.over` and `source_id` `spend_budget_id`.  `POST /v1/spend-budgets/{spend_budget_id}/over-signal` is the write.  Same-tenant published budgets are 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  A budget whose billing account belongs to another commercial `tenant_account` is 403.  under and at write zero over-signal rows.  Replay of the same over observation is `duplicate_replay` and does not enqueue a second row.  Rejected observe writes zero rows.  Existing subscriptions opt in by including `spend_budget.over`.  The payload is a thin reference plus hash and exact `over_amount`, not remaining, rated lines, PII, PAN, secrets, or statutory identifiers.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  GET evaluation, GET budget-status, persist, Storybook, and `spend_budget.published` stay unchanged.  Do not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory ID, call AIS, flip collection status, or provision a tenant.
85. Render the existing #101 `spend_budget.over` write in `operator_console` Storybook.  Use design tokens and the SpendBudgetOver, AmountDue, StatusChip, and tenant-pin modules.  Fixtures are one same-tenant first-over accepted observation, one same-tenant under observation with zero over-signal rows, and one `duplicate_replay` of the same source.  Observation fixtures validate against the existing over-signal schema.  The first-over outbox fixture validates against the existing webhook-outbox presentment schema.  Show exact `over_amount` and `budget_amount`, utilization, and the next operator action: wait.  `POST /v1/spend-budgets/{spend_budget_id}/over-signal` stays the existing #101 write.  Do not replace the Python package, add a production SPA, invent BudgetGauge, add a new HTTP write or GET side-effect, change GET evaluation, GET budget-status, persist, SpendBudget/RatedSpend/BudgetStatus Storybooks, or `spend_budget.published`, invent a journal, webhook type, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, or add Stripe.
86. Present the live #101 over-signal observation through `metering_billing.SpendBudgetOverSignalPresentmentService.present_spend_budget_over_signal` so a tenant and `spend_budget_id` reuse `SpendBudgetEvaluationPresentmentService` remaining/over math and the existing over-signal envelope plus zero or one stored #24 `spend_budget.over` webhook-outbox presentment.  `GET /v1/spend-budgets/{spend_budget_id}/over-signal` is the HTTP read.  Same-tenant published budgets are 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  A budget whose billing account belongs to another commercial `tenant_account` is 403.  Replay is a safe GET and writes no outbox row.  Do not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory ID, call AIS, flip collection status, or provision a tenant.  `POST /v1/spend-budgets/{spend_budget_id}/over-signal` stays the existing #101 write.  GET evaluation, GET budget-status, persist, Storybook, and `spend_budget.published` stay unchanged.
87. Observe first-at approaching utilization through `metering_billing.SpendBudgetApproachingSignalService.observe_spend_budget_approaching` so a tenant and `spend_budget_id` reuse `SpendBudgetEvaluationPresentmentService` remaining/over math and enqueue one existing #24 `webhook_outbox_event` with type `spend_budget.approaching` and `source_id` `spend_budget_id` when `utilization_status` is `at` the documented `budget_amount` threshold.  `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the write.  Same-tenant published budgets are 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  A budget whose billing account belongs to another commercial `tenant_account` is 403.  under and over write zero approaching-signal rows.  Replay of the same at observation is `duplicate_replay` and does not enqueue a second row.  Rejected observe writes zero rows.  Existing subscriptions opt in by including `spend_budget.approaching`.  The payload is a thin reference plus hash and exact `remaining_amount`, not over, rated lines, PII, PAN, secrets, or statutory identifiers.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  GET evaluation, GET budget-status, GET over-signal, persist, Storybook, `spend_budget.published`, and `spend_budget.over` stay unchanged.  Do not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory ID, call AIS, flip collection status, or provision a tenant.  Do not invent a default notify percentage or widen the #82 publish contract.
88. Render the existing #104 `spend_budget.approaching` write in `operator_console` Storybook.  Use design tokens and the SpendBudgetApproaching, AmountDue, StatusChip, and tenant-pin modules.  Fixtures are one same-tenant first-at accepted observation, one same-tenant under observation with zero approaching-signal rows, and one `duplicate_replay` of the same source.  Observation fixtures validate against the existing approaching-signal schema.  The first-at outbox fixture validates against the existing webhook-outbox presentment schema.  Show exact `remaining_amount` and `budget_amount`, utilization, and the next operator action: wait.  `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` stays the existing #104 write.  Do not replace the Python package, add a production SPA, invent BudgetGauge, add a new HTTP write or GET side-effect, change the over write/GET, GET evaluation, GET budget-status, persist, SpendBudget/RatedSpend/BudgetStatus/SpendBudgetOver Storybooks, `spend_budget.published`, or `spend_budget.over`, invent a journal, webhook type, PSP, VAT register, NTS filing, 연말정산, statutory ID, AIS call, or add Stripe.
89. Present the live #104 approaching-signal observation through `metering_billing.SpendBudgetApproachingSignalPresentmentService.present_spend_budget_approaching_signal` so a tenant and `spend_budget_id` reuse `SpendBudgetEvaluationPresentmentService` remaining/over math and the existing approaching-signal envelope plus zero or one stored #24 `spend_budget.approaching` webhook-outbox presentment.  `GET /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the HTTP read.  Same-tenant published budgets are 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  A budget whose billing account belongs to another commercial `tenant_account` is 403.  Replay is a safe GET and writes no outbox row.  Notify threshold stays ADR 0082 `utilization_status=at`.  Do not persist an evaluation snapshot, mutate the budget, hard-stop rating, ingest, or invoice draft, emit a journal, invent a dimension-scoped budget, emit `retained_earnings` or 310100, invent a statutory ID, call AIS, flip collection status, or provision a tenant.  `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` stays the existing #104 write.  Over write/GET, GET evaluation, GET budget-status, persist, SpendBudget/RatedSpend/BudgetStatus/SpendBudgetOver/SpendBudgetApproaching Storybooks, `spend_budget.published`, and `spend_budget.over` stay unchanged.
72. Propose a credit-note void journal through `metering_billing.AccountingExportService.propose_credit_note_void_journal` from a stored unused issued-credit-note void.  Replay of the same tenant and `issued_credit_note_void_id` returns the stored `proposal_id`.  `POST /v1/issued-credit-note-voids/{issued_credit_note_void_id}/journal-proposals` is the explicit compose command.  Debit semantic `accounts_receivable` for the exact tax-inclusive voided amount and credit semantic `usage_revenue` for the issued exclusive amount.  Taxed unused notes also credit `tax_payable` on the same journal.  Bind the original credit journal by Billing `proposal_id` plus `credit_adjustment_id` / `issued_credit_note_id` only.  Fail closed if that original Billing proposal is missing.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant void, already-applied note, currency mismatch, and zero or negative amounts.  Void an unused issued credit note, compose the journal, then let AIS pull.  Do not call AIS, flip `proposal_status` to `posted`, invent a statutory account ID, `journal_entry_id`, a new webhook type, PSP, VAT register, NTS filing, 연말정산, or negative invoice.
71. On first successful unused issued-credit-note void, enqueue one existing #24 `webhook_outbox_event` with type `credit_note.voided` and `source_id` `issued_credit_note_void_id`.  Existing subscriptions opt in by including `credit_note.voided`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, not remaining outstanding, collection status, PII, PAN, secrets, AIS ids, or statutory identifiers.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a journal, a refund, a write-off, or a settlement rewrite.
70. Void one unused same-tenant issued credit note through `metering_billing.IssuedCreditNoteVoidService.void_issued_credit_note`.  Replay of the same tenant and `issued_credit_note_id` returns the stored `issued_credit_note_void_id`.  `POST /v1/issued-credit-notes/{issued_credit_note_id}/voids` is the nested void command.  Present the stored void through `IssuedCreditNoteVoidPresentmentService.present_issued_credit_note_void`.  `GET /v1/issued-credit-note-voids/{issued_credit_note_void_id}` returns the tenant-scoped statement.  `GET /v1/issued-credit-note-voids` lists `{issued_credit_note_voids, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed when the note is missing or cross-tenant, already applied, already voided (replay only), currency mismatches, or the tenant is missing.  Collection remaining is unchanged because the note cannot have been applied.  After a void exists, apply fail-closes as `issued_credit_note_voided`.  The issued snapshot stays `issued`.  Do not invent a journal, PSP, VAT register, NTS filing, 연말정산, statutory account, negative invoice, or AIS call.
69. Present a stored issued credit note through the existing `metering_billing.IssuedCreditNotePresentmentService.present_issued_credit_note`.  `GET /v1/issued-credit-notes/{issued_credit_note_id}` and `GET /v1/issued-credit-notes` stay the #43 reads.  Item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment still reproduces the frozen credit exclusive/tax split.  Omit the field when no matching assessment exists.  List summaries stay unchanged.  Cross-tenant or unknown is 404.  Missing tenant is 422.  Do not persist `tax_assessment_id` on `issued_credit_note`, invent amounts, a VAT register, 세금계산서, NTS/hometax field, statutory tax invoice number, journal, webhook, PSP, or AIS call.
68. Present a stored issued invoice through the existing `metering_billing.IssuedInvoicePresentmentService.present_issued_invoice`.  `GET /v1/issued-invoices/{issued_invoice_id}` and `GET /v1/issued-invoices` stay the #41 reads.  Item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment amounts still match the frozen issued totals.  Omit the field when no matching assessment exists.  List summaries stay unchanged.  Cross-tenant or unknown is 404.  Missing tenant is 422.  Do not persist `tax_assessment_id` on `issued_invoice`, invent amounts, a VAT register, 세금계산서, NTS/hometax field, statutory tax invoice number, journal, webhook, PSP, void rewrite, statement rewrite, or AIS call.
67. On first successful collection-dispute release, enqueue one existing #24 `webhook_outbox_event` with type `dispute.released` and `source_id` `collection_dispute_id` on the same hold row.  Existing subscriptions opt in by including `dispute.released`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, including exact remaining outstanding at release, not collection-case status, operator action, `held_at`, PII, PAN, secrets, statutory identifiers, or a dispute-reason blob.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a journal, a refund, a write-off, or a settlement rewrite.
66. On first successful collection-dispute hold, enqueue one existing #24 `webhook_outbox_event` with type `dispute.held` and `source_id` `collection_dispute_id`.  Existing subscriptions opt in by including `dispute.held`.  Replay must not enqueue a second row.  The payload is a thin reference plus hash, including exact remaining outstanding at hold, not collection-case status, operator action, PII, PAN, secrets, statutory identifiers, or a dispute-reason blob.  HMAC, SSRF, delivery, and outbox presentment stay unchanged.  Do not add `dispute.released`, a second webhook system, call AIS, flip `proposal_status`, invent statutory numbering, a journal, a refund, a write-off, or a settlement rewrite.
65. Release one same-tenant held collection dispute through `metering_billing.CollectionDisputeReleaseService.release_collection_dispute`.  Replay of the same tenant and `collection_dispute_id` returns the stored `collection_dispute_id`.  `POST /v1/collection-disputes/{collection_dispute_id}/releases` is the nested release command.  Present the stored release through `CollectionDisputeReleasePresentmentService.present_collection_dispute_release`.  `GET /v1/collection-dispute-releases/{collection_dispute_id}` returns the tenant-scoped statement.  `GET /v1/collection-dispute-releases` lists `{collection_dispute_releases, next_cursor}`.  Cross-tenant, unknown, or still-held is 404.  Dispute status becomes `released` without changing remaining outstanding.  Case status returns to `open`, or to `dunning` when stored notices already exist.  After release, dunning, payment receipt, credit apply, leftover apply, write-off, settle-when-zero, and void follow the existing open-case rules.  Fail closed when the dispute is missing or cross-tenant, not held, the case is missing, already settled or voided, currency mismatches, or the tenant is missing.  A later hold of the same case fail-closes as `collection_dispute_released`.  Release the hold, then collect or dunn.  Do not invent a journal, webhook, PSP, refund, write-off rewrite, void rewrite, statement rewrite, AIS call, or statutory numbering.
64. Hold one same-tenant open or dunning collection case as a commercial dispute through `metering_billing.CollectionDisputeService.hold_collection_case`.  Replay of the same tenant and `collection_case_id` returns the stored `collection_dispute_id`.  `POST /v1/collection-cases/{collection_case_id}/disputes` is the nested hold command.  Present the stored hold through `CollectionDisputePresentmentService.present_collection_dispute`.  `GET /v1/collection-disputes/{collection_dispute_id}` returns the tenant-scoped statement.  `GET /v1/collection-disputes` lists `{collection_disputes, next_cursor}`.  Cross-tenant or unknown is 404.  Case status becomes `disputed` without changing remaining outstanding.  New dunning, payment receipt, credit apply, leftover apply, write-off, settle-when-zero, and void fail closed while held.  Fail closed when the case is missing or cross-tenant, already disputed without a stored row, already settled or voided, currency mismatches, or the tenant is missing.  Hold the disputed case, then wait.  Do not invent a journal, webhook, PSP, refund, write-off rewrite, void rewrite, statement rewrite, AIS call, or statutory numbering.
63. Propose a void journal through `metering_billing.AccountingExportService.propose_void_journal` from a stored issued-invoice void.  Replay of the same tenant and `issued_invoice_void_id` returns the stored `proposal_id`.  `POST /v1/issued-invoice-voids/{issued_invoice_void_id}/journal-proposals` is the explicit compose command.  Debit semantic `usage_revenue` and credit semantic `accounts_receivable` for the exact inclusive voided amount.  Taxed unused issues also debit `tax_payable` on the same journal.  Bind the original invoice journal by Billing `proposal_id` / stored invoice-draft journal identity only.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant void, currency mismatch, and zero or negative amounts.  Void an unused issue, compose the journal, then let AIS pull.  Do not call AIS, flip `proposal_status` to `posted`, invent a statutory account ID, `journal_entry_id`, a new webhook type, PSP, write-off, settlement rewrite, or negative invoice.
