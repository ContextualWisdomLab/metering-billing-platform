# Agent Development Rules

## Authority

- Preserve the billing-versus-accounting boundary.
- Never use a provider object ID as an internal primary key.
- Never let a webhook directly grant entitlement or post accounting.

## Data

- Use two-or-more-word `snake_case` database identifiers.
- Keep normalized facts relational; raw provider payloads belong in immutable object storage.
- Never store card data, PAT plaintext, prompt text, response text, provider secrets, tenant API credential plaintext, or webhook-subscription plaintext.  Persist only a keyed HMAC on `tenant_api_credential` and `webhook_subscription`.
- Use exact decimals for money and billable quantities.

## Development

- Write a failing test before behavior code.
- Require production statement and branch coverage of 100%.
- Document every public API and every accounting or monetary invariant.
- Update architecture, ADRs, and CHANGELOG when authority or behavior changes.
- Keep usage ingestion append-only.  Deduplicate by tenant-scoped source-event key and by source-payload hash plus contract version.  Present a stored event through `metering_billing.UsageEventPresentmentService.present_usage_event`.  `POST /v1/usage-events` stays the #5 ingest.  `GET /v1/usage-events/{usage_event_id}` is the HTTP read.  `GET /v1/usage-events` lists `{usage_events, next_cursor}`.  Ingest usage, then rate a window against a published card.  Present a stored rating run through `metering_billing.RatingRunPresentmentService.present_rating_run`.  `POST /v1/rating-runs` stays the #7 rate-a-window command.  `GET /v1/rating-runs/{rating_run_id}` is the HTTP read.  `GET /v1/rating-runs` lists `{rating_runs, next_cursor}`.  Rate a window, then draft an invoice.  Present a stored tax assessment through `metering_billing.TaxAssessmentPresentmentService.present_tax_assessment`.  `POST /v1/tax-assessments` stays the #19 assess command.  `GET /v1/tax-assessments/{tax_assessment_id}` stays the existing #19 item read.  `GET /v1/tax-assessments` lists `{tax_assessments, next_cursor}`.  Publish a tax rate, assess the draft, then propose the journal and let AIS pull.
- Rate stored usage through `metering_billing.UsageRatingService` so a tenant window, rate-card version, and usage snapshot replay the same `rating_run_id` and exact totals.
- Draft invoice intent through `metering_billing.InvoiceDraftService` so a tenant and `rating_run_id` replay the same `invoice_draft_id` and exact totals.  Drafts are not issued, collected, or posted.
- Present a stored draft through `metering_billing.InvoicePresentmentService.present_invoice_draft` so a tenant and `invoice_draft_id` return one statement with tax, credits, amount due, optional collection, and rating lines.  `GET /v1/invoice-drafts/{invoice_draft_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Open the draft statement, then collect or credit.
- Issue a commercial invoice snapshot through `metering_billing.IssuedInvoiceService.issue_invoice` so a tenant and `invoice_draft_id` replay the same `issued_invoice_id` and frozen totals.  Present the snapshot through `IssuedInvoicePresentmentService.present_issued_invoice`.  `POST /v1/invoice-drafts/{invoice_draft_id}/issued-invoices` is the issue command.  `GET /v1/issued-invoices/{issued_invoice_id}` is the HTTP read.  `GET /v1/issued-invoices` lists `{issued_invoices, next_cursor}`.  Cross-tenant or unknown is 404.  Issue invoice, then collect or credit.  Do not invent statutory numbering or change draft contracts.
- Issue a commercial credit-note snapshot through `metering_billing.IssuedCreditNoteService.issue_credit_note` so a tenant and `credit_adjustment_id` replay the same `issued_credit_note_id` and frozen credit totals.  Present the snapshot through `IssuedCreditNotePresentmentService.present_issued_credit_note`.  `POST /v1/credit-adjustments/{credit_adjustment_id}/issued-credit-notes` is the issue command.  `GET /v1/issued-credit-notes/{issued_credit_note_id}` is the HTTP read.  `GET /v1/issued-credit-notes` lists `{issued_credit_notes, next_cursor}`.  Cross-tenant or unknown is 404.  Issue the credit note; the validated journal remains available for AIS.  First successful issue enqueues one existing `credit_note.issued` outbox event.  Do not invent statutory numbering or credit-note lines, or change credit-adjustment contracts.
- Apply one issued credit note onto one open same-tenant collection case through `metering_billing.CreditNoteApplicationService.apply_credit_note` so a tenant and `issued_credit_note_id` replay the same `credit_note_application_id` and never double-reduce outstanding.  Present the stored application through `CreditNoteApplicationPresentmentService.present_credit_note_application`.  `POST /v1/collection-cases/{collection_case_id}/credit-note-applications` is the apply command.  `GET /v1/credit-note-applications/{credit_note_application_id}` is the HTTP read.  `GET /v1/credit-note-applications` lists `{credit_note_applications, next_cursor}`.  Cross-tenant or unknown is 404.  Reduce outstanding by the exact issued inclusive amount.  First successful apply enqueues one existing `credit_note.applied` outbox event.  Apply the issued credit note, then collect the residual.  Do not invent a journal, tax unwind, second webhook system, statutory numbering, write-off, settlement, or payment receipt.
- Settle one same-tenant open collection case at exact-zero outstanding through `metering_billing.CollectionCaseSettlementService.settle_collection_case` so a tenant and `collection_case_id` replay the same `collection_case_settlement_id` and never double-settle.  Present the stored settlement through `CollectionCaseSettlementPresentmentService.present_collection_case_settlement`.  `POST /v1/collection-cases/{collection_case_id}/settlements` is the settle command.  `GET /v1/collection-case-settlements/{collection_case_settlement_id}` is the HTTP read.  `GET /v1/collection-case-settlements` lists `{collection_case_settlements, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed when outstanding is not zero or the case is already settled.  First successful settle enqueues one existing `collection.settled` outbox event.  Settle the zero-outstanding case, then wait.  Do not invent a journal, tax unwind, second webhook system, statutory numbering, write-off, or payment receipt.
- Write off leftover remaining on one same-tenant open collection case through `metering_billing.CollectionWriteOffService.write_off_collection_case` so a tenant and `collection_case_id` replay the same `collection_write_off_id` and never re-zero outstanding.  Present the stored write-off through `CollectionWriteOffPresentmentService.present_collection_write_off`.  `POST /v1/collection-cases/{collection_case_id}/write-offs` is the write-off command.  `GET /v1/collection-write-offs/{collection_write_off_id}` is the HTTP read.  `GET /v1/collection-write-offs` lists `{collection_write_offs, next_cursor}`.  Cross-tenant or unknown is 404.  Remaining becomes exact zero without flipping case status.  Fail closed when the case is missing or settled, remaining is already zero or negative, currency mismatches, or a supplied amount does not equal remaining.  First successful write-off enqueues one existing `write_off.recorded` outbox event.  Write off leftover remaining, then compose the journal and settle.  Do not invent a tax unwind, second webhook system, statutory numbering, payment receipt, credit note, or settlement command.
- Propose a write-off journal through `metering_billing.AccountingExportService.propose_write_off_journal` so a tenant and `collection_write_off_id` replay the same `proposal_id`.  `POST /v1/collection-write-offs/{collection_write_off_id}/journal-proposals` is the explicit compose.  Debit semantic `write_off_expense` and credit semantic `accounts_receivable`.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant write-off, currency mismatch, and zero or negative amounts.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID.
- Propose a credit journal through `metering_billing.AccountingExportService.propose_credit_journal` so a tenant and `credit_adjustment_id` replay the same `proposal_id`.  Credit accept already composes this write.  `POST /v1/credit-adjustments/{credit_adjustment_id}/journal-proposals` is the explicit compose/replay.  Debit semantic `usage_revenue` and credit semantic `accounts_receivable`.  Taxed credits reuse the existing `tax_payable` unwind.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant credit, currency mismatch, and zero or negative amounts.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID.
- Render that statement in `operator_console` Storybook with tokenized `AmountDue`, `LineTable`, `StatusChip`, and tenant-pin modules.  Keep exact-decimal strings.  Do not replace the Python package, add a production SPA, call AIS, or add Stripe.
- Export a journal proposal through `metering_billing.AccountingExportService` so a tenant and `invoice_draft_id` replay the same `proposal_id`.  Propose a cash journal from a stored `payment_receipt_id` so the same tenant, receipt, hash, and contract version replay the same `proposal_id`.  Propose a credit journal from a stored `credit_adjustment_id` so the same tenant and credit replay the same `proposal_id`.  Propose a write-off journal from a stored `collection_write_off_id` so the same tenant and write-off replay the same `proposal_id`.  Proposals stay proposal-only; this repository does not post journals.
- Open a collection case through `metering_billing.CollectionCaseService` so a tenant and `invoice_draft_id` replay the same `collection_case_id`.  Dunning events are commercial reminders; they do not capture payment or post journals.
- Project a payment intent through `metering_billing.PaymentIntentService` so a tenant and `collection_case_id` replay the same `payment_intent_id`.  Intents stay projected; they do not capture, settle, or post.
- Record a payment receipt through `metering_billing.PaymentSettlementService` so a tenant, `payment_intent_id`, received amount, source-payload hash, and contract version replay the same `payment_receipt_id`.  Accept and duplicate replay idempotently propose the existing cash journal.  Receipts stay `applied`; they do not capture via a provider or post journals.  Cancel a projected intent without writing a receipt or changing outstanding.
- Accept buyer and AIS writes through `metering_billing.http_app.create_http_app` or `python -m metering_billing.http_app`.  HTTP is a thin JSON adapter over the existing services.  Require a tenant on every write.  Money stays exact-decimal strings.  HTTP 200 means `accepted` or `duplicate_replay`; HTTP 422 means `rejected`; HTTP 404 is only an unknown route.  Do not post journals from that path.
- Issue a tenant API credential through `metering_billing.TenantApiCredentialService.issue_credential`.  Return the secret once.  Persist only a keyed HMAC.  After one or more active keys exist, every `/v1` write and GET except credential issue requires `Authorization: Bearer <secret>` or `X-CWL-Api-Key: <secret>` whose tenant equals the pin.  Zero active keys keep the tenant pin (bootstrap window) so AIS `X-CWL-Tenant-Reference` pull stays up until a key is issued.  `GET /healthz` stays unauthenticated.  Present stored metadata through `metering_billing.TenantApiCredentialPresentmentService.present_tenant_api_credential`.  `GET /v1/tenant-api-credentials/{tenant_api_credential_id}` is the HTTP read.  `GET /v1/tenant-api-credentials` lists `{tenant_api_credentials, next_cursor}`.  Cross-tenant or unknown is 404.  GET never returns the secret, hash, or verifier.  Issue a key, then send it on every `/v1` call; revoke when leaked.  Do not log the secret or put it on AIS contracts.
- Let AIS pull persisted proposals through `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Require a tenant on every read.  Accept optional `X-CWL-Tenant-Reference`; body or query `tenant_reference` still works when the header is absent; a mismatch is 422.  Cash and AR proposals share `journal_proposal` and appear in the same list.  Query never mutates `proposal_status` and never emits `posted` or statutory account IDs.
- Pull an AIS posting receipt through `metering_billing.PostingReceiptPullService.pull_posting_receipt` and store one append-only `posting_receipt_observation`.  Validate the AIS-owned consumed contract.  Do not map `posting_status_code` onto `proposal_status`.  AIS 403 is cross-tenant and writes zero rows; AIS 404 is `not_yet_accepted` and writes zero rows.  `GET /v1/posting-receipt-observations/{idempotency_key}` stays the existing #16 item read and does not call AIS.  Present a stored observation through `metering_billing.PostingReceiptObservationPresentmentService.present_posting_receipt_observation`.  `GET /v1/posting-receipt-observations` lists `{posting_receipt_observations, next_cursor}`.  Drain AIS outbox, then store the receipt observation.  Do not flip `proposal_status`.
- Drain AIS `posting_receipt` outbox events through `metering_billing.AisOutboxDrainService.drain_ais_outbox` or `POST /v1/ais-outbox-drains`.  Match `payload_reference` and `aggregate_reference` by equality to URNs constructed from our stored `proposal_id`.  Do not parse the payload URN.  Look up the stored Billing `idempotency_key`, then GET `/posting-receipts?idempotency_key=`.  Empty unpublished pages skip receipt GETs.  After a stored observation, POST `/outbox-events/{outbox_event_id}/publish`.  Do not drain `journal_reversal` or `period_close`.  Do not flip `proposal_status`.
- Present a stored collection case through `metering_billing.CollectionCasePresentmentService.present_collection_case` so a tenant and `collection_case_id` return outstanding, `open`/`dunning`/`settled` status, last/next dunning, and next operator action.  `GET /v1/collection-cases/{collection_case_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Present collection aging through `metering_billing.CollectionAgingPresentmentService.present_collection_aging` so a tenant returns current / 1-30 / 31-60 / 61-90 / 90+ outstanding grouped by `currency_code`.  `GET /v1/collection-aging` is the HTTP read.  Missing tenant is 422.  Age only `open` or `dunning` cases whose remaining is a positive exact decimal.  Due date is issued-invoice `due_at` when stored, otherwise `collection_case.opened_at`.  Settled cases and exact-zero remaining are omitted.  Present a stored reminder through `metering_billing.DunningEventPresentmentService.present_dunning_event`.  `POST /v1/collection-cases/{collection_case_id}/dunning-events` stays the #10 record.  `GET /v1/dunning-events/{dunning_event_id}` is the HTTP read.  `GET /v1/dunning-events` lists `{dunning_events, next_cursor}`.  Cross-tenant or unknown is 404.  GET never invents a send channel, recipient, or notice body.  Record the commercial reminder, then collect or credit.
- Present a stored payment intent through `metering_billing.PaymentIntentPresentmentService.present_payment_intent` so a tenant and `payment_intent_id` return amount, `projected`/`cancelled`/`rejected` status, and next operator action.  `POST /v1/payment-intents` projects from a stored `collection_case_id`.  `GET /v1/payment-intents/{payment_intent_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Create a projected payment intent, then record the receipt.  Do not capture cards or store a provider charge ID.
- Present a stored payment receipt through `metering_billing.PaymentReceiptPresentmentService.present_payment_receipt` so a tenant and `payment_receipt_id` return received amount, remaining outstanding, `applied` status, and next operator action.  `POST /v1/payment-receipts` applies against a projected `payment_intent_id` and composes the #13 cash journal.  `GET /v1/payment-receipts/{payment_receipt_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Record the receipt; the cash journal is already validated for AIS to pull.  Do not capture cards or flip `proposal_status`.
- Record a commercial credit through `metering_billing.CreditAdjustmentService.record_credit_adjustment` so a tenant, `invoice_draft_id`, exact `credit_amount`, `credit_reason_code`, source-payload hash, and contract version replay the same `credit_adjustment_id` and `proposal_id`.  Credits stay `recorded`.  If a collection case exists, reduce outstanding by the same inclusive amount and settle at zero.  When a tax assessment exists, split the credit proportionally (`credit_tax_amount = round_half_even(credit_amount * tax_amount / tax_inclusive_amount)`) and emit a three-line unwind that debits `usage_revenue` and `tax_payable` and credits `accounts_receivable`.  Untaxed credits stay two-line.  Present a stored credit through `metering_billing.CreditAdjustmentPresentmentService.present_credit_adjustment`.  `POST /v1/credit-adjustments` stays the #17 write.  `GET /v1/credit-adjustments/{credit_adjustment_id}` is the HTTP read.  `GET /v1/credit-adjustments` lists `{credit_adjustments, next_cursor}`.  Cross-tenant or unknown is 404.  Do not post, call AIS, or emit statutory IDs.  Record the credit; AIS pulls the validated journal.
- Publish a tenant-scoped rate card through `metering_billing.RateCardService.publish_rate_card` so a tenant, card name, canonical line hash, and contract version replay the same `rate_card_version`.  Versions are append-only.  Rate stored usage against that persisted version; do not invent a hidden default price.  A missing metric on the card fails closed.  Present a stored card through `metering_billing.RateCardPresentmentService.present_rate_card`.  `POST /v1/rate-cards` stays the #18 write.  `GET /v1/rate-cards/{rate_card_id}` is the HTTP read.  `GET /v1/rate-cards` lists `{rate_cards, next_cursor}`.  Publish a rate card, then rate a window against that version.
- Publish a tenant-scoped tax rate through `metering_billing.TaxRateService.publish_tax_rate` so a tenant, closed `tax_code`, exact `tax_rate` in `[0, 1]`, and contract version replay the same `tax_rate_version`.  Assess a stored draft through `metering_billing.TaxAssessmentService.assess_tax`.  Round tax half-even to the documented ISO 4217 minor units.  Reject assess after a collection case is open.  Collection outstanding uses `tax_inclusive_amount` when an assessment exists.  A taxed journal proposal credits semantic `tax_payable`; AIS must map that role.  Credit remaining is inclusive.  A taxed credit unwinds `tax_payable` with the proportional split.  Publish a tax rate, assess the draft, then propose the journal and let AIS pull.
- Register an https webhook callback through `metering_billing.WebhookSubscriptionService.register_subscription` so the same tenant, callback URL, event-type set, and contract version replay the same `webhook_subscription_id`.  Return the secret once.  Persist only a keyed HMAC.  Present stored metadata through `metering_billing.WebhookSubscriptionPresentmentService.present_webhook_subscription`.  `GET /v1/webhook-subscriptions/{webhook_subscription_id}` is the HTTP read.  `GET /v1/webhook-subscriptions` lists `{webhook_subscriptions, next_cursor}`.  Cross-tenant or unknown is 404.  GET never returns the secret, hash, prefix, or signed body.  When a journal proposal is validated, a payment receipt is applied, a credit is recorded, an invoice is issued, a credit note is issued, a credit note is applied, or a collection case is settled, append one `webhook_outbox_event`.  Run deliveries through `WebhookDeliveryService.deliver_due_events` or `POST /v1/webhook-deliveries`.  Sign the raw JSON body with HMAC-SHA256 and send `X-CWL-Webhook-Signature: sha256=<hex>`.  Present stored commercial outbox metadata through `metering_billing.WebhookOutboxEventPresentmentService.present_webhook_outbox_event`.  `GET /v1/webhook-outbox-events/{outbox_event_id}` is the HTTP read.  `GET /v1/webhook-outbox-events` lists `{webhook_outbox_events, next_cursor}`.  Cross-tenant or unknown is 404.  GET never publishes, sends, retries, or marks delivered, and never returns `payload_json`, the webhook secret, hash, prefix, or signature.  Present a stored attempt through `metering_billing.WebhookDeliveryPresentmentService.present_webhook_delivery`.  `GET /v1/webhook-deliveries/{delivery_attempt_id}` is the HTTP read.  `GET /v1/webhook-deliveries` lists `{webhook_deliveries, next_cursor}`.  Cross-tenant or unknown is 404.  GET never resends or returns the secret, hash, or signed body.  AIS may keep polling.  Do not flip `proposal_status`, call AIS posting-receipt, or emit statutory IDs.
- Leave principal, account, and project identifiers usable for invoicing.  Purpose-limit access; do not mask operational billing identifiers.
