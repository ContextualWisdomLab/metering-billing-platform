# Agent Development Rules

## Authority

- Preserve the billing-versus-accounting boundary.
- Never use a provider object ID as an internal primary key.
- Never let a webhook directly grant entitlement or post accounting.

## Data

- Use two-or-more-word `snake_case` database identifiers.
- Keep normalized facts relational; raw provider payloads belong in immutable object storage.
- Never store card data, PAT plaintext, prompt text, response text, provider secrets, tenant API credential plaintext, or webhook-subscription plaintext.  Persist only a keyed HMAC on `tenant_api_credential` and `webhook_subscription`.
- Use exact decimals for money and billable quantities.

## Development

- Write a failing test before behavior code.
- Require production statement and branch coverage of 100%.
- Document every public API and every accounting or monetary invariant.
- Update architecture, ADRs, and CHANGELOG when authority or behavior changes.
- Keep usage ingestion append-only.  Deduplicate by tenant-scoped source-event key and by source-payload hash plus contract version.  Present a stored event through `metering_billing.UsageEventPresentmentService.present_usage_event`.  `POST /v1/usage-events` stays the #5 ingest.  `GET /v1/usage-events/{usage_event_id}` is the HTTP read.  `GET /v1/usage-events` lists `{usage_events, next_cursor}`.  Ingest usage, then rate a window against a published card.  Present a stored rating run through `metering_billing.RatingRunPresentmentService.present_rating_run`.  `POST /v1/rating-runs` stays the #7 rate-a-window command.  `GET /v1/rating-runs/{rating_run_id}` is the HTTP read.  `GET /v1/rating-runs` lists `{rating_runs, next_cursor}`.  Rate a window, then draft an invoice.  Present a stored tax assessment through `metering_billing.TaxAssessmentPresentmentService.present_tax_assessment`.  `POST /v1/tax-assessments` stays the #19 assess command.  `GET /v1/tax-assessments/{tax_assessment_id}` stays the existing #19 item read.  `GET /v1/tax-assessments` lists `{tax_assessments, next_cursor}`.  Publish a tax rate, assess the draft, then propose the journal and let AIS pull.
- Rate stored usage through `metering_billing.UsageRatingService` so a tenant window, rate-card version, and usage snapshot replay the same `rating_run_id` and exact totals.
- Draft invoice intent through `metering_billing.InvoiceDraftService` so a tenant and `rating_run_id` replay the same `invoice_draft_id` and exact totals.  Drafts are not issued, collected, or posted.
- Present a stored draft through `metering_billing.InvoicePresentmentService.present_invoice_draft` so a tenant and `invoice_draft_id` return one statement with tax, credits, amount due, optional collection, and rating lines.  `GET /v1/invoice-drafts/{invoice_draft_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Open the draft statement, then collect or credit.
- Issue a commercial invoice snapshot through `metering_billing.IssuedInvoiceService.issue_invoice` so a tenant and `invoice_draft_id` replay the same `issued_invoice_id` and frozen totals.  Present the snapshot through `IssuedInvoicePresentmentService.present_issued_invoice`.  `POST /v1/invoice-drafts/{invoice_draft_id}/issued-invoices` is the issue command.  `GET /v1/issued-invoices/{issued_invoice_id}` is the HTTP read.  `GET /v1/issued-invoices` lists `{issued_invoices, next_cursor}`.  Cross-tenant or unknown is 404.  Item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment amounts still match the frozen issued totals.  Issue invoice, then collect or credit.  Do not invent statutory numbering, a VAT register, NTS/hometax field, or change draft contracts.
- Void one unused same-tenant issued invoice through `metering_billing.IssuedInvoiceVoidService.void_issued_invoice` so a tenant and `issued_invoice_id` replay the same `issued_invoice_void_id` and never re-close a collection case.  Present the stored void through `IssuedInvoiceVoidPresentmentService.present_issued_invoice_void`.  `POST /v1/issued-invoices/{issued_invoice_id}/voids` is the void command.  `GET /v1/issued-invoice-voids/{issued_invoice_void_id}` is the HTTP read.  `GET /v1/issued-invoice-voids` lists `{issued_invoice_voids, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed when the case already has a payment, credit-note apply, leftover apply, or write-off, remaining no longer equals the issued amount, the case is settled, or the tenant does not match.  An unused open or dunning case closes as `voided` at exact-zero remaining.  Do not reuse `settled`.  First successful void enqueues one existing `invoice.voided` outbox event.  Do not invent a second webhook system, PSP, refund, write-off rewrite, statement rewrite, AIS call, or statutory numbering.  The void write does not compose a journal.  `operator_console` Storybook presents one unused issued-invoice void with tokenized `AmountDue`, `StatusChip`, and tenant pin.
- Propose a void journal through `metering_billing.AccountingExportService.propose_void_journal` so a tenant and `issued_invoice_void_id` replay the same `proposal_id`.  `PostgresUsageLedger` persists that unused invoice-void journal with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or mutate remaining.  A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay.  `POST /v1/issued-invoice-voids/{issued_invoice_void_id}/journal-proposals` is the explicit compose.  Debit semantic `usage_revenue` and credit semantic `accounts_receivable`.  Taxed unused issues also debit `tax_payable`.  Bind the original invoice journal by Billing `proposal_id` / stored invoice-draft journal identity only.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant void, currency mismatch, and zero or negative amounts.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID or `journal_entry_id`.
- Issue a commercial credit-note snapshot through `metering_billing.IssuedCreditNoteService.issue_credit_note` so a tenant and `credit_adjustment_id` replay the same `issued_credit_note_id` and frozen credit totals.  `PostgresUsageLedger` persists that issued row with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row.  Present the snapshot through `IssuedCreditNotePresentmentService.present_issued_credit_note`.  `POST /v1/credit-adjustments/{credit_adjustment_id}/issued-credit-notes` is the issue command.  `GET /v1/issued-credit-notes/{issued_credit_note_id}` is the HTTP read.  `GET /v1/issued-credit-notes` lists `{issued_credit_notes, next_cursor}`.  Cross-tenant or unknown is 404.  Item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment still reproduces the frozen credit exclusive/tax split.  Issue the credit note; the validated journal remains available for AIS.  First successful issue enqueues one existing `credit_note.issued` outbox event.  Do not invent statutory numbering, a VAT register, NTS/hometax field, or credit-note lines, or change credit-adjustment contracts.
- Void one unused same-tenant issued credit note through `metering_billing.IssuedCreditNoteVoidService.void_issued_credit_note` so a tenant and `issued_credit_note_id` replay the same `issued_credit_note_void_id`.  `PostgresUsageLedger` persists that unused void with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row.  Present the stored void through `IssuedCreditNoteVoidPresentmentService.present_issued_credit_note_void`.  `POST /v1/issued-credit-notes/{issued_credit_note_id}/voids` is the void command.  `GET /v1/issued-credit-note-voids/{issued_credit_note_void_id}` is the HTTP read.  `GET /v1/issued-credit-note-voids` lists `{issued_credit_note_voids, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed when the note is missing or cross-tenant, already applied, already voided (replay only), currency mismatches, or the tenant is missing.  Collection remaining is unchanged because the note cannot have been applied.  After a void exists, apply fail-closes as `issued_credit_note_voided`.  First successful void enqueues one existing `credit_note.voided` outbox event.  The void write does not compose a journal.  `operator_console` Storybook presents one unused issued-credit-note void with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Do not invent a second webhook system, PSP, VAT register, NTS filing, 연말정산, statutory account, negative invoice, or AIS call.
- Propose a credit-note void journal through `metering_billing.AccountingExportService.propose_credit_note_void_journal` so a tenant and `issued_credit_note_void_id` replay the same `proposal_id`.  `PostgresUsageLedger` persists that unused credit-note-void journal with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or mutate remaining.  A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay.  `POST /v1/issued-credit-note-voids/{issued_credit_note_void_id}/journal-proposals` is the explicit compose.  Debit semantic `accounts_receivable` for the exact tax-inclusive voided amount and credit semantic `usage_revenue` for the issued exclusive amount.  Taxed unused notes also credit `tax_payable`.  Bind the original credit journal by Billing `proposal_id` plus `credit_adjustment_id` / `issued_credit_note_id` only.  Fail closed if that original Billing proposal is missing.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant void, already-applied note, currency mismatch, and zero or negative amounts.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID or `journal_entry_id`.
- Apply one issued credit note onto one open same-tenant collection case through `metering_billing.CreditNoteApplicationService.apply_credit_note` so a tenant and `issued_credit_note_id` replay the same `credit_note_application_id` and never double-reduce outstanding.  `PostgresUsageLedger` persists that application with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row.  Present the stored application through `CreditNoteApplicationPresentmentService.present_credit_note_application`.  `POST /v1/collection-cases/{collection_case_id}/credit-note-applications` is the apply command.  `GET /v1/credit-note-applications/{credit_note_application_id}` is the HTTP read.  `GET /v1/credit-note-applications` lists `{credit_note_applications, next_cursor}`.  Cross-tenant or unknown is 404.  Reduce outstanding by the exact issued inclusive amount.  First successful apply enqueues one existing `credit_note.applied` outbox event.  Apply the issued credit note, then collect the residual.  A voided unused note fail-closes as `issued_credit_note_voided`.  Do not invent a journal, tax unwind, second webhook system, statutory numbering, write-off, settlement, or payment receipt.
- Settle one same-tenant open collection case at exact-zero outstanding through `metering_billing.CollectionCaseSettlementService.settle_collection_case` so a tenant and `collection_case_id` replay the same `collection_case_settlement_id` and never double-settle.  Present the stored settlement through `CollectionCaseSettlementPresentmentService.present_collection_case_settlement`.  `POST /v1/collection-cases/{collection_case_id}/settlements` is the settle command.  `GET /v1/collection-case-settlements/{collection_case_settlement_id}` is the HTTP read.  `GET /v1/collection-case-settlements` lists `{collection_case_settlements, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed when outstanding is not zero or the case is already settled.  First successful settle enqueues one existing `collection.settled` outbox event.  Settle the zero-outstanding case, then wait.  `operator_console` Storybook presents one leftover write-off settle-when-zero with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Do not invent a journal, tax unwind, second webhook system, statutory numbering, write-off, or payment receipt.
- Hold one same-tenant open or dunning collection case as a commercial dispute through `metering_billing.CollectionDisputeService.hold_collection_case` so a tenant and `collection_case_id` replay the same `collection_dispute_id` and never change remaining outstanding.  `PostgresUsageLedger` persists that collection dispute with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row.  Present the stored hold through `CollectionDisputePresentmentService.present_collection_dispute`.  `POST /v1/collection-cases/{collection_case_id}/disputes` is the hold command.  `GET /v1/collection-disputes/{collection_dispute_id}` is the HTTP read.  `GET /v1/collection-disputes` lists `{collection_disputes, next_cursor}`.  Cross-tenant or unknown is 404.  Case status becomes `disputed`.  New dunning, payment receipt, credit apply, leftover apply, write-off, settle-when-zero, and void fail closed while held.  Fail closed when the case is missing, settled, voided, already disputed without a stored row, or the tenant does not match.  First successful hold enqueues one existing `dispute.held` outbox event.  `operator_console` Storybook presents one held dispute and one released/fail-close with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Do not invent a second webhook system, journal, PSP, refund, write-off rewrite, void rewrite, statement rewrite, AIS call, or statutory numbering.
- Release one same-tenant held collection dispute through `metering_billing.CollectionDisputeReleaseService.release_collection_dispute` so a tenant and `collection_dispute_id` replay the same `collection_dispute_id` and never change remaining outstanding.  `PostgresUsageLedger` flips the same persisted hold row to `released` without inserting a second row.  Present the stored release through `CollectionDisputeReleasePresentmentService.present_collection_dispute_release`.  `POST /v1/collection-disputes/{collection_dispute_id}/releases` is the release command.  `GET /v1/collection-dispute-releases/{collection_dispute_id}` is the HTTP read.  `GET /v1/collection-dispute-releases` lists `{collection_dispute_releases, next_cursor}`.  Cross-tenant, unknown, or still-held is 404.  Dispute status becomes `released`.  Case status returns to `open`, or to `dunning` when stored notices already exist.  After release, dunning, payment, credit apply, leftover apply, write-off, settle-when-zero, and void follow the existing open-case rules.  A later hold of the same case fail-closes as `collection_dispute_released`.  Fail closed when the dispute is missing, not held, the case is missing, settled, or voided, or the tenant does not match.  First successful release enqueues one existing `dispute.released` outbox event.  `operator_console` Storybook presents that released/fail-close remaining snapshot.  Do not invent a second webhook system, journal, PSP, refund, write-off rewrite, void rewrite, statement rewrite, AIS call, or statutory numbering.
- Write off leftover remaining on one same-tenant open collection case through `metering_billing.CollectionWriteOffService.write_off_collection_case` so a tenant and `collection_case_id` replay the same `collection_write_off_id` and never re-zero outstanding.  Present the stored write-off through `CollectionWriteOffPresentmentService.present_collection_write_off`.  `POST /v1/collection-cases/{collection_case_id}/write-offs` is the write-off command.  `GET /v1/collection-write-offs/{collection_write_off_id}` is the HTTP read.  `GET /v1/collection-write-offs` lists `{collection_write_offs, next_cursor}`.  Cross-tenant or unknown is 404.  Remaining becomes exact zero without flipping case status.  Fail closed when the case is missing or settled, remaining is already zero or negative, currency mismatches, or a supplied amount does not equal remaining.  First successful write-off enqueues one existing `write_off.recorded` outbox event.  Write off leftover remaining, then compose the journal and settle.  `operator_console` Storybook presents one recorded leftover remaining write-off with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Do not invent a tax unwind, second webhook system, statutory numbering, payment receipt, credit note, or settlement command.
- Propose a write-off journal through `metering_billing.AccountingExportService.propose_write_off_journal` so a tenant and `collection_write_off_id` replay the same `proposal_id`.  `PostgresUsageLedger` persists that write-off journal with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or mutate remaining.  A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay.  `POST /v1/collection-write-offs/{collection_write_off_id}/journal-proposals` is the explicit compose.  Debit semantic `write_off_expense` and credit semantic `accounts_receivable`.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant write-off, currency mismatch, and zero or negative amounts.  `operator_console` Storybook presents one same-tenant validated write-off journal with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Existing collection-write-off Storybook stays the write-off row presentment.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID.
- Propose a refund journal through `metering_billing.AccountingExportService.propose_refund_journal` so a tenant and `unapplied_cash_refund_id` replay the same `proposal_id`.  `PostgresUsageLedger` persists that leftover-refund journal with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or mutate remaining.  A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay.  `POST /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}/journal-proposals` is the explicit compose.  Debit semantic `unapplied_cash` and credit semantic `cash_receipt`.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant refund, currency mismatch, and zero or negative amounts.  `operator_console` Storybook presents one same-tenant validated leftover-refund journal with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Existing leftover-refund UnappliedCash Storybook stays the leftover-refund row presentment.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID.
- Propose a leftover journal through `metering_billing.AccountingExportService.propose_unapplied_cash_journal` so a tenant and `unapplied_cash_id` replay the same `proposal_id`.  `PostgresUsageLedger` persists that leftover journal with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or mutate remaining.  A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay.  `POST /v1/unapplied-cash/{unapplied_cash_id}/journal-proposals` is the explicit compose.  Debit semantic `cash_receipt` and credit semantic `unapplied_cash`.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant leftover, leftover not parked, currency mismatch, and zero or negative amounts.  `operator_console` Storybook presents one same-tenant validated leftover journal with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Existing leftover / unapplied-cash Storybook stays the leftover-row presentment.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID.
- Propose an apply journal through `metering_billing.AccountingExportService.propose_unapplied_cash_application_journal` so a tenant and `unapplied_cash_application_id` replay the same `proposal_id`.  `PostgresUsageLedger` persists that leftover-apply journal with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or mutate remaining.  A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay.  `POST /v1/unapplied-cash-applications/{unapplied_cash_application_id}/journal-proposals` is the explicit compose.  Debit semantic `unapplied_cash` and credit semantic `accounts_receivable`.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant application, currency mismatch, and zero or negative amounts.  `operator_console` Storybook presents one same-tenant validated leftover-apply journal with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Existing leftover-apply UnappliedCash Storybook stays the leftover-apply row presentment.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID.
- Propose a credit journal through `metering_billing.AccountingExportService.propose_credit_journal` so a tenant and `credit_adjustment_id` replay the same `proposal_id`.  Credit accept already composes this write.  `POST /v1/credit-adjustments/{credit_adjustment_id}/journal-proposals` is the explicit compose/replay.  Debit semantic `usage_revenue` and credit semantic `accounts_receivable`.  Taxed credits reuse the existing `tax_payable` unwind.  `proposal_status` stays `validated`.  AIS pulls through existing GET journal-proposal routes.  Fail closed on missing/cross-tenant credit, currency mismatch, zero or negative amounts, and debit or credit amounts with more than six significant fractional digits.  Do not call AIS, flip `proposal_status` to `posted`, or invent a statutory account ID.
- Render that statement in `operator_console` Storybook with tokenized `AmountDue`, `LineTable`, `StatusChip`, and tenant-pin modules.  Keep exact-decimal strings.  Do not replace the Python package, add a production SPA, call AIS, or add Stripe.
- Export a journal proposal through `metering_billing.AccountingExportService` so a tenant and `invoice_draft_id` replay the same `proposal_id`.  `PostgresUsageLedger` persists that invoice-draft journal with tenant-scoped composite foreign keys and exact `numeric` amounts; replay of the same tenant, draft, source hash, and contract version does not insert a second row.  A crash after insert and before the existing `journal_proposal.validated` outbox enqueue is healed by the next replay.  Propose a cash journal from a stored `payment_receipt_id` so the same tenant, receipt, hash, and contract version replay the same `proposal_id`.  Propose a credit journal from a stored `credit_adjustment_id` so the same tenant and credit replay the same `proposal_id`.  Propose a write-off journal from a stored `collection_write_off_id` so the same tenant and write-off replay the same `proposal_id`.  Propose a leftover journal from a stored `unapplied_cash_id` so the same tenant and leftover replay the same `proposal_id`.  Propose an apply journal from a stored `unapplied_cash_application_id` so the same tenant and leftover apply replay the same `proposal_id`.  Propose a void journal from a stored `issued_invoice_void_id` so the same tenant and void replay the same `proposal_id`.  Propose a credit-note void journal from a stored `issued_credit_note_void_id` so the same tenant and unused credit-note void replay the same `proposal_id`.  Fail closed when any journal-line debit or credit has more than six significant fractional digits; do not round into `numeric(38, 6)`.  Proposals stay proposal-only; this repository does not post journals.
- Open a collection case through `metering_billing.CollectionCaseService` so a tenant and `invoice_draft_id` replay the same `collection_case_id`.  Dunning events are commercial reminders; they do not capture payment or post journals.
- Project a payment intent through `metering_billing.PaymentIntentService` so a tenant and `collection_case_id` replay the same `payment_intent_id`.  Intents stay projected; they do not capture, settle, or post.
- Record a payment receipt through `metering_billing.PaymentSettlementService` so a tenant, `payment_intent_id`, received amount, source-payload hash, and contract version replay the same `payment_receipt_id`.  Accept and duplicate replay idempotently propose the existing cash journal.  Receipts stay `applied`; they do not capture via a provider or post journals.  Cancel a projected intent without writing a receipt or changing outstanding.
- Accept buyer and AIS writes through `metering_billing.http_app.create_http_app` or `python -m metering_billing.http_app`.  HTTP is a thin JSON adapter over the existing services.  Require a tenant on every write.  Money stays exact-decimal strings.  HTTP 200 means `accepted` or `duplicate_replay`; HTTP 422 means `rejected`; HTTP 404 is only an unknown route.  Do not post journals from that path.  The bound ledger defaults to the deterministic in-memory reference adapter; `metering_billing.http_app.create_default_ledger` selects the durable `PostgresUsageLedger` production system of record when `METERING_BILLING_LEDGER_BACKEND=postgres` and `METERING_BILLING_POSTGRES_DSN` is set (missing or empty DSN fails closed at startup).  Unauthenticated `GET /readyz` reports `{"status": "ready", "backend": "memory" | "postgres"}` or `503 {"status": "not_ready", ..., "reason": "migration_history_unavailable"}` with stable reason codes only (ADR 0123).  Standalone serving is the module-level `ThreadingWSGIServer` (one daemon thread per connection) on `0.0.0.0:$PORT`.
- Deploy through the Compose surface in `compose/docker-compose.yml` (fixed project name `metering-billing-platform`; root `Dockerfile`; one-shot idempotent migration service before `billing_api`).  Record real load measurements from `compose/k6/e2e_smoke.js` in `docs/operations/load-test-baseline.md` — never assert unmeasured performance numbers (ADR 0124).
- Issue a tenant API credential through `metering_billing.TenantApiCredentialService.issue_credential`.  Return the secret once.  Persist only a keyed HMAC.  After any credential history exists, every `/v1` write and GET, including credential issue, requires `Authorization: Bearer <secret>` or `X-CWL-Api-Key: <secret>` whose tenant equals the pin.  Only the first issue for a tenant with no credential history may use the tenant pin; revoking all keys does not reopen it.  `GET /healthz` stays unauthenticated.  Present stored metadata through `metering_billing.TenantApiCredentialPresentmentService.present_tenant_api_credential`.  `GET /v1/tenant-api-credentials/{tenant_api_credential_id}` is the HTTP read.  `GET /v1/tenant-api-credentials` lists `{tenant_api_credentials, next_cursor}`.  Cross-tenant or unknown is 404.  GET never returns the secret, hash, or verifier.  Issue a key, then send it on every `/v1` call; revoke when leaked.  Do not log the secret or put it on AIS contracts.
- Let AIS pull persisted proposals through `GET /v1/journal-proposals` and `GET /v1/journal-proposals/{proposal_id}`.  Require a tenant on every read.  Accept optional `X-CWL-Tenant-Reference`; body or query `tenant_reference` still works when the header is absent; a mismatch is 422.  Cash and AR proposals share `journal_proposal` and appear in the same list.  Query never mutates `proposal_status` and never emits `posted` or statutory account IDs.  `operator_console` Storybook presents one validated morning cash journal, one same-tenant validated invoice-draft journal, one same-tenant validated leftover journal, one same-tenant validated leftover-apply journal, one same-tenant validated leftover-refund journal, and one same-tenant validated write-off journal with tokenized `AmountDue`, `StatusChip`, and tenant pin.
- Pull an AIS posting receipt through `metering_billing.PostingReceiptPullService.pull_posting_receipt` and store one append-only `posting_receipt_observation`.  Validate the AIS-owned consumed contract.  Do not map `posting_status_code` onto `proposal_status`.  AIS 403 is cross-tenant and writes zero rows; AIS 404 is `not_yet_accepted` and writes zero rows.  `GET /v1/posting-receipt-observations/{idempotency_key}` stays the existing #16 item read and does not call AIS.  Present a stored observation through `metering_billing.PostingReceiptObservationPresentmentService.present_posting_receipt_observation`.  `GET /v1/posting-receipt-observations` lists `{posting_receipt_observations, next_cursor}`.  Drain AIS outbox, then store the receipt observation.  Do not flip `proposal_status`.
- Drain AIS `posting_receipt` outbox events through `metering_billing.AisOutboxDrainService.drain_ais_outbox` or `POST /v1/ais-outbox-drains`.  Match `payload_reference` and `aggregate_reference` by equality to URNs constructed from our stored `proposal_id`.  Do not parse the payload URN.  Look up the stored Billing `idempotency_key`, then GET `/posting-receipts?idempotency_key=`.  Empty unpublished pages skip receipt GETs.  After a stored observation, POST `/outbox-events/{outbox_event_id}/publish`.  Do not drain `journal_reversal` or `period_close`.  Do not flip `proposal_status`.
- Present a stored collection case through `metering_billing.CollectionCasePresentmentService.present_collection_case` so a tenant and `collection_case_id` return outstanding, `open`/`dunning`/`settled`/`voided` status, last/next dunning, and next operator action.  `GET /v1/collection-cases/{collection_case_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Present collection aging through `metering_billing.CollectionAgingPresentmentService.present_collection_aging` so a tenant returns current / 1-30 / 31-60 / 61-90 / 90+ outstanding grouped by `currency_code`.  `GET /v1/collection-aging` is the HTTP read.  Missing tenant is 422.  Present a billing-account statement through `metering_billing.AccountStatementPresentmentService.present_account_statement` so a tenant and `billing_account_id` return issued-invoice totals, unused issued-invoice voids, open collection remaining, applied credits, unused issued-credit-note voids, write-offs, parked leftover, and refunded leftover grouped by `currency_code`.  `GET /v1/billing-accounts/{billing_account_id}/statement` is the HTTP read.  `operator_console` Storybook presents those exact-decimal currency rows.  Missing tenant is 422.  Missing account is 404.  Cross-tenant account is 403.  Present already-rated spend through `metering_billing.RatedSpendPresentmentService.present_rated_spend` so a tenant, `billing_account_id`, and half-open window return stored rating-run and exclusive invoice-draft line amounts grouped by `product_code`.  Optional `group_by=project` adds stored exclusive-account `project_reference`.  Optional `group_by=credential` adds stored exclusive-account `credential_reference`.  Optional `group_by=principal` adds stored exclusive-account `billing_principal_reference`.  Optional `group_by=cost_center` adds stored exclusive-account `cost_center_reference`.  `GET /v1/billing-accounts/{billing_account_id}/rated-spend` is the HTTP read.  `operator_console` Storybook presents those exact-decimal product and project rows.  Missing tenant, an illegal window, or unknown `group_by` is 422.  Missing account is 404.  Cross-tenant account is 403.  Do not re-rate, invent a unit price, include unrated usage, invent a project, credential, principal, or cost center, or write a money fact.  Publish one commercial spend budget through `metering_billing.SpendBudgetService.publish_spend_budget` so a tenant, `billing_account_id`, currency, exact `budget_amount` greater than zero, and half-open window persist one append-only `spend_budget`.  Replay of the same identity returns the stored `spend_budget_id`.  `POST /v1/billing-accounts/{billing_account_id}/spend-budgets` is the write.  Present the stored row through `SpendBudgetPresentmentService.present_spend_budget`.  `GET /v1/spend-budgets/{spend_budget_id}` is the HTTP read.  `GET /v1/spend-budgets` lists `{spend_budgets, next_cursor}`.  Cross-tenant or unknown is 404.  Missing tenant is 422.  Unknown billing account is 404.  Cross-tenant account is 403.  Do not compare the budget to rated spend, stop rating, compose a journal, or invent a dimension-scoped budget.  Present a stored budget evaluation through `metering_billing.SpendBudgetEvaluationPresentmentService.present_spend_budget_evaluation` so a tenant and `spend_budget_id` return exact `remaining_amount`, `over_amount`, and `utilization_status` `under`/`at`/`over`.  `GET /v1/spend-budgets/{spend_budget_id}/evaluation` is the HTTP read.  Reuse `RatedSpendPresentmentService` with `group_by=product`.  Same-tenant is 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  A budget whose billing account belongs to another commercial `tenant_account` is 403.  Next operator action is `wait`.  Observe first-over utilization through `metering_billing.SpendBudgetOverSignalService.observe_spend_budget_over` so a tenant and `spend_budget_id` reuse that evaluation math and enqueue one existing `spend_budget.over` outbox event when `utilization_status` is `over`.  `POST /v1/spend-budgets/{spend_budget_id}/over-signal` is the write.  Same-tenant published budgets are 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  Cross-tenant account is 403.  under and at write zero over-signal rows.  Replay of the same over observation is `duplicate_replay` and does not enqueue a second row.  Rejected observe writes zero rows.  Present the live observation through `metering_billing.SpendBudgetOverSignalPresentmentService.present_spend_budget_over_signal` so a tenant and `spend_budget_id` reuse that same evaluation math and the existing over-signal envelope plus zero or one stored `spend_budget.over` webhook-outbox presentment.  `GET /v1/spend-budgets/{spend_budget_id}/over-signal` is the HTTP read.  Same-tenant is 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  Cross-tenant account is 403.  Replay is a safe GET and writes no outbox row.  Envelope `data` is a thin reference plus hash and exact `over_amount`; remaining and rated lines are omitted.  Existing subscriptions opt in by including `spend_budget.over`.  `operator_console` Storybook presents one first-over accepted observation and one same-tenant under observation with zero over-signal rows.  Observe first-at approaching utilization through `metering_billing.SpendBudgetApproachingSignalService.observe_spend_budget_approaching` so a tenant and `spend_budget_id` reuse that evaluation math and enqueue one existing `spend_budget.approaching` outbox event when `utilization_status` is `at` the documented `budget_amount` threshold.  `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the write.  Same-tenant published budgets are 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  Cross-tenant account is 403.  under and over write zero approaching-signal rows.  Replay of the same at observation is `duplicate_replay` and does not enqueue a second row.  Rejected observe writes zero rows.  Envelope `data` is a thin reference plus hash and exact `remaining_amount`; over and rated lines are omitted.  Existing subscriptions opt in by including `spend_budget.approaching`.  Present the live approaching observation through `metering_billing.SpendBudgetApproachingSignalPresentmentService.present_spend_budget_approaching_signal` so a tenant and `spend_budget_id` reuse that same evaluation math and the existing approaching-signal envelope plus zero or one stored `spend_budget.approaching` webhook-outbox presentment.  `GET /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the HTTP read.  Same-tenant is 200.  Unknown or cross-tenant is 404.  Missing tenant is 422.  Cross-tenant account is 403.  Replay is a safe GET and writes no outbox row.  Envelope `data` is a thin reference plus hash and exact `remaining_amount`; over and rated lines are omitted.  `operator_console` Storybook presents one first-at accepted observation and one same-tenant under observation with zero approaching-signal rows.  List every published budget on one same-tenant billing account through `list_billing_account_budget_statuses`.  `GET /v1/billing-accounts/{billing_account_id}/budget-status` lists `{budget_statuses, next_cursor}`.  Same-tenant is 200.  Unknown billing account is 404.  Cross-tenant account is 403.  Missing tenant is 422.  Unknown or cross-tenant budgets are omitted with no leak.  `operator_console` Storybook presents those exact-decimal under, at, and over rows plus the keyset cursor.  Do not persist, mutate the budget, emit a webhook or journal, call AIS, invent a statutory identifier, emit `retained_earnings` or 310100, or hard-stop rating, ingest, or invoice.  Age only `open` or `dunning` cases whose remaining is a positive exact decimal.  Due date is issued-invoice `due_at` when stored, otherwise `collection_case.opened_at`.  Settled cases and exact-zero remaining are omitted.  Present a stored reminder through `metering_billing.DunningEventPresentmentService.present_dunning_event`.  `POST /v1/collection-cases/{collection_case_id}/dunning-events` stays the #10 record.  `GET /v1/dunning-events/{dunning_event_id}` is the HTTP read.  `GET /v1/dunning-events` lists `{dunning_events, next_cursor}`.  Cross-tenant or unknown is 404.  GET never invents a send channel, recipient, or notice body.  Record the commercial reminder, then collect or credit.
- Present a stored payment intent through `metering_billing.PaymentIntentPresentmentService.present_payment_intent` so a tenant and `payment_intent_id` return amount, `projected`/`cancelled`/`rejected` status, and next operator action.  `POST /v1/payment-intents` projects from a stored `collection_case_id`.  `GET /v1/payment-intents/{payment_intent_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Create a projected payment intent, then record the receipt.  Do not capture cards or store a provider charge ID.
- Present a stored payment receipt through `metering_billing.PaymentReceiptPresentmentService.present_payment_receipt` so a tenant and `payment_receipt_id` return received amount, remaining outstanding, `applied` status, and next operator action.  `POST /v1/payment-receipts` applies against a projected `payment_intent_id` and composes the #13 cash journal.  `GET /v1/payment-receipts/{payment_receipt_id}` is the HTTP read.  Cross-tenant or unknown is 404.  Record the receipt; the cash journal is already validated for AIS to pull.  Do not capture cards or flip `proposal_status`.
- Park leftover remittance against one same-tenant stored payment receipt through `metering_billing.UnappliedCashService.park_unapplied_cash` so a tenant and `payment_receipt_id` replay the same `unapplied_cash_id` and never write a second row.  `PostgresUsageLedger` persists that leftover with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row.  Present the stored leftover through `UnappliedCashPresentmentService.present_unapplied_cash`.  `POST /v1/payment-receipts/{payment_receipt_id}/unapplied-cash` is the park command.  `GET /v1/unapplied-cash/{unapplied_cash_id}` is the HTTP read.  `GET /v1/unapplied-cash` lists `{unapplied_cash, next_cursor}`.  Cross-tenant or unknown is 404.  #12 still rejects overpay.  Omitting leftover fail-closes as already consumed.  Fail closed on zero, negative, oversized leftover, currency mismatch, IEEE leftover, and missing or cross-tenant receipt.  Do not auto-apply leftover to another case.  `operator_console` Storybook presents one parked leftover with tokenized `AmountDue`, `StatusChip`, and tenant pin.  Do not invent a journal, webhook, write-off, settlement, credit note, dunning engine, PSP, AIS call, or statutory numbering.
- Apply one parked leftover onto one same-tenant open collection case through `metering_billing.UnappliedCashApplicationService.apply_unapplied_cash` so a tenant and `unapplied_cash_id` replay the same `unapplied_cash_application_id` and never double-reduce outstanding.  `PostgresUsageLedger` persists that leftover-apply with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or reduce remaining again once remaining already excludes the applied amount.  A crash after insert and before outstanding reduction is healed by the next replay.  Present the stored application through `UnappliedCashApplicationPresentmentService.present_unapplied_cash_application`.  `POST /v1/collection-cases/{collection_case_id}/unapplied-cash-applications` is the apply command.  `GET /v1/unapplied-cash-applications/{unapplied_cash_application_id}` is the HTTP read.  `GET /v1/unapplied-cash-applications` lists `{unapplied_cash_applications, next_cursor}`.  Cross-tenant or unknown is 404.  Apply the full parked amount.  Remaining zero does not settle; #46 remains the explicit settle-when-zero command.  Fail closed on missing/cross-tenant leftover or case, leftover already applied, leftover already refunded, already settled case, currency mismatch, apply greater than parked or remaining, zero/negative leftover, negative remaining, and IEEE leftover.  First successful apply enqueues one existing `unapplied_cash.applied` outbox event.  Apply parked leftover, then collect the residual or settle at exact zero.  `operator_console` Storybook presents that leftover-apply residual.  Do not invent a journal, write-off, credit note, dunning engine, PSP, AIS call, statutory numbering, or settlement command.
- Refund one parked leftover through `metering_billing.UnappliedCashRefundService.refund_unapplied_cash` so a tenant and `unapplied_cash_id` replay the same `unapplied_cash_refund_id` and never write a second row.  `PostgresUsageLedger` persists that leftover refund with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row.  Present the stored refund through `UnappliedCashRefundPresentmentService.present_unapplied_cash_refund`.  `POST /v1/unapplied-cash/{unapplied_cash_id}/refunds` is the refund command.  `GET /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}` is the HTTP read.  `GET /v1/unapplied-cash-refunds` lists `{unapplied_cash_refunds, next_cursor}`.  Cross-tenant or unknown is 404.  Refund the full parked amount.  The parked leftover row stays `parked`.  Fail closed on missing/cross-tenant leftover, leftover already applied, leftover not parked, currency mismatch, zero/negative leftover, mismatched amount, and IEEE leftover.  First successful refund enqueues one existing `refund.recorded` outbox event.  `operator_console` Storybook presents that leftover-refund remaining snapshot.  Do not invent a journal, write-off, settlement, credit note, dunning engine, PSP capture, AIS call, or statutory numbering.
- Record a commercial credit through `metering_billing.CreditAdjustmentService.record_credit_adjustment` so a tenant, `invoice_draft_id`, exact `credit_amount`, `credit_reason_code`, source-payload hash, and contract version replay the same `credit_adjustment_id` and `proposal_id`.  Credits stay `recorded`.  If a collection case exists, reduce outstanding by the same inclusive amount and settle at zero.  When a tax assessment exists, split the credit proportionally (`credit_tax_amount = round_half_even(credit_amount * tax_amount / tax_inclusive_amount)`) and emit a three-line unwind that debits `usage_revenue` and `tax_payable` and credits `accounts_receivable`.  Untaxed credits stay two-line.  Present a stored credit through `metering_billing.CreditAdjustmentPresentmentService.present_credit_adjustment`.  `POST /v1/credit-adjustments` stays the #17 write.  `GET /v1/credit-adjustments/{credit_adjustment_id}` is the HTTP read.  `GET /v1/credit-adjustments` lists `{credit_adjustments, next_cursor}`.  Cross-tenant or unknown is 404.  Fail closed before insert when the credit or its exclusive/tax split has more than six significant fractional digits so an unpostable journal is never composed.  Do not post, call AIS, or emit statutory IDs.  Record the credit; AIS pulls the validated journal.
- Publish a tenant-scoped rate card through `metering_billing.RateCardService.publish_rate_card` so a tenant, card name, canonical line hash, and contract version replay the same `rate_card_version`.  Versions are append-only.  Rate stored usage against that persisted version; do not invent a hidden default price.  A missing metric on the card fails closed.  Present a stored card through `metering_billing.RateCardPresentmentService.present_rate_card`.  `POST /v1/rate-cards` stays the #18 write.  `GET /v1/rate-cards/{rate_card_id}` is the HTTP read.  `GET /v1/rate-cards` lists `{rate_cards, next_cursor}`.  Publish a rate card, then rate a window against that version.
- Publish a tenant-scoped tax rate through `metering_billing.TaxRateService.publish_tax_rate` so a tenant, closed `tax_code`, exact `tax_rate` in `[0, 1]`, and contract version replay the same `tax_rate_version`.  Assess a stored draft through `metering_billing.TaxAssessmentService.assess_tax`.  Round tax half-even to the documented ISO 4217 minor units.  Reject assess after a collection case is open.  Collection outstanding uses `tax_inclusive_amount` when an assessment exists.  A taxed journal proposal credits semantic `tax_payable`; AIS must map that role.  Credit remaining is inclusive.  A taxed credit unwinds `tax_payable` with the proportional split.  Publish a tax rate, assess the draft, then propose the journal and let AIS pull.
- Register an https webhook callback through `metering_billing.WebhookSubscriptionService.register_subscription` so the same tenant, callback URL, event-type set, and contract version replay the same `webhook_subscription_id`.  Return the secret once.  Persist only a keyed HMAC.  Present stored metadata through `metering_billing.WebhookSubscriptionPresentmentService.present_webhook_subscription`.  `GET /v1/webhook-subscriptions/{webhook_subscription_id}` is the HTTP read.  `GET /v1/webhook-subscriptions` lists `{webhook_subscriptions, next_cursor}`.  Cross-tenant or unknown is 404.  GET never returns the secret, hash, prefix, or signed body.  When a journal proposal is validated, a payment receipt is applied, a credit is recorded, an invoice is issued, a credit note is issued, an unused issued credit note is voided, a credit note is applied, a collection case is settled, leftover is written off, parked leftover is applied, unused parked leftover is refunded, a collection case is held as disputed, a held dispute is released, a spend budget is published, or a published spend budget is first observed as over, append one `webhook_outbox_event`.  Run deliveries through `WebhookDeliveryService.deliver_due_events` or `POST /v1/webhook-deliveries`.  Sign the raw JSON body with HMAC-SHA256 and send `X-CWL-Webhook-Signature: sha256=<hex>`.  Present stored commercial outbox metadata through `metering_billing.WebhookOutboxEventPresentmentService.present_webhook_outbox_event`.  `GET /v1/webhook-outbox-events/{outbox_event_id}` is the HTTP read.  `GET /v1/webhook-outbox-events` lists `{webhook_outbox_events, next_cursor}`.  Cross-tenant or unknown is 404.  GET never publishes, sends, retries, or marks delivered, and never returns `payload_json`, the webhook secret, hash, prefix, or signature.  Present a stored attempt through `metering_billing.WebhookDeliveryPresentmentService.present_webhook_delivery`.  `GET /v1/webhook-deliveries/{delivery_attempt_id}` is the HTTP read.  `GET /v1/webhook-deliveries` lists `{webhook_deliveries, next_cursor}`.  Cross-tenant or unknown is 404.  GET never resends or returns the secret, hash, or signed body.  AIS may keep polling.  Do not flip `proposal_status`, call AIS posting-receipt, or emit statutory IDs.
- Leave principal, account, and project identifiers usable for invoicing.  Purpose-limit access; do not mask operational billing identifiers.
