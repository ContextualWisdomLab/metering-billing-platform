# Technical Requirements Document

## Architecture style

Start as a modular, contract-first repository. Runtime services can later be deployed as a modular monolith and split at stable ports when throughput or PCI/network boundaries justify separation.

## Contract plane

- JSON Schema Draft 2020-12 for external payloads.
- Exact decimal values represented as strings at API boundaries.
- UUIDv7 identifiers for new records where PostgreSQL 18 generates IDs.
- Idempotency keys on all state-changing external commands.
- Usage ingestion deduplicates on tenant-scoped `source_event_key` and on `source_payload_hash` plus `event_contract_version`.
- CloudEvents-compatible envelopes in the event milestone.

## Usage-ingestion plane

The importable `metering_billing` package is the first runtime module.  Usage
ingest can run against `MemoryUsageLedger` for isolated reference tests or
against `PostgresUsageLedger` for the durable usage, measurement, catalog,
invoice, collection, payment, credit, cash-journal, write-off, settle-when-zero,
and webhook-delivery slice. Both
paths use the same hash, tenant, decimal, and
rating-identity rules; the PostgreSQL path owns the event and receipt write in
one transaction.  The PostgreSQL commercial vertical now continues through
rate-card publish, rating, invoice draft, issued-invoice snapshot, tenant-scoped
tax-rate publish, tax assessment, collection case/dunning, payment intent,
applied payment receipt, row-locked settlement, balanced cash journal proposal,
credit adjustment with collection reduction, balanced credit journal proposal,
issued-credit-note snapshot, unused issued-credit-note void, credit-note application, collection write-off with exact-zero outstanding,
explicit collection settlement,
and atomic commercial outbox enqueue. Provider capture and the remaining
commercial exception services still use the in-memory reference ledger until
their own durable repositories land. Canonical
source-payload hashing excludes envelope identifiers (`event_id`,
`source_event_key`), `source_payload_hash`, and `recorded_at`.  Batch ingest,
usage queries, and rating accept half-open ISO 8601 windows.  Rating identity
is tenant, window, persisted rate-card version, and usage-snapshot hash.
`UsageEventPresentmentService` projects a stored event as a statement.
`GET /v1/usage-events/{usage_event_id}` and `GET /v1/usage-events` are safe
tenant-scoped reads (Fielding et al., 2022; Google, 2024).  Ingest usage, then
rate a window against a published card.  `RatingRunPresentmentService`
projects a stored run as a statement.  `GET /v1/rating-runs/{rating_run_id}`
and `GET /v1/rating-runs` are safe tenant-scoped reads.  Rate a window, then
draft an invoice.

## Persistence plane

PostgreSQL is the authoritative store for normalized records in the durable
usage-to-issued-invoice, collection, payment, credit, cash-journal, write-off,
settle-when-zero, and webhook-delivery vertical. Raw webhook and usage
payloads will be stored immutably in S3-compatible object storage in a later
milestone; relational records retain hashes and references. PostgreSQL stores
subscription metadata, delivery attempts, and outbox delivered status, while
the one-time webhook secret remains process-local until a secure secret
provider is added. The broader commercial repository, production-default,
readiness, recovery, HA, and telemetry requirements remain tracked by issue
#84.

## Provider plane

Provider integration is capability-based. Checkout, subscription, usage export, invoice export, collection, refund, dispute, tax document, and settlement are separate ports. A provider implements only declared capabilities.

## Accounting plane

`metering_billing.AccountingExportService` produces semantically validated, balanced journal proposals from a persisted invoice draft, payment receipt, credit adjustment, collection write-off, leftover refund, parked leftover, leftover apply, issued-invoice void, or issued-credit-note void using semantic account roles and an intended book role. Cash proposals debit `cash_receipt` and credit `accounts_receivable`. Credit proposals debit `usage_revenue` and credit `accounts_receivable`. Write-off proposals debit `write_off_expense` and credit `accounts_receivable`. Refund proposals debit `unapplied_cash` and credit `cash_receipt`. Leftover-park proposals debit `cash_receipt` and credit `unapplied_cash`. Leftover-apply proposals debit `unapplied_cash` and credit `accounts_receivable`. Void proposals debit `usage_revenue` and credit `accounts_receivable`; taxed unused issues also debit `tax_payable`. Credit-note void proposals debit `accounts_receivable` and credit `usage_revenue`; taxed unused notes also credit `tax_payable`. Credit accept already composes the credit journal; `propose_credit_journal` and `POST /v1/credit-adjustments/{credit_adjustment_id}/journal-proposals` are the explicit compose or replay. Void accept does not compose; `propose_void_journal` and `POST /v1/issued-invoice-voids/{issued_invoice_void_id}/journal-proposals` are the explicit compose. Issued-credit-note void accept does not compose; `propose_credit_note_void_journal` and `POST /v1/issued-credit-note-voids/{issued_credit_note_void_id}/journal-proposals` are the explicit compose. The Accounting Information Platform resolves authoritative chart-account IDs, accounting policy, legal entity, accounting book, fiscal period, currency treatment, revenue recognition, and final posting. Billing never claims that posting.

`metering_billing.CollectionCaseService` opens commercial collection cases from those drafts and appends dunning reminders. Collection does not capture payment or post a journal.

`metering_billing.PaymentIntentService` projects a provider-neutral payment intent from a stored collection case. The intent does not capture, settle, store a card PAN, or post a journal.

`metering_billing.PaymentSettlementService` applies a commercial payment receipt against a projected intent and reduces collection outstanding. Receipts stay `applied` and do not capture via a provider or post a journal. Accept composes the existing cash journal so AIS can pull it immediately.

`metering_billing.http_app.create_http_app` exposes those services as stdlib JSON HTTP. The adapter requires a tenant on every write, returns published `as_contract_dict` contracts, and never posts a journal. After a tenant has one or more active API credentials, every `/v1` call except credential issue also requires a matching bearer or `X-CWL-Api-Key` secret (National Institute of Standards and Technology, 2020; OWASP, 2023; American Institute of Certified Public Accountants, 2017).

`GET /v1/journal-proposals` is a safe collection read (Fielding et al., 2022; Google, 2024). AIS pulls validated proposals and may pin `X-CWL-Tenant-Reference`. Body or query `tenant_reference` still works when the header is absent; a mismatch is HTTP 422. That pull keeps working until a key is issued for the tenant. Cash, AR, and credit rows share `journal_proposal`. Query does not flip `proposal_status`; AIS later returns `posting_receipt`. Billing emits semantic account roles only.

`metering_billing.PostingReceiptPullService` GETs that AIS receipt with stdlib `urllib` and stores a commercial observation. The consumed schema stays AIS-owned. `POST /v1/posting-receipt-observations` is the operator trigger; `GET /v1/posting-receipt-observations/{idempotency_key}` is a safe stored read and does not call AIS (Fielding et al., 2022). `PostingReceiptObservationPresentmentService` projects a stored observation as a statement. `GET /v1/posting-receipt-observations` lists `{posting_receipt_observations, next_cursor}` (Google, 2024). `posting_status_code` is not mapped onto `proposal_status`. If AIS returns 404, the operator accepts the proposal on AIS and retries.

`metering_billing.AisOutboxDrainService` consumes AIS `GET /outbox-events?event_type_code=posting_receipt` and then the same receipt GET (Fielding et al., 2022). Matching is equality against URNs constructed from our stored `proposal_id`; Billing does not parse `payload_reference`. Empty unpublished pages skip receipt GETs. After a stored observation, `POST /outbox-events/{outbox_event_id}/publish` is idempotent. `POST /v1/ais-outbox-drains` is the explicit drain. Drain AIS outbox, then store the receipt observation; AIS may keep being polled only when the outbox is non-empty.

`metering_billing.CollectionCasePresentmentService` projects a stored collection case into a commercial statement. IFRS 15 treats remaining outstanding as presentation of consideration, not collected revenue (IFRS Foundation, 2024). `GET /v1/collection-cases/{collection_case_id}` and `GET /v1/collection-cases` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). `operator_console` Storybook presents outstanding and the next action. Open the collection case, then collect or credit.

`metering_billing.CollectionAgingPresentmentService` projects stored open-case remaining into current / 1-30 / 31-60 / 61-90 / 90+ buckets grouped by currency. IFRS 15 treats remaining consideration as presentation, not a posted AR trial balance (IFRS Foundation, 2024). IAS 21 requires source currency to stay unmixed (IFRS Foundation, 2024). `GET /v1/collection-aging` is a safe tenant-scoped read (Fielding et al., 2022). `operator_console` Storybook presents those exact-decimal buckets. Open the aging statement, then collect or credit.

`metering_billing.AccountStatementPresentmentService` projects stored issued-invoice totals, unused issued-invoice voids, open collection remaining, applied credits, unused issued-credit-note voids, write-offs, parked leftover, and leftover refunds for one billing account, grouped by currency. `issued_invoice_total` stays the issued snapshot. `applied_credit_total` stays applied credits only. IFRS 15 treats remaining consideration as presentation, not a posted trial balance (IFRS Foundation, 2024). IAS 21 requires source currency to stay unmixed (IFRS Foundation, 2024). `GET /v1/billing-accounts/{billing_account_id}/statement` is a safe tenant-scoped read (Fielding et al., 2022). Missing account is 404. Cross-tenant account is 403. `operator_console` Storybook presents those exact-decimal currency rows. Open the account statement, then collect, credit, park, apply, or refund.

`metering_billing.RatedSpendPresentmentService` projects already-stored rating-run and exclusive invoice-draft line amounts for one billing account and half-open window, grouped by `product_code`. Optional `group_by=project` adds stored exclusive-account `project_reference` and omits usage without that URN. Optional `group_by=credential` adds stored exclusive-account `credential_reference` and omits usage without that URN. Optional `group_by=principal` adds stored exclusive-account `billing_principal_reference`. Mixed or unresolved principals omit the run. Optional `group_by=cost_center` adds stored exclusive-account `cost_center_reference` and omits usage without that URN. Mixed cost centers omit the run. IFRS 15 treats rated consideration as presentation, not collected revenue (IFRS Foundation, 2024). IAS 21 requires source currency to stay unmixed (IFRS Foundation, 2024). `GET /v1/billing-accounts/{billing_account_id}/rated-spend` is a safe tenant-scoped read (Fielding et al., 2022). Missing account is 404. Cross-tenant account is 403. An illegal window or unknown `group_by` is 422. The read does not re-rate, invent a unit price, or write a money fact. `operator_console` Storybook presents those exact-decimal product and project rows. Inspect rated product, project, credential, principal, or cost-center spend, then draft an invoice.

`metering_billing.SpendBudgetService` persists one append-only commercial spend budget for one billing account, half-open window, and currency. IFRS 15 treats a commercial budget as control evidence, not collected revenue (IFRS Foundation, 2024). Helland (2012) requires that a replay of the same identity return the stored `spend_budget_id` and not grow the outbox. IEEE 754 rejects binary floating-point money (IEEE, 2019). PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `POST /v1/billing-accounts/{billing_account_id}/spend-budgets` is the write. First successful publish enqueues one existing `spend_budget.published` outbox event. `SpendBudgetPresentmentService` projects the stored row. `GET /v1/spend-budgets/{spend_budget_id}` and `GET /v1/spend-budgets` are safe tenant-scoped reads and list `{spend_budgets, next_cursor}` (Fielding et al., 2022; Google, 2024). Missing account is 404. Cross-tenant account is 403. The write does not compare rated spend or compose a journal. Publish the commercial budget, then wait.

`metering_billing.SpendBudgetEvaluationPresentmentService` compares one published spend budget to already-rated spend for the same tenant, billing account, half-open window, and currency. IFRS 15 treats the comparison as control evidence, not collected revenue (IFRS Foundation, 2024). IAS 21 requires source currency to stay unmixed (IFRS Foundation, 2024). IEEE 754 rejects binary floating-point money (IEEE, 2019). `GET /v1/spend-budgets/{spend_budget_id}/evaluation` is a safe tenant-scoped read (Fielding et al., 2022). The read reuses `RatedSpendPresentmentService` with `group_by=product`. Same tenant is 200. Unknown or cross-tenant is 404. Missing tenant is 422. A billing account stored for another commercial `tenant_account` is 403. `GET /v1/billing-accounts/{billing_account_id}/budget-status` lists those evaluations as `{budget_statuses, next_cursor}` ordered by `published_at` then `spend_budget_id` (Fielding et al., 2022; Google, 2024). Same-tenant is 200. Unknown billing account is 404. Cross-tenant account is 403. Unknown or cross-tenant budgets are omitted with no leak. The read does not persist, hard-stop rating, or compose a journal. Inspect remaining, then wait. `operator_console` Storybook presents those exact-decimal under, at, and over rows plus the keyset cursor. Open the account budget status, then wait.

`metering_billing.SpendBudgetOverSignalService` observes one published spend budget with that same evaluation math and enqueues one existing `spend_budget.over` outbox event when utilization is first over. IFRS 15 treats the signal as control evidence, not collected revenue (IFRS Foundation, 2024). Helland (2012) requires that replay of the same observation not grow the outbox. IEEE 754 rejects binary floating-point money (IEEE, 2019). PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `POST /v1/spend-budgets/{spend_budget_id}/over-signal` is the write. Same tenant is 200. Unknown or cross-tenant is 404. Missing tenant is 422. A billing account stored for another commercial `tenant_account` is 403. under and at write zero over-signal rows. The write does not persist an evaluation snapshot, hard-stop rating, or compose a journal. `metering_billing.SpendBudgetOverSignalPresentmentService` presents the live over-signal envelope plus zero or one stored `spend_budget.over` outbox presentment. RFC 9110 treats GET as a safe, idempotent read (Fielding et al., 2022). `GET /v1/spend-budgets/{spend_budget_id}/over-signal` is the HTTP read. Same tenant is 200. Unknown or cross-tenant is 404. Missing tenant is 422. A billing account stored for another commercial `tenant_account` is 403. Replay writes no outbox row. Observe first-over utilization, then inspect the live observation or run deliveries. `metering_billing.SpendBudgetApproachingSignalService` observes that same evaluation math and enqueues one existing `spend_budget.approaching` outbox event when utilization is first `at` the documented `budget_amount`. IFRS 15 treats the signal as control evidence, not collected revenue (IFRS Foundation, 2024). Helland (2012) requires that replay of the same observation not grow the outbox. IEEE 754 rejects binary floating-point money (IEEE, 2019). PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `POST /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the write. Same tenant is 200. Unknown or cross-tenant is 404. Missing tenant is 422. A billing account stored for another commercial `tenant_account` is 403. under and over write zero approaching-signal rows. The write does not persist an evaluation snapshot, hard-stop rating, or compose a journal. `metering_billing.SpendBudgetApproachingSignalPresentmentService` presents the live approaching-signal envelope plus zero or one stored `spend_budget.approaching` outbox presentment. RFC 9110 treats GET as a safe, idempotent read (Fielding et al., 2022). `GET /v1/spend-budgets/{spend_budget_id}/approaching-signal` is the HTTP read. Same tenant is 200. Unknown or cross-tenant is 404. Missing tenant is 422. A billing account stored for another commercial `tenant_account` is 403. Replay writes no outbox row. Observe first-at utilization, then inspect the live observation or run deliveries.

`metering_billing.DunningEventPresentmentService` projects a stored `collection_dunning_event` into a commercial reminder statement. The reminder is not a statutory collection notice and does not send mail or SMS. `POST /v1/collection-cases/{collection_case_id}/dunning-events` stays the #10 record. `GET /v1/dunning-events/{dunning_event_id}` and `GET /v1/dunning-events` are safe tenant-scoped reads and never invent a send channel (Fielding et al., 2022; Google, 2024). Record the commercial reminder, then collect or credit.

`metering_billing.PaymentIntentPresentmentService` projects a stored payment intent into a commercial statement. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `POST /v1/payment-intents`, `GET /v1/payment-intents/{payment_intent_id}`, and `GET /v1/payment-intents` are tenant-scoped (Fielding et al., 2022; Google, 2024). `operator_console` Storybook presents the projected amount and the next action. Create a projected payment intent, then record the receipt.

`metering_billing.PaymentReceiptPresentmentService` projects a stored payment receipt into a commercial statement. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `POST /v1/payment-receipts`, `GET /v1/payment-receipts/{payment_receipt_id}`, and `GET /v1/payment-receipts` are tenant-scoped (Fielding et al., 2022; Google, 2024). Accept enqueues `payment_receipt.applied`, composes the existing cash journal, and does not flip `proposal_status`. `operator_console` Storybook presents the received amount and the next action. Record the receipt; the cash journal is already validated for AIS to pull.

`metering_billing.UnappliedCashService` parks leftover remittance against one same-tenant stored payment receipt. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). Identity is `(tenant_account_id, payment_receipt_id)` (Helland, 2012). `PostgresUsageLedger` persists the leftover with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row (Helland, 2012; IEEE, 2019). `POST /v1/payment-receipts/{payment_receipt_id}/unapplied-cash` is the nested park command. `UnappliedCashPresentmentService` projects the stored leftover. `GET /v1/unapplied-cash/{unapplied_cash_id}` and `GET /v1/unapplied-cash` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). `AccountingExportService.propose_unapplied_cash_journal` and `POST /v1/unapplied-cash/{unapplied_cash_id}/journal-proposals` compose one validated cash/unapplied-cash proposal for AIS to pull (Fielding et al., 2022). #12 still rejects overpay. Do not auto-apply leftover to another case. Park leftover against a stored receipt, compose the leftover journal, then later apply or refund it. `operator_console` Storybook presents that parked leftover as an exact-decimal string.

`metering_billing.UnappliedCashApplicationService` applies one parked leftover onto one same-tenant open collection case. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). Identity is `(tenant_account_id, unapplied_cash_id)` (Helland, 2012). `PostgresUsageLedger` persists the leftover-apply with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row or reduce remaining again once remaining already excludes the applied amount (Helland, 2012; IEEE, 2019). A crash after insert and before outstanding reduction is healed by the next replay. `POST /v1/collection-cases/{collection_case_id}/unapplied-cash-applications` is the nested apply command. First successful apply enqueues one existing `unapplied_cash.applied` commercial webhook outbox event (Fielding et al., 2022). `UnappliedCashApplicationPresentmentService` projects the stored application plus current remaining. `GET /v1/unapplied-cash-applications/{unapplied_cash_application_id}` and `GET /v1/unapplied-cash-applications` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). `AccountingExportService.propose_unapplied_cash_application_journal` and `POST /v1/unapplied-cash-applications/{unapplied_cash_application_id}/journal-proposals` compose one validated unapplied-cash/AR proposal for AIS to pull (Fielding et al., 2022). Remaining zero does not settle; #46 remains the explicit settle-when-zero command. Apply fail-closes when leftover is already refunded. Apply parked leftover, compose the apply journal, then collect the residual or settle at exact zero. `operator_console` Storybook presents that leftover-apply residual.

`metering_billing.UnappliedCashRefundService` records one commercial refund of parked leftover. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). Identity is `(tenant_account_id, unapplied_cash_id)` (Helland, 2012). `PostgresUsageLedger` persists the leftover refund with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row (Helland, 2012; IEEE, 2019). `POST /v1/unapplied-cash/{unapplied_cash_id}/refunds` is the nested refund command. First successful refund enqueues one existing `refund.recorded` commercial webhook outbox event (Fielding et al., 2022). `UnappliedCashRefundPresentmentService` projects the stored refund plus current leftover status. `GET /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}` and `GET /v1/unapplied-cash-refunds` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). `AccountingExportService.propose_refund_journal` and `POST /v1/unapplied-cash-refunds/{unapplied_cash_refund_id}/journal-proposals` compose one validated unapplied-cash/cash proposal for AIS to pull (Fielding et al., 2022). The parked leftover row stays `parked`. Refund unused leftover as a commercial fact; do not capture cards or call a PSP. `operator_console` Storybook presents that leftover-refund remaining snapshot.

`metering_billing.CreditAdjustmentService` records a commercial credit against a stored invoice draft and emits one validated journal proposal. IFRS 15 treats the credit as variable consideration, not as proof that revenue has been reversed in the statutory books (IFRS Foundation, 2024). When a tax assessment exists the inclusive credit is split proportionally and the journal debits semantic `tax_payable` as well as `usage_revenue` (IFRS Foundation, n.d.; International Organization for Standardization, 2015). ISO 20022 keeps the commercial credit note separate from a posted `camt` settlement (International Organization for Standardization, 2026). `POST /v1/credit-adjustments` records the credit; AIS later pulls the proposal. `AccountingExportService.propose_credit_journal` and `POST /v1/credit-adjustments/{credit_adjustment_id}/journal-proposals` compose or replay that same validated credit journal (Fielding et al., 2022). `CreditAdjustmentPresentmentService` projects the stored credit as a statement. `GET /v1/credit-adjustments/{credit_adjustment_id}` and `GET /v1/credit-adjustments` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). Record the credit; AIS pulls the validated journal.

`metering_billing.RateCardService` publishes a tenant-scoped catalog version with flat unit prices (TM Forum, 2024). Replay of the same tenant, card name, canonical line hash, and contract version returns the stored `rate_card_version` (Helland, 2012). `UsageRatingService` must resolve that persisted version; a missing metric fails closed and does not invent a price. `POST /v1/rate-cards` publishes a version. `RateCardPresentmentService` projects the stored card as a statement. `GET /v1/rate-cards/{rate_card_id}` and `GET /v1/rate-cards` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). Version GET routes stay the #18 catalog reads. Publish a rate card, then rate a window against that version.

`metering_billing.TaxRateService` publishes a tenant-scoped tax-rate version. `metering_billing.TaxAssessmentService` applies that version to a stored invoice draft and half-even rounds to ISO 4217 minor units (International Organization for Standardization, 2015; OECD, 2017). IFRS 15 keeps amounts collected for a tax authority out of revenue (IFRS Foundation, 2024). A taxed journal proposal credits semantic `tax_payable`; AIS must map that role and owns IAS 12 presentation (IFRS Foundation, n.d.). `POST /v1/tax-rates` and `POST /v1/tax-assessments` write; GET list, version, and assessment routes are safe tenant-scoped reads (Fielding et al., 2022). `TaxAssessmentPresentmentService` projects a stored assessment as a statement. `GET /v1/tax-assessments` lists `{tax_assessments, next_cursor}` (Google, 2024). Publish a tax rate, assess the draft, then propose the journal and let AIS pull.

`metering_billing.InvoicePresentmentService` projects a stored invoice draft into a commercial statement. IFRS 15 treats that statement as presentation of consideration, not earned revenue (IFRS Foundation, 2024). ISO 20022 keeps the commercial invoice document separate from a posted financial message (International Organization for Standardization, 2026). `GET /v1/invoice-drafts/{invoice_draft_id}` and `GET /v1/invoice-drafts` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). `operator_console` Storybook presents the same exact-decimal JSON. Open the draft statement, then collect or credit.

`metering_billing.IssuedInvoiceService` freezes one commercial invoice snapshot from a stored draft. IFRS 15 treats that snapshot as presentation of consideration, not a statutory invoice or earned revenue (IFRS Foundation, 2024). ISO 20022 keeps the commercial invoice document separate from a posted financial message (International Organization for Standardization, 2026). `POST /v1/invoice-drafts/{invoice_draft_id}/issued-invoices` is the nested issue command. First successful issue enqueues one existing `invoice.issued` commercial webhook outbox event (Fielding et al., 2022). `IssuedInvoicePresentmentService` projects the stored snapshot. `GET /v1/issued-invoices/{issued_invoice_id}` and `GET /v1/issued-invoices` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). Item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment amounts still match the frozen issued totals. The path does not invent sequential legal numbering, a VAT register, NTS/hometax field, or call AIS. Issue invoice, then collect or credit.

`metering_billing.IssuedInvoiceVoidService` records one commercial void of an unused issued invoice. IFRS 15 treats that void as presentation of unused issued consideration, not reversed revenue or a statutory posting (IFRS Foundation, 2024). Helland (2012) requires that a replay acknowledge the stored void and never re-close the case. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `POST /v1/issued-invoices/{issued_invoice_id}/voids` is the nested void command. First successful void enqueues one existing `invoice.voided` commercial webhook outbox event (Fielding et al., 2022). `IssuedInvoiceVoidPresentmentService` projects the stored void. `GET /v1/issued-invoice-voids/{issued_invoice_void_id}` and `GET /v1/issued-invoice-voids` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). An unused open or dunning case closes as `voided`; `settled` is not reused. The void command does not compose a journal. `AccountingExportService.propose_void_journal` and `POST /v1/issued-invoice-voids/{issued_invoice_void_id}/journal-proposals` compose one validated revenue/AR reverse for AIS to pull (Fielding et al., 2022). The path does not invent a second webhook system, AIS call, statutory account ID, or `journal_entry_id`. Void an unused issue, compose the journal, then let AIS pull.

`metering_billing.IssuedCreditNoteService` freezes one commercial credit-note snapshot from a stored credit adjustment. IFRS 15 treats that snapshot as presentation of a later price concession or billing correction, not a statutory credit note or reversed revenue (IFRS Foundation, 2024). ISO 20022 keeps the commercial credit-note document separate from a posted financial message (International Organization for Standardization, 2026). `PostgresUsageLedger` persists the issued row with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row (Helland, 2012; IEEE, 2019). `POST /v1/credit-adjustments/{credit_adjustment_id}/issued-credit-notes` is the nested issue command. First successful issue enqueues one existing `credit_note.issued` commercial webhook outbox event (Fielding et al., 2022). `IssuedCreditNotePresentmentService` projects the stored snapshot. `GET /v1/issued-credit-notes/{issued_credit_note_id}` and `GET /v1/issued-credit-notes` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). Item GET includes optional stored `tax_assessment_id` when the draft's commercial assessment still reproduces the frozen credit exclusive/tax split. The path does not invent sequential legal numbering, a VAT register, NTS/hometax field, or call AIS. Issue the credit note; the validated journal remains available for AIS.

`metering_billing.IssuedCreditNoteVoidService` records one commercial void of an unused issued credit note. IFRS 15 treats that void as presentation of unused issued credit, not reversed revenue or a statutory posting (IFRS Foundation, 2024). Helland (2012) requires that a replay acknowledge the stored void. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `PostgresUsageLedger` persists the unused void with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row (Helland, 2012; IEEE, 2019). `POST /v1/issued-credit-notes/{issued_credit_note_id}/voids` is the nested void command. First successful void enqueues one existing `credit_note.voided` commercial webhook outbox event (Fielding et al., 2022). `IssuedCreditNoteVoidPresentmentService` projects the stored void. `GET /v1/issued-credit-note-voids/{issued_credit_note_void_id}` and `GET /v1/issued-credit-note-voids` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). Collection remaining is unchanged because the note cannot have been applied. After a void exists, apply fail-closes as `issued_credit_note_voided`. The void command does not compose a journal. `AccountingExportService.propose_credit_note_void_journal` and `POST /v1/issued-credit-note-voids/{issued_credit_note_void_id}/journal-proposals` compose one validated AR/revenue reverse of the original credit journal for AIS to pull (Fielding et al., 2022). Missing original Billing credit proposal fails closed. The path does not invent a second webhook system, AIS call, statutory account ID, or `journal_entry_id`. Void an unused issued credit note, compose the journal, then let AIS pull.

`metering_billing.CreditNoteApplicationService` applies one issued commercial credit note onto one open collection case. IFRS 15 treats the reduced outstanding as presentation of remaining consideration, not reversed revenue (IFRS Foundation, 2024). Helland (2012) requires that a replay acknowledge the stored application and never double-reduce money. PCI DSS keeps card PAN off this path (PCI Security Standards Council, 2024). `PostgresUsageLedger` persists the application with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row (Helland, 2012; IEEE, 2019). `POST /v1/collection-cases/{collection_case_id}/credit-note-applications` is the nested apply command. First successful apply enqueues one existing `credit_note.applied` commercial webhook outbox event (Fielding et al., 2022). `CreditNoteApplicationPresentmentService` projects the stored application. `GET /v1/credit-note-applications/{credit_note_application_id}` and `GET /v1/credit-note-applications` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). A voided unused note fail-closes as `issued_credit_note_voided`. The path does not invent a journal, tax unwind, second webhook system, write-off, settlement, or AIS call. Apply the issued credit note, then collect the residual.

`metering_billing.CollectionCaseSettlementService` settles one open collection case whose remaining outstanding is exact zero. IFRS 15 treats zero remaining consideration as presentation, not a write-off or reversed revenue (IFRS Foundation, 2024). Helland (2012) requires that a replay acknowledge the stored settlement and never double-settle. `POST /v1/collection-cases/{collection_case_id}/settlements` is the nested settle command. First successful settle enqueues one existing `collection.settled` commercial webhook outbox event (Fielding et al., 2022). `CollectionCaseSettlementPresentmentService` projects the stored settlement. `GET /v1/collection-case-settlements/{collection_case_settlement_id}` and `GET /v1/collection-case-settlements` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). A voided case fail-closes as `collection_case_voided`. The path does not invent a journal, tax unwind, second webhook system, write-off, payment receipt, or AIS call. Settle the zero-outstanding case, then wait.

`metering_billing.CollectionDisputeService` holds one open or dunning collection case as a commercial dispute. IFRS 15 treats remaining consideration as still owed; the hold pauses dunning without reversing revenue or posting (IFRS Foundation, 2024). Helland (2012) requires that a replay acknowledge the stored hold and never change remaining. `PostgresUsageLedger` persists the collection dispute with tenant-scoped composite foreign keys and exact `numeric` amounts; replay does not insert a second row (Helland, 2012; IEEE, 2019). `POST /v1/collection-cases/{collection_case_id}/disputes` is the nested hold command. First successful hold enqueues one existing `dispute.held` commercial webhook outbox event (Fielding et al., 2022). `CollectionDisputePresentmentService` projects the stored hold. `GET /v1/collection-disputes/{collection_dispute_id}` and `GET /v1/collection-disputes` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). New dunning, payment, credit apply, leftover apply, write-off, settle-when-zero, and void fail closed while held. The path does not invent a journal, second webhook system, write-off rewrite, void rewrite, statutory numbering, or AIS call. Hold the disputed case, then wait. `operator_console` Storybook presents one held dispute with exact remaining. `metering_billing.CollectionDisputeReleaseService` releases one held dispute in place. Helland (2012) requires that a replay acknowledge the stored release and never change remaining. `POST /v1/collection-disputes/{collection_dispute_id}/releases` is the nested release command. First successful release enqueues one existing `dispute.released` commercial webhook outbox event (Fielding et al., 2022). `CollectionDisputeReleasePresentmentService` projects the stored release. `GET /v1/collection-dispute-releases/{collection_dispute_id}` and `GET /v1/collection-dispute-releases` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). After release, dunning and money-close commands follow the existing open-case rules. The path does not invent a journal, second webhook system, write-off rewrite, void rewrite, statutory numbering, or AIS call. Release the hold, then collect or dunn. `operator_console` Storybook presents that released/fail-close remaining snapshot.

`metering_billing.CollectionWriteOffService` writes off leftover remaining on one open collection case. IFRS 15 treats that commercial write-off as presentation of remaining consideration, not reversed revenue or a statutory posting (IFRS Foundation, 2024). Helland (2012) requires that a replay acknowledge the stored write-off and never re-zero money. `POST /v1/collection-cases/{collection_case_id}/write-offs` is the nested write-off command. First successful write-off enqueues one existing `write_off.recorded` commercial webhook outbox event (Fielding et al., 2022). `CollectionWriteOffPresentmentService` projects the stored write-off. `GET /v1/collection-write-offs/{collection_write_off_id}` and `GET /v1/collection-write-offs` are safe tenant-scoped reads (Fielding et al., 2022; Google, 2024). `AccountingExportService.propose_write_off_journal` and `POST /v1/collection-write-offs/{collection_write_off_id}/journal-proposals` compose one validated write-off/AR proposal for AIS to pull (Fielding et al., 2022). The path does not invent a tax unwind, second webhook system, settlement command, payment receipt, credit note, statutory account ID, or AIS call. Write off leftover remaining, compose the journal, then settle.

`metering_billing.TenantApiCredentialService` issues append-only HTTP API credentials. NIST SP 800-63B requires the verifier to store a keyed hash, never the recoverable secret (National Institute of Standards and Technology, 2020). OWASP treats leaked keys as revocable bearer credentials (OWASP, 2023). SOC 2 CC6 requires logical access control on a shippable HTTP surface (American Institute of Certified Public Accountants, 2017). `POST /v1/tenant-api-credentials` returns the secret once. `TenantApiCredentialPresentmentService` projects stored metadata as a commercial statement. `GET /v1/tenant-api-credentials/{tenant_api_credential_id}` and `GET /v1/tenant-api-credentials` are safe tenant-scoped reads and never include the secret (Fielding et al., 2022; Google, 2024). `GET /healthz` stays unauthenticated. Issue a key, then send it on every `/v1` call; revoke when leaked.

`metering_billing.WebhookSubscriptionService` registers an https callback and returns the signing secret once. The verifier stores a keyed HMAC, never the recoverable secret in SQL (Krawczyk et al., 1997; National Institute of Standards and Technology, 2020). `PostgresUsageLedger` persists subscription metadata, delivery attempts, and the delivered outbox transition with tenant-scoped predicates and composite foreign keys; its one-time secret remains process-local, so restart-safe unattended delivery still requires a secure secret provider. `WebhookSubscriptionPresentmentService` projects stored metadata as a commercial statement. `GET /v1/webhook-subscriptions/{webhook_subscription_id}` and `GET /v1/webhook-subscriptions` are safe tenant-scoped reads and never include the secret (Fielding et al., 2022; Google, 2024). `WebhookDeliveryService.deliver_due_events` POSTs the published commercial envelope and signs the raw body; receivers check `X-CWL-Webhook-Signature` rather than HTTP Message Signatures (Fielding et al., 2022; Backman et al., 2024). `WebhookOutboxEventPresentmentService` projects a stored commercial `webhook_outbox_event` as metadata only. `GET /v1/webhook-outbox-events/{outbox_event_id}` and `GET /v1/webhook-outbox-events` are safe tenant-scoped reads and never publish, send, or return `payload_json` or the webhook secret (Fielding et al., 2022; Google, 2024). `WebhookDeliveryPresentmentService` projects a stored `webhook_delivery_attempt` as a commercial statement. `GET /v1/webhook-deliveries/{delivery_attempt_id}` and `GET /v1/webhook-deliveries` are safe tenant-scoped reads and never resend or return the secret or signed body (Fielding et al., 2022; Google, 2024). Register an https callback, then run deliveries; AIS may keep polling.

## Security

- No card number, CVC, provider secret, PAT plaintext, prompt, response, tenant API credential plaintext, or webhook-subscription plaintext is accepted in billing contracts after issue or register. The issue and register responses are the only places those secrets appear.
- Webhooks are evidence until signature verification and normalization succeed.
- Tenant isolation is enforced in the application and database layers.
- Historical facts are corrected by compensating records.
